[build-system]
requires = ["setuptools>=68", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-thpcf"
version = "0.2.1"
description = "Blind two-harmonic phase/chirality finite-core trefoil falsifier"
requires-python = ">=3.10"
dependencies = ["numpy>=1.26"]
