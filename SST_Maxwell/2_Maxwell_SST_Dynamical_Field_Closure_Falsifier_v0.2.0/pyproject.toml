[project]
name = "2-maxwell-sst-dynamical-field-closure-falsifier"
version = "0.2.0"
description = "Blind Maxwell-SST DFC falsifier with relaxed-knot intake and pybind11 C++ acceleration"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
