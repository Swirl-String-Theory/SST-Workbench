[project]
name = "sst-ssdl-audit"
version = "0.2.0"
description = "Research-track audit harness for SST Separatrix Surface-Density Lift (SSDL)."
requires-python = ">=3.10"
dependencies = ["numpy"]

[tool.setuptools]
packages = ["sst_ssdl_audit"]
