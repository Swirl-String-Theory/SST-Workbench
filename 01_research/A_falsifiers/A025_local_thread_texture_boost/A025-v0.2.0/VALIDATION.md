# Validation — SST Local Thread Texture + Boost Invariance Blind Falsifier v0.2.1

## Generation-environment checks

The package was checked in the generation environment with:

- Python syntax compilation of all `.py` files: **PASS**.
- Pure-Python synthetic trefoil selftest: **PASS**.
- Pure-Python two-geometry blind QUICK campaign: **PASS** for structural and conditional bridge gates.
- Test fixed-core resolution ladder on a synthetic geometry: **PASS**.
- Common-boost Kabsch residual in the synthetic selftest: approximately machine precision.
- Closed-thread construction: zero filament endpoints by connectivity; local closing edge is continuous with neighboring discretization.
- Return-flux test: local outgoing-leg discretization is invariant when the remote closure distance changes.

## Native build status in this environment

The generation container does not have the `pybind11` package/headers installed, so the C++ extension was **not compiled here**.  This is not recorded as a native PASS.

On Windows, the one-click chains intentionally require:

```cmd
run_build_native.cmd
run_selftest.cmd
```

before the physical campaign.  `run_selftest.cmd` uses `--require-native` and therefore fails if the C++ backend is not actually loaded or if native-vs-Python parity exceeds tolerance.

## Important interpretation

A successful execution does not establish SST.  The output explicitly separates:

- structural/covariance/locality gates G0--G5;
- conditional thread-dynamics gates G6--G9;
- fixed-core convergence G10.

The primary scientific improvement over v0.1.0 is that the background field is now generated by committed closed vortex filaments rather than a prescribed curl-free velocity texture.


## v0.2.1 Windows build hotfix

The first real Windows run with Python 3.14.3, pybind11 3.1.0 and setuptools 84.0.0 exposed a packaging-only failure before C++ compilation:

```text
Multiple top-level packages discovered in a flat-layout: ['cpp', 'config', 'sst_thread_falsifier']
```

The generated setuptools build script now passes an explicit package list:

```python
packages=["sst_thread_falsifier", "sst_thread_falsifier.native_ext"]
```

This disables ambiguous flat-layout auto-discovery while leaving the extension target `sst_thread_falsifier.native_ext._native` unchanged.  This is a build-system correction only; no scientific gate or numerical kernel was altered.

The root `pyproject.toml` also declares the identical package list under `[tool.setuptools]`, so both the temporary native build and any root setuptools invocation avoid flat-layout auto-discovery.
