# A037-v0.3.2 symmetry blocks

Pure adapter around the existing paper-upgrade selection-matrix rule. The transport thresholds and CAMPAIGN matrix are not retuned.

`symmetry_blocks.json` records integer matrix `[[1,0,0],[0,1,1],[0,1,1]]` and blocks `[[0],[1,2]]` with `measured_response_claim: false`.
