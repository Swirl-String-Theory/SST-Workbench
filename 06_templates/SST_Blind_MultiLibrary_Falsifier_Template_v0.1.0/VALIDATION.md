# Validation — SST Blind Multi-Library Falsifier Template v0.1.0

- Core Python tests: **3 / 3 PASS**.
- `tools/new_falsifier.py` instantiation smoke: **PASS**; a standalone package was generated and its example gate-ledger pipeline executed.
- The example experiment is infrastructure-only and carries no scientific claim.
- Native C++ source is included; compilation is intentionally performed after `run_00_setup.cmd` installs `pybind11` in the target environment.
- Every real instance must replace/freeze the six scientific contracts in `EXPERIMENT_CONTRACT.md` before inspecting campaign outcomes.
