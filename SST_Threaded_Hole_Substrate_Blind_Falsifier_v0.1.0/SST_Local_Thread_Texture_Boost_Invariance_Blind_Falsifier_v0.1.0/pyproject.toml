[build-system]
requires = ["setuptools>=75", "wheel", "pybind11>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-local-thread-texture-falsifier"
version = "0.1.0"
description = "Blind boost-invariance vs local thread-texture falsifier for SST centerline datasets"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0", "pybind11>=3.0"]
