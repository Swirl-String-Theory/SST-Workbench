# Discovery → Recipe → Catalog workflow

```text
3.1 seed
  -> force/parameter matrix
  -> strict logs + checkpoint outputs
  -> matrix-effect metrics
  -> downstream physical stability tests
  -> explicit recipe selection/approval
  -> regenerate catalog scripts with recipe hash
  -> run knots/links/tori catalog
```

The catalog must never be generated from an implicit KnotPlot default.
