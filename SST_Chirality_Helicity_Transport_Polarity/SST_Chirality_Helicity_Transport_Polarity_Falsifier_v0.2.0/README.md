# SST Chirality–Helicity Transport Polarity Blind Falsifier v0.2.0

**Purpose.** Test whether a finite-core vortex knot/link possesses a dynamically generated, mirror-odd directional response that survives numerical, relative-equilibrium, localization, gauge and amplification gates.

This release is a methodological replacement of v0.1.0. It does **not** infer transport polarity from helicity. Helicity is an independent pseudoscalar diagnostic; transport is generated by trajectory-based linearized finite-core Biot–Savart dynamics.

## One-click Windows run

From the package directory:

```bat
run_all.cmd
```

Default dataset:

```text
..\..\KnotPlot\knots\final
```

The chain performs:

```text
setup -> native C++ build -> self-test -> prepare/seal inputs
      -> blind trajectory dynamics -> blind analysis -> BLIND zip
```

After reading and saving `REPORT_BLIND.md`, reveal explicitly:

```bat
run_40_reveal.cmd outputs\basic_YYYYMMDD_HHMMSS
```

That verifies the blind seal and private commitment before producing:

```text
REVEALED_RESULTS.json
REPORT_REVEALED.md
REVEAL_MAPPING.json
archives\basic_..._REVEALED.zip
```

The private mapping is **not present under `outputs\...` during the blind run**. It is stored as a random key file under `private_reveal_keys\` and therefore is not included in the automatically generated `*_BLIND.zip`.

---

## Physical operator

The native backend evaluates a Rosenhead-type regularized polygonal Biot–Savart field

\[
\mathbf v(\mathbf x)
=
\frac{\Gamma}{4\pi}
\sum_j
\frac{
\Delta\boldsymbol\ell_j\times(\mathbf x-\mathbf m_j)
}{
\left(|\mathbf x-\mathbf m_j|^2+a^2\right)^{3/2}
},
\]

where \(\mathbf m_j\) is the midpoint of segment \(j\), \(a\) is the finite-core regularization radius and \(\Gamma\) is circulation.

All source geometries are normalized to total arclength

\[
L=\sum_\alpha L_\alpha=1,
\]

so reported `core_fraction` is \(a/L\).

For the SST canonical values

\[
r_c=1.40897017\times10^{-15}\ \mathrm m,
\qquad
\mathbf{v}_{\!\boldsymbol{\circlearrowleft}}
=1.09384563\times10^6\ \mathrm{m\,s^{-1}},
\]

one useful circulation scale is

\[
\Gamma_c=2\pi r_c\mathbf{v}_{\!\boldsymbol{\circlearrowleft}}
=9.683619203488876\times10^{-9}\ \mathrm{m^2\,s^{-1}}.
\]

The simulations themselves use nondimensional \(L=1\), \(\Gamma=1\); physical times rescale with \(L^2/\Gamma\).

---

## 1. True multi-component geometry

v0.1.0 flattened every source to one closed polygon. That is invalid for links because it can create artificial connector segments between components.

v0.2.0 stores geometry as

\[
K=\{C_1,C_2,\ldots,C_m\}
\]

with a packed coordinate array plus explicit component offsets. Each component closes only onto itself. The velocity is

\[
\mathbf v(\mathbf x)=\sum_{\alpha=1}^{m}\mathbf v_\alpha(\mathbf x).
\]

Supported component detection, in descending reliability:

1. native Geomview/KnotPlot `VECT` component counts;
2. blank-line / block-separated XYZ files;
3. large inter-block gap detection for concatenated XYZ exports;
4. for `torus_p.q...` only, equal contiguous splitting when `gcd(p,q)>1` and the point count is compatible.

A generic filename containing `link` that still parses as one component is rejected by default. v0.2.0 prefers dropping an ambiguous source over inventing a connector.

---

## 2. Physical parity partner

For reflection matrix \(R\) with \(\det R=-1\), every component is reflected and its point orientation is reversed. At fixed positive circulation this implements the correct axial transformation of vorticity:

\[
\mathbf v\xrightarrow{P}R\mathbf v,
\qquad
\boldsymbol\omega\xrightarrow{P}-R\boldsymbol\omega.
\]

The mirror identity is randomly assigned to anonymous labels `A` and `B`.

The primary helicity proxy must satisfy

\[
\Xi_H[PK]\simeq-\Xi_H[K].
\]

Time reversal is tested separately by reversing component orientation without coordinate reflection. Then both \(\mathbf v\) and \(\boldsymbol\omega\) reverse and helicity remains even.

---

## 3. Helicity diagnostics

### Primary operator-consistent line diagnostic

v0.2.0 uses

\[
\boxed{
\Xi_H^{\mathrm{CL}}
=
\frac{1}{\Gamma}
\sum_\alpha
\oint_{C_\alpha}
\mathbf v_{\mathrm{BS}}\cdot d\boldsymbol\ell
}
\]

with the same finite-core Biot–Savart operator used by the dynamics. For the nondimensional equal-circulation runs, \(\Gamma=1\).

### Independent Gauss diagnostic

`xi_gauss` evaluates the polygonal Gauss double integral. For multiple equal-circulation thin tubes it includes self-writhe contributions and twice the pairwise linking contributions.

### Optional Gaussian tube cross-check

`extended.json` and `full.json` can also reconstruct a Gaussian vorticity tube and evaluate a numerical volume integral for \(H/\Gamma^2\). This is secondary because the dataset contains centerlines, not independently resolved internal twist fields.

---

## 4. Relative-equilibrium gate

A relaxed geometric knot is not automatically a dynamical equilibrium. Before a transport claim is accepted, v0.2.0 fits the instantaneous filament velocity to the best global rigid motion

\[
\mathbf F_i
\approx
\mathbf U+\boldsymbol\Omega\times\mathbf X_i.
\]

The weighted residual is

\[
\boxed{
\epsilon_{\mathrm{RE}}
=
\frac{
\left\|\mathbf F-(\mathbf U+\boldsymbol\Omega\times\mathbf X)\right\|_w
}{
\|\mathbf F\|_w
}.
}
\]

A candidate with \(\epsilon_{\mathrm{RE}}\) above the configured tolerance can still be simulated for diagnostic information, but it cannot receive a physical transport `PASS`; it is classified as

```text
INDETERMINATE_NOT_RELATIVE_EQUILIBRIUM
```

This prevents frozen-base amplification from being misreported as a propagation effect.

---

## 5. Trajectory-based variational dynamics

The baseline now evolves:

\[
\dot{\mathbf X}=\mathbf F(\mathbf X).
\]

The perturbation follows the variational equation along that evolving trajectory:

\[
\boxed{
\dot{\delta\mathbf X}
=
D\mathbf F[\mathbf X(t)]\,\delta\mathbf X.
}
\]

No full Jacobian is required for the primary run. The Jacobian-vector product is evaluated matrix-free by a symmetric finite difference:

\[
D\mathbf F[\mathbf X] \delta\mathbf X
\approx
\frac{
\mathbf F(\mathbf X+\varepsilon\widehat{\delta\mathbf X})
-
\mathbf F(\mathbf X-\varepsilon\widehat{\delta\mathbf X})
}{2\varepsilon}
\,\|\delta\mathbf X\|_{\mathrm{RMS}}.
\]

The baseline uses RK4 and retains its four RK stage states for every step. Tangent propagation uses those exact same stage states, so the discrete variational update is consistent with the discrete base map. No post-step recentering or other hidden gauge projection is applied. The step size is selected from the finite-core/segment CFL scale and is rejected if the configured maximum step count is insufficient.

No arclength reparameterization is silently applied during this trajectory, because doing so without differentiating the reparameterization map would contaminate the variational equation. Instead `ds_CV`, length drift and shape drift are explicit gates.

---

## 6. Parity-neutral packet

A localized transverse standing packet is initialized on one component:

\[
a(s,0)
=
\exp\!\left[-\frac{d(s,s_0)^2}{2\sigma^2}\right]
\cos\!\left[2\pi m(s-s_0)\right],
\qquad
b(s,0)=0.
\]

It contains no imposed one-way propagation preference.

Before observables are evaluated, the best rigid perturbation

\[
\delta\mathbf X_{\mathrm{rigid}}
=
\delta\mathbf U+\delta\boldsymbol\Omega\times\mathbf X
\]

is subtracted. Tangential perturbation energy is also measured separately so gauge/reparameterization contamination cannot masquerade as transverse transport.

---

## 7. Three separate response observables

### A. Smooth signed directional energy moment — primary `transport_pi`

A hard left/right split on a closed discrete loop can become grid-sensitive when a sample lies exactly on the antipode. v0.2.0 therefore uses a smooth periodic odd weight. For \(\Delta s=s-s_0\), define

\[
W(\Delta s)
=
\sin(2\pi\Delta s)
\left[\frac{1+\cos(2\pi\Delta s)}{2}\right]^p,
\qquad p=2\ \mathrm{by\ default}.
\]

The primary directional response is

\[
\boxed{
\Pi_{\mathrm{dir}}
=
\frac{\sum_j E_j W(\Delta s_j)}{\sum_j E_j |W(\Delta s_j)|}.
}
\]

Because \(W(-\Delta s)=-W(\Delta s)\), this observable is analytically mirror-odd; it also vanishes at the launch point and at the antipode. The initial numerical offset is subtracted, so the reported `transport_pi` is generated by the dynamics. A hard-window split is retained only as a secondary diagnostic.

### B. Circular centroid drift

The transverse-energy centroid is obtained from

\[
Z_E=\sum_j E_j e^{2\pi i s_j},
\qquad
s_E=\frac{\arg Z_E}{2\pi},
\]

and reported as signed circular displacement/velocity.

### C. Amplification

The tangent norm is tracked separately:

\[
G=\ln\frac{\|\delta\mathbf X(T)\|}{\|\delta\mathbf X(0)\|}.
\]

Large \(G\) does not count as transport. It triggers an amplification-dominated indeterminate gate when it exceeds the configured threshold.

The older positive/negative Fourier-power polarization remains as a secondary diagnostic only.

---

## 8. Local chirality texture

Because v0.1.0 showed that global \(|\Xi_H|\) did not predict \(|\Pi|\), v0.2.0 records the local parity-odd differential texture

\[
\chi_{\mathrm{loc}}(s)
=
\mathbf t\cdot
\left(
\frac{d\mathbf t}{ds}
\times
\frac{d^2\mathbf t}{ds^2}
\right).
\]

For each launch point, the code stores `local_chirality_at_center` and the candidate-level correlation between local chirality and packet response. This is diagnostic in v0.2.0, not a pass criterion.

---

## 9. Blind decision hierarchy

A mirror-odd signal cannot receive `PASS` until all earlier gates are satisfied. The order is:

```text
INVALID_PARITY_HELICITY
INVALID_PARITY_RELATIVE_EQUILIBRIUM
INVALID_EXCITATION_PARITY
INVALID_PARITY_SPECTRUM               [only if spectral diagnostic enabled]
INVALID_TRAJECTORY_TIMESTEP
INVALID_TRAJECTORY_MESH
INDETERMINATE_NOT_RELATIVE_EQUILIBRIUM
INDETERMINATE_SHAPE_DRIFT
INDETERMINATE_GAUGE_CONTAMINATION
INDETERMINATE_AMPLIFICATION_DOMINATED
NULL_NO_DIRECTIONAL_SIGNAL
INDETERMINATE_LOCALIZATION_SENSITIVE
PASS_SINGLE_COMPONENT_MIRROR_ODD_TRANSPORT
PASS_MULTICOMPONENT_MIRROR_ODD_TRANSPORT
FAIL_MIRROR_ODD_TRANSPORT
```

The fundamental mirror prediction is

\[
\Pi[PK]\stackrel{?}{=}-\Pi[K],
\qquad
|\Pi[PK]|\stackrel{?}{=}|\Pi[K]|.
\]

This is also checked excitation-by-excitation. Because the physical parity construction reverses component parameterization, a launch at \(s_0\) on `A` is paired with \((-s_0)\bmod 1\) on `B`, at fixed component index and carrier mode. A good aggregate average therefore cannot hide a local parity failure.

Racemic cancellation follows as

\[
\Pi_{\mathrm{racemic}}
=
\frac{\Pi[K]+\Pi[PK]}{2}
\stackrel{?}{\longrightarrow}0.
\]

The pair-invariant coupling diagnostic is

\[
\boxed{
\beta_i
=
\frac{\Pi_A-\Pi_B}{\Xi_{H,A}-\Xi_{H,B}},
}
\]

which is unchanged if the hidden labels \(A\leftrightarrow B\) are swapped.

Statistical correlations use **one mirror pair as one independent unit**. The two parity partners are never counted as two independent observations.

---

## 10. Campaigns

### BASIC

```bat
run_all.cmd
```

Target-priority patterns favor, when present:

- \(3_1\) trefoil;
- \(7_4\);
- \(4_1\) achiral topological control;
- \(T(2,7)\);
- selected multi-component torus/link sources.

If a targeted source cannot be parsed without ambiguous component connectivity, it is privately skipped and another valid source is used.

### EXTENDED

```bat
run_all_extended.cmd
```

Runs a resolution/core-radius cube, more packet positions and carrier modes, and the Gaussian tube-helicity cross-check.

### FULL

```bat
run_all_full.cmd
```

Runs over all parseable source geometries with stricter gates.

### Frozen spectral diagnostic

```bat
run_all_spectral_diagnostic.cmd
```

This additionally constructs the full initial transverse Jacobian for small geometries and checks mirror spectral parity. It is deliberately labeled **frozen spectral diagnostic**. It is not advertised as true Floquet monodromy.

A genuine Floquet/RPO release requires identification of a periodic or relative-periodic base trajectory and construction of the monodromy map over that period. v0.2.0 provides the trajectory-JVP machinery needed for that later step but does not fake a Floquet result.

---

## 11. What would count as a meaningful positive result?

For a single-component knot, the strongest v0.2.0 positive case is:

\[
\epsilon_{\mathrm{RE}}\ll1,
\]

stable trajectory/mesh gates,

\[
\Pi\neq0,
\qquad
\Pi[PK]\simeq-\Pi[K],
\]

low localization sensitivity, moderate amplification, and convergence of sign/magnitude across \(N\) and \(a/L\).

A much more informative negative outcome is also possible:

\[
\Xi_H[PK]=-\Xi_H[K]
\]

passes to high precision while

\[
\Pi\to0
\]

under refinement. That would directly reject this finite-core directional-transport channel even though chirality/helicity itself remains well defined.

---

## 12. Known limits

1. This is a regularized vortex-filament model, not full 3D incompressible Euler DNS.
2. All components currently use equal fixed circulation \(\Gamma=+1\) before parity/time transformations. Mixed circulation strengths are not yet a campaign parameter.
3. Core deformation, axial core flow and independent internal twist fields are not dynamically resolved.
4. The trajectory is not reparameterized; long runs that distort point spacing are rejected rather than silently repaired.
5. `xi_gauss` and the Gaussian tube reconstruction use polygonal/quadrature approximations and must be checked for resolution convergence.
6. A hydrodynamic mirror-odd transport channel would be an SST-relevant structural result; it would **not** by itself establish the microscopic quantum mechanism of CISS.

See `REFERENCES.tex` and `CHANGELOG.md`.
