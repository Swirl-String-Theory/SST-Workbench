{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CSV logical schema for explicit active struts",
  "description": "The actual sidecar is CSV. Required columns: comp_a,s_norm,comp_b,t_norm. Optional multiplier is the solver-supplied nonnegative KKT multiplier.",
  "required_columns": ["comp_a", "s_norm", "comp_b", "t_norm"],
  "optional_columns": ["multiplier"]
}
