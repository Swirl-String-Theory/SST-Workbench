# A029-v0.4.0 independent observable capture

v0.3.0 accounting remains: \(\Phi_{\rm loop}\) is derived. This version adds the raw \(a_m(t)\) contract and a phase-blind return detector. It does not invent a time-domain trajectory from \(e^{\lambda t}\).

PRED_M0 is the leave-one-carrier-out null. PRED_M1 is advection at \(k_{\rm ref}\). PRED_M2 adds intrinsic frequency at \(k_{\rm ref}\). PRED_M3 moves to \(k_{\rm closed}\) with holonomy embedded in \(k\) only.

Without a scientific raw series the residual is `INDETERMINATE_NO_RAW_MODAL_TIMESERIES`. C006 / A031 / A021 stay closed.
