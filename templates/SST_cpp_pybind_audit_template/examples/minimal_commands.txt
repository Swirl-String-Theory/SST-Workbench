# Quick smoke tests — run from the project root (this template folder or your copy).

# Build C++ backend when available; otherwise Python fallback remains usable.
python -m native_ext.build_ext_if_needed --force

# Default smoke run (a=2, b=3, expects 5).
python run_example.py

# One-line summary.
python run_example.py --summary-only

# Compare C++ vs Python fallback on the same inputs.
python run_example.py --a 2 --b 3 --out example_cpp.json
python run_example.py --a 2 --b 3 --force-python --out example_python.json

# Minimal sweep.
python run_sweep.py --values-a 1,2 --values-b 0.5,1.5 --out-json example_sweep.json --out-csv example_sweep.csv

# Full check battery -> audit_out/
python run_all_checks.py --out-dir audit_out
