[build-system]
requires = ["setuptools>=70", "wheel>=0.43", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-counterpulley-alpha-falsifier"
version = "0.5.0"
description = "Blinded C++/pybind11 + Python Newton-Krylov multiple-shooting falsifier for relative-periodic counter-rotating vortex orbits and true Floquet monodromy."
requires-python = ">=3.10"
dependencies = ["numpy>=1.26", "pybind11>=2.12", "setuptools>=70", "wheel>=0.43"]
