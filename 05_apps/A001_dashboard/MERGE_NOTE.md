# SST-dashboard merge

Flattened former `sstcore/` and `swirl/` trees into this folder.
Sole content conflict: `exports/ideal.txt` kept from sstcore; swirl copy at `_merge_conflict/exports/ideal.txt`.
