SST Phase-Feedback Delay Knot Stability Blind Falsifier v0.1.3
================================================================

Hotfix: PowerShell ExecutionPolicy-independent input discovery.

Observed target-machine failure:
  resolve_input.ps1 cannot be loaded because it is not digitally signed.

v0.1.3 changes:
1. Adds resolve_input.py implementing the v0.1.2 content-based resolver.
2. _RESOLVE_INPUT.cmd no longer invokes PowerShell at all.
3. Python selection order:
     .venv\Scripts\python.exe
     py -3
     python
4. Existing scientific preregistration, native kernels, delay gates,
   nonlinear measurement, blinding and reveal logic are unchanged.
5. resolve_input.ps1 is retained only for audit/back-compatibility and is unused.
