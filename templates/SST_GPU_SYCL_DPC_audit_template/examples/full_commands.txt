# Full command reference — GPU-first SYCL worker template (Arc A770).
# Session setvars only; no permanent PATH. GPU via sst_sycl_worker.exe.

# ---------------------------------------------------------------------------
# Windows one-command launchers
# ---------------------------------------------------------------------------
run_install.cmd
run_sycl_worker_smoke.cmd
run_all.cmd
run_arc.cmd

# ---------------------------------------------------------------------------
# Host OpenMP .pyd (no SYCL device code in CPython)
# ---------------------------------------------------------------------------
python -m native_ext.build_ext_if_needed --force
python -m native_ext.build_ext_if_needed --strict

# ---------------------------------------------------------------------------
# External worker tools
# ---------------------------------------------------------------------------
python tools\build_sycl_worker.py --force --strict
python -X faulthandler tools\sycl_worker_smoke.py

# Optional env (set by run_*.cmd):
#   set ONEAPI_DEVICE_SELECTOR=level_zero:gpu
#   set SYCL_CACHE_PERSISTENT=0
#   set SST_SYCL_ALLOW_FP32=1

# ---------------------------------------------------------------------------
# run_example.py
# ---------------------------------------------------------------------------
python run_example.py --tiny --backend python
python run_example.py --n-segments 512 --n-queries 8192 --backend sycl --summary-only
python run_example.py --n-segments 64 --n-queries 32 --backend openmp --out example_openmp.json

# ---------------------------------------------------------------------------
# run_all_checks.py (default: {folder}_outputs/)
# ---------------------------------------------------------------------------
python run_all_checks.py --force-python
python run_all_checks.py --force-build --backend sycl
