# Reproducibility inputs

These are exact copies of the two local `3_1` sources used by the blind test:

- `knot.3_1.fseries` — Fremlin Fourier-series source.
- `knot_3.1_final.txt` — KnotPlot/RidgeRunner final source.

Hashes are recorded in `INPUTS.sha256`.

Normal `run_basic.cmd` / `run_extended.cmd` still use the configured working-copy paths. These bundled files are used by the historical reproduction runners.
