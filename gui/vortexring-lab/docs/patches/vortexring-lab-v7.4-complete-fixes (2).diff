--- a/vortexring-lab-v7.4(1).html
+++ b/vortexring-lab-v7.4(1).html
@@ -3,10 +3,10 @@
 <head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
-<title>Superfluïde vortexlab v7.3.1 — ringen, trefoil, heliciteit, wederzijdse wrijving (T&gt;0)</title>
+<title>Superfluïde vortexlab v7.4 — ringen, trefoil, heliciteit, wederzijdse wrijving (T&gt;0)</title>
 <meta name="vortexlab-version" content="7.4">
-<meta name="vortexlab-base" content="7.3">
-<meta name="vortexlab-patch" content="diag-acn-hotfix,model-log-0.2,safe-a-sim,overview-dedupe,runtime-guard">
+<meta name="vortexlab-base" content="7.3.1">
+<meta name="vortexlab-patch" content="diag-acn-hotfix,model-log-0.2,safe-a-sim,overview-dedupe,runtime-guard,passive-first-hit,taylor-contact,frame-split,geo-score,planck-rosetta,regression-matrix">
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.9/katex.min.css">
 <style>
   :root{
@@ -325,6 +325,7 @@
       <div class="row hidden" id="rowGprod"><span class="lbl">teken Γ_A·Γ_B</span><span class="val" id="hGprod">—</span></div>
       <div class="row hidden" id="rowMF"><span class="lbl">wrijving α / α′ / v_n</span><span class="val" id="hMF">—</span></div>
       <div class="row hidden" id="rowRdot"><span class="lbl">Ṙ meting / α(v_n∥−U_K)</span><span class="val" id="hRdot">—</span></div>
+      <div class="row hidden" id="rowRevErr"><span class="lbl">ε_rev naar resettoestand</span><span class="val" id="hRevErr">—</span></div>
       <div class="row"><span class="lbl">t (fysisch)</span><span class="val" id="hT">0.0 s</span></div>
       <div class="row"><span class="lbl">effectieve versnelling</span><span class="val" id="hAcc">—</span></div>
     </div>
@@ -359,7 +360,7 @@
       <p>\[ \mathbf u(\mathbf x_i) \;=\; \underbrace{\frac{\Gamma}{4\pi}\Big[\ln\!\frac{2\sqrt{\ell_+\ell_-}}{e^{\Delta}\,a}+c_0\Big]\;\mathbf s'\!\times\mathbf s''}_{\text{lokaal}} \;+\; \underbrace{\frac{\Gamma}{4\pi}\sum_{j\notin\{i-1,\,i\}} \frac{d\boldsymbol\ell_j\times\mathbf r_{ij}}{\left(|\mathbf r_{ij}|^2+\delta\,a^2\right)^{3/2}}}_{\text{niet-lokaal}} \;+\; w\,\hat{\mathbf z}\;\text{(solo)}, \]</p>
       <p>De axiale superstroming \(w\,\hat{\mathbf z}\) geldt uitsluitend in <b>solo-modus</b> (één knoop). In botsing-modus gebruik je per-drager <em>v_z-drift</em> sliders i.p.v. \(w\).</p>
       <p>met \(\delta=0\) op het eigen filament (de kernfysica zit volledig in de lokale term) en \(\delta=1\) voor de kruisterm tussen dragers. De constante \(c_0=0.1395\) is een <em>gemeten</em> discretisatieconstante van dit schema, geverifieerd universeel over \(R\in[4,10]\,\mathrm{cm}\), \(a\in[0.5,3]\,\mathrm{mm}\), \(N\in[48,256]\) en alle drie kernmodellen. Integratie: RK4 met Kelvin-golf-CFL \(\omega_{\max}\,\Delta t\le 0.5\), \(\omega=\nu k^2\), \(\nu=\tfrac{\Gamma}{4\pi}\Lambda\). RK2 is hier principieel ongeschikt: Kelvin-golven zijn zuiver dispersief en RK2 heeft geen stabiliteit op de imaginaire as.</p>
-      <p><b>Validatie:</b> ringsnelheid vs. Kelvin-formule: \(|\text{fout}|\le 0.01\%\) voor alle kernmodellen bij \(N=48\!-\!256\) (resolutie-onafhankelijk). De rotatie wordt exact behandeld door in het meeroterende frame te integreren; \(\Omega\) legt dus géén tijdstaplimiet op.</p>
+      <p><b>Validatie:</b> ringsnelheid vs. Kelvin-formule, gemeten over alle drie kernmodellen: maximale relatieve fout circa \(0.075\%\) bij \(N=48\), \(0.015\%\) bij \(N=96\), en \(<0.01\%\) voor \(N\ge144\). De fout is dus klein maar aantoonbaar resolutie-afhankelijk. In framegesplitste modus wordt de uniforme rotatie analytisch door de weergavetransformatie behandeld; alleen wanneer \(\boldsymbol\Omega\times\mathbf r\) expliciet in de solver staat geldt de extra staplimiet \(\Delta t\le0.2/|\Omega|\).</p>
 
       <p><b>Deterministische stepper (v6):</b> de integrator zet uitsluitend volledige CFL-stappen \(\Delta t=\Delta t_{\rm CFL}(\text{toestand})\); de <em>simulatiesnelheid</em> bepaalt via een stap-debet-accumulator alleen hoeveel van die stappen per frame worden afgespeeld. Het traject is daardoor bit-voor-bit onafhankelijk van afspeelsnelheid, framerate en veiligheidsrem. In eerdere versies werd de laatste substap op het tijdbudget afgekapt, waardoor de snelheidsknop het resultaat wél kon beïnvloeden. Uitzondering: auto-relax regulariseert per weergaveframe en verbreekt deze onafhankelijkheid; dat staat bij de knop vermeld. <b>v7.1:</b> de laatste bekende framegekoppelde toestandsmutatie (kernstraal-klem vanuit de stabiliteitsdiagnose) is verwijderd — de diagnose is nu puur. <b>v7.2:</b> contactdetectie gebruikt exacte segment-segmentafstanden (geklemde closest-point, met boog-venster \(6a\) tegen valse zelf-treffers) ná iedere geaccepteerde RK4-stap, en bij harde 3a-overschrijding wordt de first-hit binnen de stap gebisecteerd in plaats van met een volle CFL-stap gepasseerd. Tracers integreren per geaccepteerde stap met exact dezelfde tijdstap als de filamenten. Wandmarges rekenen met de búisrand (\(r+a\), \(z\pm a\)), niet de centerline. Regressieharnas: open met <code>?selftest=1</code> of de 🧪-knop; resultaten als JSON exporteerbaar.</p>
       <h2>KERNMODELLEN</h2>
@@ -388,8 +389,10 @@
       <h2>MEDIA</h2>
       <div class="card"><div class="ct kB">HELIUM-II</div>
         <p>\(\kappa = h/m_4 = 9.969\times10^{-8}\ \mathrm{m^2\,s^{-1}}\); kernstraal \(a_0\sim 1\ \text{Å}\); \(\rho_s\approx 145\ \mathrm{kg\,m^{-3}}\) (\(T\to0\)). Historisch kernmodel: hol (Rayfield–Reif); modern: GP-achtig.</p></div>
-      <div class="card"><div class="ct kA">SST <span class="tag">CANON v0.8.19</span></div>
+      <div class="card"><div class="ct kA">SST <span class="tag">CANON v0.8.20</span></div>
         <p>\(\Gamma_0 = 2\pi r_c v_{\circlearrowleft} = 9.68362\times10^{-9}\ \mathrm{m^2\,s^{-1}}\); \(r_c = 1.40897\times10^{-15}\ \mathrm m\); \(v_{\circlearrowleft} = 1.09384563\times10^{6}\ \mathrm{m\,s^{-1}}\); \(\rho_f = 7.0\times10^{-7}\ \mathrm{kg\,m^{-3}}\); \(p_{\circlearrowleft,0}=\tfrac12\rho_f v_{\circlearrowleft}^2 = 4.188\times10^{5}\ \mathrm{Pa}\). Kernmodel per Canon: GP/NLSE.</p></div>
+      <div class="card"><div class="ct" style="color:#D8B4FE">STRING-THEORIE PROBE <span class="tag">SCHAALTEST</span></div>
+        <p>Deze optie zet uitsluitend de fysieke Rosetta-schaal op de Plancklengte \(\ell_P\). De dynamica blijft dezelfde filament-ODE met een vrije demo-circulatie; er wordt geen snaaractie, compactificatie, extra dimensie of canonieke string-circulatie verondersteld. Resultaten uit deze modus zijn dus numerieke schaal- en interfaceproeven, geen stringtheorievoorspellingen.</p></div>
       <p><b>Schaalmodel-eerlijkheid.</b> In de sim staat \(a\) op mm-schaal; de echte kern is Å (He-II) resp. fm (SST) — dat gat is niet resolveerbaar in een filamentcode op meterschaal. \(U\) schaalt \(\propto\Gamma\) en slechts logaritmisch met \(a\); de topologische diagnostiek (Wr, Lk, \(H/\Gamma^2\)) is exact schaal-invariant. Kies je een medium, dan is \(\Gamma=n\kappa\) fysisch en staat de tijdversnelling navenant hoog.</p>
 
       <h2>TAYLOR-KOLOM &amp; STEWARTSON (Research Track)</h2>
@@ -399,7 +402,7 @@
 
       <h2>FILAMENT-SPIN &amp; ACHTERGROND-Ω</h2>
       <p><b>Body-frame:</b> \(\Omega_{\rm body}\) uit least-squares fit van punt-snelheden in het xy-vlak; \(\hat\chi\) = chiraliteitsrichting (trefoil/knoop). Ring: \(\mathrm{Wr}=0\), geen macroscopische spin.</p>
-      <p><b>Achtergrond Omega koppelen</b> (optioneel, default uit): lab-frame superflow \(\mathbf u_{\rm bg}=\boldsymbol\Omega\times\mathbf r\) met \(\Omega=P.Om\,\hat z\) — Feynman-rooster advectie. Uit wanneer co-rot frame aan.</p>
+      <p><b>Achtergrond Omega koppelen</b> (optioneel, default uit): expliciete solverterm \(\mathbf u_{\rm bg}=\boldsymbol\Omega\times\mathbf r\) met \(\Omega=P.Om\,\hat z\) — Feynman-rooster-advectie. Deze solverkeuze staat los van Absolute/Rotating als weergaveframe; de rendering transformeert de baan overeenkomstig.</p>
       <p><b>Transport-proxy</b> (v7.1, voorheen "Tw_proxy"): geïntegreerde tangentiële verplaatsing \(\int \mathbf u\cdot\hat{\mathbf t}\,dt\), dimensie <em>lengte</em>. Dit is géén twist: echte \(\mathrm{Tw}\) is dimensieloos en vereist een ribbon-framing of parallel-getransporteerd materiaalframe, dat een kale centerline niet bezit. De proxy mag daarom niet bij \(\mathrm{Wr}\) worden opgeteld; het twist-kanaal blijft in dit lab expliciet niet gerepresenteerd. <b>v7.2:</b> de uitlezing is nu het booglengte-gewogen gemiddelde \(\langle\int\mathbf u\cdot\hat{\mathbf t}\,dt\rangle_L\) — de oude kale knooppuntsom schaalde \(\propto N\) (zelfde knoop, dubbele resolutie, dubbele waarde) en deelde bovendien door \(2\pi\), een restant van de twist-claim.</p>
 
       <h2>STABILITEIT &amp; AUTO-RELAX</h2>
@@ -417,12 +420,12 @@
       <h2>WEDERZIJDSE WRIJVING (T &gt; 0) <span class="tag">v7</span></h2>
       <p>De knooppuntsnelheid volgt de Schwarz-vergelijking</p>
       <p>\[ \dot{\mathbf s} \;=\; \mathbf v_s \;+\; \alpha\,\hat{\mathbf s}'\times(\mathbf v_n-\mathbf v_s)\;-\;\alpha'\,\hat{\mathbf s}'\times\big[\hat{\mathbf s}'\times(\mathbf v_n-\mathbf v_s)\big], \]</p>
-      <p>met \(\mathbf v_s\) de <em>totale</em> lokale superfluïde snelheid (LIA + niet-lokale Biot–Savart + opgelegde drift; in het lab-frame met achtergrond-Ω-koppeling ook \(\boldsymbol\Omega\times\mathbf r\)) en \(\mathbf v_n\) de opgelegde normale-vloeistofsnelheid: uniform axiaal \(v_n\hat{\mathbf z}\), plus \(\boldsymbol\Omega\times\mathbf r\) wanneer de achtergrondrotatie in het lab-frame staat (de normale component co-roteert in evenwicht, zodat de azimutale \(\mathbf v_{ns}\)-bijdrage wegvalt). De wrijvingsterm zit ín de RHS van de ODE en dus binnen alle vier RK4-evaluaties; de deterministische stepper blijft bit-voor-bit afspeelsnelheid-onafhankelijk.</p>
+      <p>met \(\mathbf v_s\) de <em>totale</em> lokale superfluïde snelheid (LIA + niet-lokale Biot–Savart + opgelegde drift; bij expliciete achtergrondkoppeling ook \(\boldsymbol\Omega\times\mathbf r\)) en \(\mathbf v_n\) de opgelegde normale-vloeistofsnelheid: uniform axiaal \(v_n\hat{\mathbf z}\), plus \(\boldsymbol\Omega\times\mathbf r\) wanneer die solverterm actief is (de normale component co-roteert in evenwicht, zodat de azimutale \(\mathbf v_{ns}\)-bijdrage wegvalt). De wrijvingsterm zit ín de RHS van de ODE en dus binnen alle vier RK4-evaluaties; de deterministische stepper blijft bit-voor-bit afspeelsnelheid-onafhankelijk.</p>
       <p><b>Orthodoxe consequenties</b> (LIA-exact voor een cirkelring, live geverifieerd in de HUD): in rustende normale vloeistof krimpt een ring met \(\dot R=-\alpha U\) en transleert met \((1-\alpha')U\); met tegenstroom groeit hij zodra \(v_{n\parallel}>U\) (Donnelly–Glaberson). Kelvin-golven worden gedempt met rate \(\sim\alpha\,\omega_{\rm KW}\). Bij \(\alpha\neq0\) vervalt het Kelvin-circulatietheorema op de bewegende kernlijn: energie, \(H/\Gamma^2\) en \(\mathrm{Wr}+\mathrm{Tw}\) zijn <em>niet langer</em> behouden — drift daarin is nu fysica, geen modelgrens.</p>
-      <p><b>Coëfficiënten:</b> \(\alpha(T),\alpha'(T)\) voor He-II bij SVP (T90) uit de Donnelly-compilatie van Barenghi–Donnelly–Vinen, <i>J. Low Temp. Phys.</i> <b>52</b>, 189 (1983), tabelbereik 1.30–2.10 K; \(\alpha'\) wisselt van teken tussen 2.06 en 2.08 K. Onder 1.30 K en boven 2.10 K geen tabelwaarden — gebruik "aangepast". <b>Eerlijkheidsgrens:</b> in SST- of demo-medium zijn \(\alpha,\alpha'\) zuiver fenomenologische knoppen; er bestaat geen SST-canonwaarde voor wederzijdse wrijving en de He-II-temperatuurlabels claimen daar niets. De koppeling is één-richting: \(\mathbf v_n\) is voorgeschreven, er is geen terugreactie op de normale component en dus geen zelfconsistente twee-vloeistof-(HVBK-)dynamica. Tijd terugdraaien met \(\alpha\neq0\) integreert een anti-dissipatieve ODE en is geen fysische omkering; de sim waarschuwt hiervoor.</p>
+      <p><b>Coëfficiënten:</b> \(\alpha(T),\alpha'(T)\) voor He-II bij SVP (T90) uit de Donnelly-compilatie van Barenghi–Donnelly–Vinen, <i>J. Low Temp. Phys.</i> <b>52</b>, 189 (1983), tabelbereik 1.30–2.10 K; \(\alpha'\) wisselt van teken tussen 2.06 en 2.08 K. Onder 1.30 K en boven 2.10 K geen tabelwaarden — gebruik "aangepast". <b>Eerlijkheidsgrens:</b> in SST-, demo- en string-probe-medium zijn \(\alpha,\alpha'\) zuiver fenomenologische knoppen; er bestaat geen SST- of string-canonwaarde voor wederzijdse wrijving en de He-II-temperatuurlabels claimen daar niets. De koppeling is één-richting: \(\mathbf v_n\) is voorgeschreven, er is geen terugreactie op de normale component en dus geen zelfconsistente twee-vloeistof-(HVBK-)dynamica. Tijd terugdraaien met \(\alpha\neq0\) integreert een anti-dissipatieve ODE en is geen fysische omkering; de sim waarschuwt hiervoor.</p>
 
       <h2>NIET GEMODELLEERD</h2>
-      <p>Reconnecties (run stopt bij onderlinge afstand \(<3a\); in LIA-modus alleen een waarschuwing, want LIA ziet de nadering niet) · wandbeelden · kern-twist · normale-vloeistof-terugreactie (\(\mathbf v_n\) is opgelegd; geen zelfconsistente twee-vloeistofdynamica, geen normale-vloeistofturbulentie) · kernvolume bij rek · echte roosterdynamica (Tkachenko-modi).</p>
+      <p>Reconnecties (run stopt bij onderlinge afstand \(<3a\); in LIA-modus alleen een waarschuwing, want LIA ziet de nadering niet) · lokale GP-reconnectiebrug · wandbeelden/BEM en vortex-pinning · fysische Taylor/Stewartson-continuumdynamica · kern-twist · normale-vloeistof-terugreactie (\(\mathbf v_n\) is opgelegd; geen zelfconsistente twee-vloeistofdynamica, geen normale-vloeistofturbulentie) · kernvolume bij rek · echte roosterdynamica (Tkachenko-modi) · snaardynamica of andere stringtheorie-veldvergelijkingen.</p>
       <p class="note" style="max-width:820px;">Ingebouwd: ring 0₁, Hopf-link 2₂, ideal trefoil 3₁, figure-eight 4₁, cinquefoil 5₁ en three-twist 5₂. De trefoil gebruikt Brian Gilbert ideal <code>3:1:1</code> (183 Fourier-modi). 4₁ en 5₁ gebruiken gladde analytische parametrisaties; 5₂ gebruikt een Lissajous-representatie. Optioneel: leg <code>ideal_knots_data.js</code> naast dit bestand voor de volledige knopencatalogus.</p>
     </div>
 </div>
@@ -504,6 +507,7 @@
             <button type="button" class="seg-btn" data-med="demo">Demo</button>
             <button type="button" class="seg-btn" data-med="he">He-II</button>
             <button type="button" class="seg-btn active" data-med="sst">SST</button>
+            <button type="button" class="seg-btn" data-med="string">String-probe</button>
           </div></div>
         <div class="ctrl"><label>kernmodel <span class="v" id="vCore">Δ = ½</span></label>
           <div class="seg" id="coreSeg">
@@ -522,7 +526,14 @@
           <input type="number" id="sA" min="1e-15" max="70" step="any" value="1.500"></div>
         <div class="ctrl" id="coreRadiusPhysRow"><label>fysieke kernstraal a_phys <span class="v" id="vAPhys">1.409·10⁻¹⁵ m</span></label>
           <input type="text" id="sAPhys" inputmode="decimal" value="1.40897e-15" autocomplete="off" aria-label="Fysieke kernstraal in meters">
-          <div class="note" style="margin-top:3px;">Rosetta-readout (m); stuurt de filamentintegrator niet.</div></div>
+          <input type="range" id="sAPhysLog" min="-36" max="-9" step="0.01" value="-14.851" aria-label="Logaritme van de fysieke kernstraal in meters">
+          <select id="aPhysPreset" aria-label="Preset voor fysieke kernstraal">
+            <option value="custom">Vrije invoer</option>
+            <option value="planck">Plancklengte ℓ_P</option>
+            <option value="sst" selected>SST r_c</option>
+            <option value="helium">He-II · 1 Å</option>
+          </select>
+          <div class="note" style="margin-top:3px;">Rosetta-readout; tekst accepteert o.a. <code>planck</code>, <code>1.4 fm</code> en SI-notatie. De logslider bestrijkt \(10^{-36}\)–\(10^{-9}\) m. a_phys stuurt de filamentintegrator nooit.</div></div>
         <div class="core-flow-link" id="coreFlowLinkPanel">
           <label class="chk"><input type="checkbox" id="cCoreFlowLock"> Koppel kernstraal, circulatie en Ω <span style="opacity:.65">(Rankine display-similarity ansatz)</span></label>
           <div class="core-flow-readout" id="coreFlowReadout">Vrij: a, Γ en Ω zijn onafhankelijk.</div>
@@ -573,7 +584,7 @@
             <input type="number" id="sVn" min="0" max="2000" step="0.0001" value="0" data-scale="log" data-log-min="0.0001">
           </div></div>
       </div>
-      <div class="note" style="margin-top:4px;">Schwarz-wrijving op de kernlijn: \(\dot{\mathbf s}=\mathbf v_s+\alpha\,\hat{\mathbf s}'\!\times(\mathbf v_n-\mathbf v_s)-\alpha'\,\hat{\mathbf s}'\!\times[\hat{\mathbf s}'\!\times(\mathbf v_n-\mathbf v_s)]\). Tabelwaarden: He-II bij SVP (T90), Donnelly-compilatie van Barenghi–Donnelly–Vinen (1983); onder 1.30 K geen tabelwaarden. In SST- of demo-medium zijn α en α′ zuiver fenomenologisch — er bestaat géén SST-canonwaarde voor wederzijdse wrijving. \(v_n\) is opgelegd en uniform (één-richtingskoppeling, geen normale-vloeistof-terugreactie). Bij \(\alpha\neq0\) vervallen energie- en heliciteitsbehoud: verwacht ringkrimp \(\dot R=\alpha(v_{n\parallel}-U)\), Kelvin-golfdemping, en ringgroei zodra \(v_{n\parallel}>U\) (Donnelly–Glaberson). Tijd terugdraaien is met \(\alpha\neq0\) anti-dissipatief, geen fysische omkering.</div>
+      <div class="note" style="margin-top:4px;">Schwarz-wrijving op de kernlijn: \(\dot{\mathbf s}=\mathbf v_s+\alpha\,\hat{\mathbf s}'\!\times(\mathbf v_n-\mathbf v_s)-\alpha'\,\hat{\mathbf s}'\!\times[\hat{\mathbf s}'\!\times(\mathbf v_n-\mathbf v_s)]\). Tabelwaarden: He-II bij SVP (T90), Donnelly-compilatie van Barenghi–Donnelly–Vinen (1983); onder 1.30 K geen tabelwaarden. In SST-, demo- en string-probe-medium zijn α en α′ zuiver fenomenologisch — er bestaat géén SST- of string-canonwaarde voor wederzijdse wrijving. \(v_n\) is opgelegd en uniform (één-richtingskoppeling, geen normale-vloeistof-terugreactie). Bij \(\alpha\neq0\) vervallen energie- en heliciteitsbehoud: verwacht ringkrimp \(\dot R=\alpha(v_{n\parallel}-U)\), Kelvin-golfdemping, en ringgroei zodra \(v_{n\parallel}>U\) (Donnelly–Glaberson). Tijd terugdraaien is met \(\alpha\neq0\) anti-dissipatief, geen fysische omkering.</div>
       </div>
     </details>
 
@@ -647,20 +658,20 @@
         <label class="chk hidden" id="mirrorRow"><input type="checkbox" id="cMirror"> Mirror B</label>
         <label class="chk hidden" id="lockVzRow"><input type="checkbox" id="cLockVz" checked> v_z A = B</label>
         <label class="chk" style="margin-top:6px;"><input type="checkbox" id="cBgOmega"> Achtergrond Ω koppelen</label>
-        <div class="note" style="margin-top:4px;">Integreer in lab-frame \(\mathbf u_{\rm bg}=\boldsymbol\Omega\times\mathbf r\) i.p.v. frame-splitsing — fysisch identiek voor deze achtergrond, maar stijver (dt ≤ 0.2/Ω); didactisch ter vergelijking.</div>
+        <div class="note" style="margin-top:4px;">Solverkeuze, onafhankelijk van het weergaveframe: integreer \(\mathbf u_{\rm bg}=\boldsymbol\Omega\times\mathbf r\) expliciet. De getransformeerde baan is fysisch equivalent voor deze achtergrond, maar stijver (dt ≤ 0.2/Ω).</div>
       </div>
       </div>
     </details>
 
     <details class="coll" id="collEnergy">
-      <summary>ENERGIE E_eff</summary>
+      <summary>GEOMETRISCHE SCORE Ŝ</summary>
       <div class="coll-body">
-        <p class="energy-help">Visuele termen van \(\mathcal{E}_{\rm eff}[K]\). β = geel wireframe 1.03×; ∂V = domein-hoge Taylor-kolom met eindcaps en Stewartson-zijlaag.</p>
+        <p class="energy-help">Dimensieloze scoretermen en afzonderlijke visuele lagen. β = geel wireframe 1.03×; ∂V = uitsluitend de domein-hoge Taylor/Stewartson-visualisatie en telt niet mee in Ŝ.</p>
         <div class="seg ind-seg" id="indSeg">
-          <button type="button" class="seg-btn" data-ind="alpha" title="α C(K) — Biot-Savart afstoting bij nadering">α C</button>
-          <button type="button" class="seg-btn" data-ind="beta" title="β L(K) — lijnspanning overlay">β L</button>
-          <button type="button" class="seg-btn" data-ind="gamma" title="γ H(K) — Hopf / flow overlay">γ H</button>
-          <button type="button" class="seg-btn" data-ind="sep" title="∂V — volledige Taylor-kolom + eindcaps + Stewartson-zijlaag">∂V</button>
+          <button type="button" class="seg-btn" data-ind="alpha" title="α C(K) — gewogen ACN-bijdrage aan de geometrische score">α C</button>
+          <button type="button" class="seg-btn" data-ind="beta" title="β L̂(K) — gewogen genormaliseerde lengtebijdrage">β L̂</button>
+          <button type="button" class="seg-btn" data-ind="gamma" title="γ Ĥ(K) — gewogen getekende heliciteitsbijdrage">γ Ĥ</button>
+          <button type="button" class="seg-btn" data-ind="sep" title="∂V — uitsluitend visualisatie; geen term in Ŝ">∂V</button>
         </div>
         <div class="wgrid">
           <div class="ctrl"><label>α <span class="v" id="vWAl">1.0</span></label>
@@ -760,7 +771,7 @@
             <div class="stab-summary" id="stabilitySummary">Mesh, kern/slankheid, niet-lokale afstand, domeinmarge en rekensnelheid.</div>
             <div class="stab-metrics">
               <span>mesh ℓmax/ℓmin</span><span id="stabMesh">—</span>
-              <span>vrije afstand / a</span><span id="stabGap">—</span>
+              <span>g_a = d_min/a_sim</span><span id="stabGap">—</span>
               <span>max aκ</span><span id="stabCurv">—</span>
               <span>domeinmarge</span><span id="stabBoundary">—</span>
               <span>lengtedrift</span><span id="stabLength">—</span>
@@ -792,6 +803,7 @@
             <option value="taylor">Taylor-kolom</option>
             <option value="pistol">Pistool (solo)</option>
             <option value="sst">⚛ SST proportioneel</option>
+            <option value="string">🧵 String-theorie schaalprobe · ℓ_P</option>
             <option value="friction">🌡 T &gt; 0 · ringverval (He-II 1.90 K)</option>
           </select>
         </div>
@@ -811,7 +823,7 @@
       </div>
     </details>
     <div class="note" id="footNote">
-      v7.3.1 (hotfix op v7.3) · één OVERZICHT-dock · veilige a_sim-invoer · ModelLog 0.2 · runtime-guard · ACN-diagfix · a_sim/a_phys · ModelLog · vorticiteit #0F1A29 · exacte segmentafstand + first-hit-bisectie · exacte polygonale Wr/Lk/ACN (Klenin–Langowski; trefoil-Wr gemeten −3.418, Fourier-verklaring bevestigd) · tracers per CFL-stap · ghost numeriek onzichtbaar · Stewartson gereduceerd tot dimensieloze q_S · booglengte-gewogen transport-proxy · buis-gecorrigeerde wandmarge · zelftest ?selftest=1 · v7.1 · auditpatch: modusgescheiden drift (w solo / v_z botsing) · tracers &amp; contactdetectie op werkelijk geïntegreerde tijd (per RK4-stap) · Stewartson-ghost &amp; Tw_proxy uit de dynamica (dimensioneel ongeldig) · passieve stabiliteitsdiagnose · CFL over alle RK4-stages · v7: wederzijdse wrijving (Schwarz α, α′; He-II-tabel 1.30–2.10 K + v_n axiaal) · stap 20 frame-indicator: geen twist meer; in Rotating frame verschijnt een fictieve inertiaalcilinder op r &gt; R_cyl met uitsluitend verticale referentielijnen die tegengesteld aan Ω bewegen · SST trefoil default · Schwarz + RK4 · volume-kader geen wand · LIA in botsing = géén wederzijdse inductie · camera: links/midden slepen = roteren, rechts slepen = transleren, wiel = zoomen.
+      v7.4 · passieve first-hit-diagnostiek · Taylor-contact binnen de geaccepteerde stap · gescheiden solver-/weergaveframe · geometrische score Ŝ · Planck/SST/He-II Rosetta-presets · String-theorie schaalprobe zonder nieuwe fysicaclaim · Canon v0.8.20 · uitgebreide regressietests · één OVERZICHT-dock · veilige a_sim-invoer · ModelLog 0.2 · runtime-guard · exacte segmentafstand + first-hit-bisectie · exacte polygonale Wr/Lk/ACN · tracers per CFL-stap · ghost numeriek onzichtbaar · Stewartson gereduceerd tot dimensieloze q_S · booglengte-gewogen transport-proxy · buis-gecorrigeerde wandmarge · Schwarz + RK4 · volume-kader geen wand · LIA in botsing = géén wederzijdse inductie.
     </div>
 </div>
 
@@ -828,7 +840,12 @@
   'ModelLog 0.2 with global UI capture and drop counters',
   'Safe aSim input and quantized-lock auto-unlock',
   'Single OVERZICHT dock',
-  'Runtime error guard'
+  'Runtime error guard',
+  'Passive first-hit bisection with accepted-step transport diagnostics',
+  'Taylor contact localization and frame/background decoupling',
+  'Geometric-score and release-provenance cleanup',
+  'Planck-scale Rosetta controls and String-theory scale probe',
+  'Expanded regression matrix'
 ];
 const A_SIM_EPS=1e-30;
 const A_SIM_INPUT_FLOOR=1e-18;
@@ -1054,6 +1071,8 @@
 const GAMMA0_SST= 9.683619e-9;    // m^2/s  (2*pi*r_c*v_swirl, Canon v0.8.20; waarde ongewijzigd)
 const RCORE_SST = 1.40897017e-15; // m, canonical SST core radius
 const VSWIRL_SST= 1.09384563e6;   // m/s, canonical SST swirl speed
+const PLANCK_LENGTH=1.616255e-35; // m, CODATA-style Rosetta scale; metadata only
+const RCORE_HE_REF=1.0e-10;       // m, orde-grootte 1 Å; metadata only
 const OMEGA_CORE_SST = GAMMA0_SST/(2*Math.PI*RCORE_SST*RCORE_SST);
 const C0        = 0.1395;         // gemeten discretisatieconstante Schwarz-schema
 const DELTA     = {hol:0.5, vast:0.25, gp:0.615};
@@ -1089,20 +1108,21 @@
 }
 const Flags = {alpha:false, beta:false, gamma:false, sep:false};
 const EXPLAIN = {
-  alpha:{title:'α C(K) — Biot-Savart Afstoting', cls:'on-alpha', color:'#FF6E6E',
-    text:'Massieve wervelkernen naderen elkaar; incompressibiliteit creëert een drukbarrière die reconnectie voorkomt.'},
-  beta:{title:'β L(K) — Lijnspanning', cls:'on-beta', color:'#FFAE45',
-    text:'Samentrekkende kracht van de wervelstreng — geel wireframe overlay op 1.03× straal.'},
-  gamma:{title:'γ H(K) — Hopf Stabiliteit', cls:'on-gamma', color:'#A855F7',
-    text:'Topologische verstrengeling (kruis-heliciteit) houdt het macroscopische geheel bijeen.'},
-  sep:{title:'∂V — Separatrix / Taylor Caps', cls:'on-sep', color:'#fff',
-    text:'Taylor-kolom over de volledige z-as; de lokale separatrix blijft radiaal begrensd door r_cap.'}
+  alpha:{title:'α C(K) — geometrische verwevenheid', cls:'on-alpha', color:'#FF6E6E',
+    text:'Gewogen ACN-bijdrage aan de dimensieloze score. Dit is geen drukbarrière, kracht of reconnectiemodel.'},
+  beta:{title:'β L̂(K) — genormaliseerde lengte', cls:'on-beta', color:'#FFAE45',
+    text:'Gewogen L/L₀-bijdrage; het gele wireframe is uitsluitend een geometrische overlay op 1.03× straal.'},
+  gamma:{title:'γ Ĥ(K) — getekende heliciteit', cls:'on-gamma', color:'#A855F7',
+    text:'Gewogen Wr+2Lk-bijdrage. Het teken selecteert chiraliteit en is geen stabiliteitsenergie.'},
+  sep:{title:'∂V — Taylor/Stewartson-visualisatie', cls:'on-sep', color:'#fff',
+    text:'Visuele domeinlaag over de z-as; ∂V telt niet mee in de numerieke score Ŝ.'}
 };
 function clamp(x,lo,hi){return Math.max(lo,Math.min(hi,x));}
 function aSimActive(){return Math.max(P.a,A_SIM_EPS);}
 function fmtLengthSI(m){
   if(!Number.isFinite(m))return '—';
   const a=Math.abs(m);if(a===0)return '0 m';
+  if(a<1e-18)return m.toExponential(6)+' m';
   if(a<1e-15)return (m*1e18).toFixed(3)+' am';
   if(a<1e-12)return (m*1e15).toFixed(3)+' fm';
   if(a<1e-9)return (m*1e12).toFixed(3)+' pm';
@@ -1114,6 +1134,7 @@
 function parseLengthInput(str){
   const t=String(str||'').trim().toLowerCase().replace(',','.').replace(/μ/g,'µ');
   if(!t)return NaN;
+  if(/^(planck|plancklengte|l[_ ]?p|ℓ[_ ]?p)$/.test(t))return PLANCK_LENGTH;
   const m=t.match(/^([+-]?(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?)\s*(am|fm|pm|nm|um|µm|mm|cm|m)?$/i);
   if(!m)return NaN;
   const value=Number(m[1]);if(!Number.isFinite(value))return NaN;
@@ -1130,6 +1151,12 @@
   const numerical=CONTACT_ULP_FACTOR*Number.EPSILON*scale;
   return {physical,numerical,effective:Math.max(physical,numerical),floorActive:numerical>physical};
 }
+function gapRatios(minGap){
+  const ct=contactThresholdInfo();
+  const aPhysical=Math.max(P.a,A_SIM_EPS);
+  const aEffective=Math.max(aPhysical,ct.effective/3);
+  return {physical:minGap/aPhysical,effective:minGap/aEffective,floorActive:ct.floorActive};
+}
 function buildDiagRecord(Wr,Lk,ACN,sA){
   const ct=contactThresholdInfo();
   return {t:tPhys,Wr,Lk,ACN,RA:sA.R,zA:sA.z,a:P.a,aSim:P.a,aPhys:P.aPhys,
@@ -1215,6 +1242,7 @@
 let lastFrameVel=null;
 let stabilityLast=null, stabilityFrame=0, autoRelaxFrame=0;
 let stabilityThrottle=1, stabilityThrottleTarget=1;
+let reverseReference=null,reverseReferenceTime=0;
 let carrierAnchors=Object.create(null);
 let coreCouplingBusy=false;
 let coreFlowNotice='';
@@ -1354,8 +1382,24 @@
   applySSTSimilarityPreset();
   P.nQ=10;
   syncCoreFlowCoupling('gamma');
+  // Conservatieve wetenschappelijke default: behoud de afgeleide zichtbare
+  // startstraal, maar laat a_sim, Γ en Ω daarna onafhankelijk. De expliciete
+  // SST-proportioneel-preset schakelt de Rankine-displayansatz wel in.
+  P.coreFlowLock=false;
   syncUi();updateSubtitle();
 }
+function applyStringTheoryPreset(){
+  // Schaalprobe, geen nieuw dynamisch stringmodel. De solver blijft de gewone
+  // filament-ODE met vrije demo-circulatie; alleen a_phys krijgt ℓ_P.
+  P.mode='solo';P.topo='trefoil';P.inter='lia';P.qual='hoog';
+  P.knotIdx=-1;P.knotKey='';P.compA=1;P.compB=1;
+  P.med='string';P.core='gp';P.coreFlowLock=false;
+  P.aPhys=PLANCK_LENGTH;P.a=1.2415e-4;P.GaDemo=0.2;
+  P.Om=1;P.revOm=false;P.coRot=true;P.bgOmegaCoupling=false;
+  P.R0=0.07;P.zSolo=0;P.off=0;P.w=0;P.vzA=0;P.vzB=0;
+  P.timeReverse=false;P.autoRelax=false;P.centerLock=true;
+  P.tracerWrapZ=true;P.mfTemp='0';P.mfAlpha=0;P.mfAlphaP=0;P.vnZ=0;
+}
 function applyFrictionPreset(){
   // Demonstratie wederzijdse wrijving: solo He-II-ring bij 1.90 K in rustende
   // normale vloeistof. Verwacht: Ṙ = −αU (krimp), translatie ×(1−α′), en de
@@ -1652,7 +1696,9 @@
   // wrijving alleen in de echte dynamica: niet in de includeExternal:false
   // diagnostiekaanroepen (zelfgeïnduceerde vz voor richtbepaling).
   const mfOn=includeExternal&&options.mutualFriction!==false&&mfActive();
-  const mfRot=P.bgOmegaCoupling&&!P.coRot;
+  // Solverachtergrond en weergaveframe zijn onafhankelijk. Wanneer de
+  // Ω×r-term expliciet in de solver zit, co-roteert ook de normale component.
+  const mfRot=P.bgOmegaCoupling;
   const a=P.a, a2=a*a, eD=Math.exp(DELTA[P.core]);
   // segmentdata per filament
   const mids=[],dls=[];
@@ -1690,7 +1736,7 @@
         const carrier=fl[ft].carrier||'A';
         const zBias=effectiveW()+carrierAxialDrift(carrier);
         uz+=zBias;
-        if(P.bgOmegaCoupling&&!P.coRot&&!fl[ft].ghost){
+        if(P.bgOmegaCoupling&&!fl[ft].ghost){
           ux+=-P.Om*py;
           uy+= P.Om*px;
         }
@@ -1779,7 +1825,8 @@
   wrapFilamentCarriersZ();
   enforceCenterLock();
   constrainGhostRing();
-  if(P.twistProxyEnabled) updateTwistProxy(dt,K4);
+  // Diagnostische accumulatoren worden pas na acceptatie van de (eventueel
+  // gebisecteerde) stap bijgewerkt. Daardoor blijven trial-stappen passief.
   // v7.1 (B7): verplaatsingslimiet in dtCFL gebruikt de snelste van alle vier
   // RK4-stages; voorheen alleen K1, waardoor snelle tussenstadia bij nadering
   // de tijdstap konden onderschatten.
@@ -2192,6 +2239,9 @@
 function sampledSelfGap(f){
   return dminSelf(f); // v7.2: exact segmentniveau
 }
+function tubeBoundaryMarginAt(x,y,z){
+  return Math.min(P.Rcyl-Math.hypot(x,y)-P.a,z-zMin()-P.a,zMax()-z-P.a);
+}
 function updateChiPanel(){
   const el=document.getElementById('chiRope');
   if(!el||!Y||!fils.length)return;
@@ -2228,17 +2278,19 @@
     for(let k=0;k<f.N;k++){
       const x=Y[f.off+3*k],y=Y[f.off+3*k+1],z=Y[f.off+3*k+2];
       // v7.2: marge t.o.v. de búisrand, niet de centerline (audit #9)
-      boundary=Math.min(boundary,P.Rcyl-Math.hypot(x,y)-P.a,z-zMin()-P.a,zMax()-z-P.a);
+      boundary=Math.min(boundary,tubeBoundaryMarginAt(x,y,z));
     }
   }
   for(let i=0;i<realFils.length;i++)for(let j=i+1;j<realFils.length;j++)
     minGap=Math.min(minGap,sampledPairGap(realFils[i],realFils[j]));
   lmean/=Math.max(1,nm);
-  const gapRatio=minGap/Math.max(P.a,1e-12);
+  // Rapporteer g_a exact met a_sim. Voor de veiligheidsscore wordt daarnaast
+  // de expliciete numerieke contactvloer als effectieve kernschaal gebruikt.
+  const gaps=gapRatios(minGap),gapRatio=gaps.physical,gapRatioEffective=gaps.effective;
   const boundaryRatio=boundary/Math.max(P.a,lmean,1e-12);
   const lengthDrift=Math.abs(Lnow/Math.max(L0,1e-12)-1);
   const meshScore=scoreDescending(q,1.22,2.10);
-  const gapScore=scoreAscending(gapRatio,3.0,8.0);
+  const gapScore=scoreAscending(gapRatioEffective,3.0,8.0);
   const curvatureScore=scoreDescending(maxAk,0.10,0.45);
   const coreScore=scoreAscending(minLogArg,1.15,4.0);
   const boundaryScore=scoreAscending(boundaryRatio,1.5,7.0);
@@ -2247,11 +2299,11 @@
   const perfRatio=Math.abs(tPhys)<0.15||requestedAccNow<1e-8?1:clamp(effAcc/requestedAccNow,0,1);
   const perfScore=scoreAscending(perfRatio,0.20,0.80);
   let modelScore=100;
-  if(P.inter==='lia'&&gapRatio<10)modelScore=gapRatio<5?20:55;
+  if(P.inter==='lia'&&gapRatioEffective<10)modelScore=gapRatioEffective<5?20:55;
   let score=0.20*meshScore+0.23*gapScore+0.17*curvatureScore+0.10*coreScore+
     0.13*boundaryScore+0.09*lengthScore+0.08*perfScore;
   score=Math.min(score,modelScore);
-  if(gapRatio<3||boundary<0||minLogArg<=1)score=Math.min(score,18);
+  if(gapRatioEffective<3||boundary<0||minLogArg<=1)score=Math.min(score,18);
   const suggestions=[];
   if(meshScore<75)suggestions.push('verhoog kwaliteit of zet Auto-relax aan om de puntverdeling gelijkmatiger te maken');
   if(gapScore<75)suggestions.push('vergroot de vrije afstand: verlaag kernstraal a, verminder drift/botsingssnelheid, vergroot offset of reset');
@@ -2262,7 +2314,8 @@
   if(perfScore<60)suggestions.push('doelsnelheid wordt niet gehaald: verlaag de simulatiesnelheid (traject blijft identiek; alleen het afspeeltempo zakt), Γ/n, kwaliteit of aantal particles');
   if(modelScore<75)suggestions.push('LIA mist belangrijke niet-lokale interactie: kies Biot–Savart');
   if(!suggestions.length)suggestions.push('instellingen liggen binnen de numerieke comfortzone; dit is geen bewijs van fysische stabiliteit');
-  return {score:clamp(score,0,100),status:statusFromScore(score),q,gapRatio,maxAk,minLogArg,
+  return {score:clamp(score,0,100),status:statusFromScore(score),q,gapRatio,gapRatioEffective,
+    contactFloorActive:gaps.floorActive,maxAk,minLogArg,
     boundary,boundaryRatio,lengthDrift,perfRatio,meshScore,gapScore,curvatureScore,coreScore,
     boundaryScore,lengthScore,perfScore,modelScore,suggestions};
 }
@@ -2279,7 +2332,9 @@
     ?'Tijd loopt terug; Auto-relax is gepauzeerd en de veiligheidsrem blijft actief.'
     :(P.autoRelax?'Auto-relax actief; geometrische regularisatie corrigeert langzaam.':'Passieve diagnose; rode/oranje instellingen tonen de eerste herstelroute.');
   document.getElementById('stabMesh').textContent=rep.q.toFixed(2);
-  document.getElementById('stabGap').textContent=isFinite(rep.gapRatio)?rep.gapRatio.toFixed(1):'∞';
+  const fmtRatio=x=>!isFinite(x)?'∞':(Math.abs(x)>=1e5?x.toExponential(2):x.toFixed(1));
+  document.getElementById('stabGap').textContent=fmtRatio(rep.gapRatio)+
+    (rep.contactFloorActive?' · eff '+fmtRatio(rep.gapRatioEffective):'');
   document.getElementById('stabCurv').textContent=rep.maxAk.toFixed(3);
   document.getElementById('stabBoundary').textContent=(rep.boundary*1000).toFixed(1)+' mm';
   document.getElementById('stabLength').textContent=(100*rep.lengthDrift).toFixed(1)+'%';
@@ -2460,6 +2515,7 @@
   K1=new Float64Array(Y.length);K2=new Float64Array(Y.length);
   K3=new Float64Array(Y.length);K4=new Float64Array(Y.length);TT=new Float64Array(Y.length);
   tPhys=0;phi=0;flagged="";warned=false;lastUmax=1e-9;effAcc=0;
+  reverseReference=Y.slice();reverseReferenceTime=tPhys;
   effAccSimSum=0;effAccRealSum=0;stepDebt=0;hist.length=0;
   stabilityLast=null;stabilityFrame=0;autoRelaxFrame=0;
   stabilityThrottle=1;stabilityThrottleTarget=1;
@@ -2474,15 +2530,36 @@
   updateCoreRadiusLimit(true);
   rebuildStreamlines(true);
 }
-function applyTaylorOscillation(){
-  if(P.centerLock||!P.taylorOsc.enabled||!Y||!fils.length)return;
+function taylorOscillationApplies(){
+  return P.mode==='solo'&&!P.centerLock&&P.taylorOsc.enabled&&!!Y&&!!fils.length;
+}
+function applyTaylorOscillation(atTime=tPhys){
+  // Taylor-forcing hoort uitsluitend bij de solo-proef. Een moduswissel mag
+  // geen verborgen kinematische verplaatsing van drager A in botsing geven.
+  if(!taylorOscillationApplies())return;
   const st=carrierGroupStats('A');
   const zAnchor=(P.mode==='solo')?P.zSolo:P.zA;
-  const zT=zAnchor+P.taylorOsc.amplitude*Math.sin(2*Math.PI*tPhys/Math.max(0.5,P.taylorOsc.period));
+  const zT=zAnchor+P.taylorOsc.amplitude*Math.sin(2*Math.PI*atTime/Math.max(0.5,P.taylorOsc.period));
   const dz=zT-st.z;
   if(Math.abs(dz)<1e-9)return;
   for(const f of carrierFilaments('A'))for(let k=0;k<f.N;k++)Y[f.off+3*k+2]+=dz;
 }
+function reverseReferenceError(){
+  if(!reverseReference||!Y||Y.length<reverseReference.length)return NaN;
+  let num=0,den=0;
+  for(let i=0;i<reverseReference.length;i++){
+    const d=Y[i]-reverseReference[i];num+=d*d;den+=reverseReference[i]*reverseReference[i];
+  }
+  return Math.sqrt(num/Math.max(den,1e-300));
+}
+function advanceFilamentCandidate(dt,endTime){
+  const umax=rk4Step(dt);
+  // De exacte kinematische correctie ligt vóór de contacttest en wordt ook in
+  // iedere bisectietrial toegepast; zo kan zij de 3a-drempel niet overslaan.
+  applyTaylorOscillation(endTime);
+  wrapFilamentCarriersZ();
+  return umax;
+}
 
 // ================= three.js scène =================
 const canvas=document.getElementById('c3d');
@@ -2862,7 +2939,7 @@
         ux+=(dl[3*j+1]*rz-dl[3*j+2]*ry)*inv;
         uy+=(dl[3*j+2]*rx-dl[3*j]*rz)*inv;
         uz+=(dl[3*j]*ry-dl[3*j+1]*rx)*inv;}}
-    const omegaBgIsInVelocity=P.bgOmegaCoupling&&!P.coRot;
+    const omegaBgIsInVelocity=P.bgOmegaCoupling;
     if(omegaBgIsInVelocity){ux+=-P.Om*py;uy+=P.Om*px;}
 
     // Orbitale tracer-hoeksnelheid rond de z-as:
@@ -2942,7 +3019,7 @@
       uz+=(sg.dl[3*j]*ry-sg.dl[3*j+1]*rx)*inv;
     }
   }
-  if(P.bgOmegaCoupling&&!P.coRot){ux+=-P.Om*py;uy+=P.Om*px;}
+  if(P.bgOmegaCoupling){ux+=-P.Om*py;uy+=P.Om*px;}
   return {ux,uy,uz,speed:Math.hypot(ux,uy,uz)};
 }
 function insideCylinder(x,y,z){return x*x+y*y<0.9409*P.Rcyl*P.Rcyl&&z>zMin()+0.002&&z<zMax()-0.002;}
@@ -3384,7 +3461,8 @@
 function updateSubtitle(){
   const topoTxt=topologyLabel();
   const nComp=topologyComponentCount();
-  const medTxt={demo:'demo-Γ',he:'He-II, Γ=nκ',sst:'SST, Γ=nΓ₀ [Canon 0.8.19]'}[P.med];
+  const medTxt={demo:'demo-Γ',he:'He-II, Γ=nκ',sst:'SST, Γ=nΓ₀ [Canon 0.8.20]',
+    string:'String-theorie schaalprobe, vrije Γ · geen snaardynamica'}[P.med]||P.med;
   const coreTxt={hol:'holle kern',vast:'vaste kern',gp:'GP-kern'}[P.core];
   const modeTxt=P.mode==='botsing'
     ?`Twee coaxiale ${topoTxt}-dragers${nComp>1?` (${nComp} componenten per drager)`:''}, |Γ| identiek, frontaal${P.inter==='lia'?' · LIA (geen wederzijdse inductie!)':''}`
@@ -3435,18 +3513,23 @@
   const el=document.getElementById('eFormula');
   if(!el||!window.katex)return;
   const parts=[
-    {on:Flags.alpha,t:'\\alpha C(K)',c:'#FF6E6E'},{on:Flags.beta,t:'\\beta L(K)',c:'#FFAE45'},
-    {on:Flags.gamma,t:'\\gamma \\mathcal{H}(K)',c:'#A855F7'},{on:Flags.sep,t:'\\partial V',c:'#EAF2FA'}
+    {on:Flags.alpha,t:'\\alpha C(K)',c:'#FF6E6E'},{on:Flags.beta,t:'\\beta \\widehat{L}(K)',c:'#FFAE45'},
+    {on:Flags.gamma,t:'\\gamma \\widehat{H}(K)',c:'#A855F7'}
   ];
   el.innerHTML='';
   const lead=document.createElement('span');
-  katex.render('\\mathcal{E}_{\\rm eff}[K]=',lead);el.appendChild(lead);
+  katex.render('\\widehat{S}[K]=',lead);el.appendChild(lead);
   parts.forEach((p,i)=>{
     if(i) el.appendChild(document.createTextNode(' + '));
     const s=document.createElement('span');
     s.style.color=p.on?p.c:'#6F82A0';if(p.on)s.style.fontWeight='600';
     katex.render(p.t,s);el.appendChild(s);
   });
+  const sep=document.createElement('span');
+  sep.textContent=' · ∂V: alleen visualisatie';
+  sep.style.color=Flags.sep?'#EAF2FA':'#6F82A0';
+  if(Flags.sep)sep.style.fontWeight='600';
+  el.appendChild(sep);
 }
 function initEnergyToggles(){
   syncEnergyToggles();
@@ -3512,8 +3595,9 @@
   document.getElementById('mirrorRow').classList.toggle('hidden',P.mode!=='botsing');
   document.getElementById('lockVzRow').classList.toggle('hidden',P.mode!=='botsing');
   document.getElementById('qualRow').classList.remove('hidden');
-  document.getElementById('gaRow').classList.toggle('hidden',P.med!=='demo');
-  document.getElementById('nqRow').classList.toggle('hidden',P.med==='demo');
+  const quantized=P.med==='he'||P.med==='sst';
+  document.getElementById('gaRow').classList.toggle('hidden',quantized);
+  document.getElementById('nqRow').classList.toggle('hidden',!quantized);
   document.getElementById('cCenterline').checked=P.showCenterline;
   document.getElementById('cCcwA').checked=P.ccwA;
   document.getElementById('cCcwB').checked=P.ccwB;
@@ -3554,6 +3638,13 @@
   const vvc=document.getElementById('vVorticityColor');if(vvc)vvc.textContent=(P.vorticityLineColor||'#0F1A29').toUpperCase();
   const sAP=document.getElementById('sAPhys');if(sAP)sAP.value=Number.isFinite(P.aPhys)?String(P.aPhys):'';
   const vAP=document.getElementById('vAPhys');if(vAP)vAP.textContent=fmtLengthSI(P.aPhys);
+  const sAPL=document.getElementById('sAPhysLog');if(sAPL&&P.aPhys>0)
+    sAPL.value=String(clamp(Math.log10(P.aPhys),Number(sAPL.min),Number(sAPL.max)));
+  const apPreset=document.getElementById('aPhysPreset');if(apPreset){
+    const rel=(a,b)=>Math.abs(a-b)/Math.max(Math.abs(b),1e-300);
+    apPreset.value=rel(P.aPhys,PLANCK_LENGTH)<1e-9?'planck':
+      (rel(P.aPhys,RCORE_SST)<1e-9?'sst':(rel(P.aPhys,RCORE_HE_REF)<1e-9?'helium':'custom'));
+  }
   syncEnergyToggles();
   document.getElementById('vCore').textContent='Δ = '+({hol:'½',vast:'¼',gp:'0.615'}[P.core]);
   document.getElementById('vGa').textContent=fmtGa();
@@ -4018,16 +4109,17 @@
 segHandler('frameSeg','frame',v=>{
   const co=v==='rotating';if(co===P.coRot)return;
   P.coRot=co;
-  if(P.coRot&&P.bgOmegaCoupling)P.bgOmegaCoupling=false;
   syncUi();updateSubtitle();
 });
 segHandler('medSeg','med',v=>{
   if(v===P.med&&v!=='sst')return;
   if(v==='sst'){
     applySSTSimilarityPreset();
+  }else if(v==='string'){
+    P.med='string';P.core='gp';P.coreFlowLock=false;P.aPhys=PLANCK_LENGTH;
   }else{
     P.med=v;
-    if(v==='he')P.core='hol';
+    if(v==='he'){P.core='hol';P.aPhys=RCORE_HE_REF;}
     P.coreFlowLock=false;
   }
   // Tijdversnelling blijft uitsluitend handmatig.
@@ -4048,15 +4140,11 @@
 document.getElementById('bResetParticles').addEventListener('click',resetParticlesToTaylorColumn);
 document.getElementById('cCoRot').addEventListener('change',e=>{
   P.coRot=e.target.checked;
-  if(P.coRot&&P.bgOmegaCoupling)P.bgOmegaCoupling=false;
   syncUi();updateSubtitle();
 });
 document.getElementById('cBgOmega').addEventListener('change',e=>{
   P.bgOmegaCoupling=e.target.checked;
-  if(P.bgOmegaCoupling&&P.coRot){
-    P.coRot=false;
-    document.getElementById('cCoRot').checked=false;
-  }
+  syncUi();updateSubtitle();
 });
 document.getElementById('cChiArrow').addEventListener('change',e=>{P.showChiArrow=e.target.checked;});
 document.getElementById('cTracers').addEventListener('change',e=>{
@@ -4200,6 +4288,7 @@
   else if(preset==='taylor'){applyTaylorPreset();resetState();}
   else if(preset==='pistol'){applyPistolPreset();resetState();}
   else if(preset==='sst'){applySSTSimilarityPreset();syncUi();updateSubtitle();resetState();}
+  else if(preset==='string'){applyStringTheoryPreset();syncUi();updateSubtitle();resetState();}
   else if(preset==='friction'){applyFrictionPreset();syncUi();updateSubtitle();resetState();}
 });
 document.getElementById('compA').addEventListener('change',e=>{P.compA=+e.target.value;resetState();});
@@ -4320,16 +4409,23 @@
 // 3a-drempel niet met een volle CFL-stap wordt gepasseerd.
 let Ypre=null;
 function bisectFirstHit(signedDtFull,lia){
-  if(P.twistProxyEnabled)return signedDtFull; // proxy-accumulator in rk4Step is niet herstelbaar
+  const tStart=tPhys;
+  return bisectHitTime(signedDtFull,dt=>{
+    Y.set(Ypre);
+    lastUmax=advanceFilamentCandidate(dt,tStart+dt);
+  },()=>{
+    const c=contactEvent(lia);return !!(c&&!c.warn);
+  });
+}
+function bisectHitTime(signedDtFull,advanceFromStart,hasHardHit){
   const sgn=Math.sign(signedDtFull)||1, full=Math.abs(signedDtFull);
   let lo=0,hi=full;
   for(let it=0;it<16&&(hi-lo)>1e-4*full;it++){
     const mid=.5*(lo+hi);
-    Y.set(Ypre);rk4Step(sgn*mid);
-    const c=contactEvent(lia);
-    (c&&!c.warn)?hi=mid:lo=mid;
+    advanceFromStart(sgn*mid);
+    hasHardHit()?hi=mid:lo=mid;
   }
-  Y.set(Ypre);lastUmax=rk4Step(sgn*hi);
+  advanceFromStart(sgn*hi);
   return sgn*hi;
 }
 function setFlag(msg,warnOnly){
@@ -4391,7 +4487,8 @@
     stepCounter++;
     const rep=stabilityLast;
     pushRing('steps',steps,Object.assign({tPhys,dt:extra&&extra.dt,lastUmax,effAcc,
-      score:rep&&rep.score,gapRatio:rep&&rep.gapRatio,maxAk:rep&&rep.maxAk,flagged:!!flagged,warned:!!warned},extra||{}),maxSteps);
+      score:rep&&rep.score,gapRatio:rep&&rep.gapRatio,gapRatioEffective:rep&&rep.gapRatioEffective,
+      maxAk:rep&&rep.maxAk,flagged:!!flagged,warned:!!warned},extra||{}),maxSteps);
     updateStats();
   }
   function exportJson(){
@@ -4424,8 +4521,9 @@
 // Puur lokaal: bouwt eigen toestandsarrays, raakt globale Y/fils niet aan;
 // P-velden worden gesnapshot en hersteld. Resultaten als JSON exporteerbaar.
 const SelfTest=(()=>{ 
-  const PKEYS=['mode','topo','inter','core','med','qual','Om','GaDemo','nQ','a','w','vzA','vzB',
-    'lockVz','coRot','bgOmegaCoupling','mfTemp','mfAlpha','mfAlphaP','vnZ','timeReverse','coreFlowLock'];
+  const PKEYS=['mode','topo','inter','core','med','qual','Om','GaDemo','nQ','a','aPhys','w','vzA','vzB',
+    'lockVz','coRot','bgOmegaCoupling','mfTemp','mfAlpha','mfAlphaP','vnZ','timeReverse','coreFlowLock',
+    'centerLock','twistProxyEnabled','ghostStewartson','Rcyl','Hcyl'];
   function snap(){const o={};for(const k of PKEYS)o[k]=P[k];o.tOsc=P.taylorOsc.enabled;return o;}
   function restore(o){for(const k of PKEYS)P[k]=o[k];P.taylorOsc.enabled=o.tOsc;syncUi();}
   function baseline(){
@@ -4468,26 +4566,53 @@
     try{
       baseline();
       const metaVersion=document.querySelector('meta[name="vortexlab-version"]')?.content;
-      add('T0 versie/provenance consistent',metaVersion===APP_VERSION&&APP_BASE_VERSION==='7.3.1','meta='+metaVersion+' runtime='+APP_VERSION+' base='+APP_BASE_VERSION);
+      const metaBase=document.querySelector('meta[name="vortexlab-base"]')?.content;
+      const titleOk=document.title.includes('v'+APP_VERSION);
+      const footOk=document.getElementById('footNote')?.textContent.trim().startsWith('v'+APP_VERSION);
+      const canonTags=[...document.querySelectorAll('.tag')].filter(x=>/canon/i.test(x.textContent));
+      const canonOk=canonTags.length>0&&canonTags.every(x=>x.textContent.includes('0.8.20'));
+      add('T0 versie/provenance consistent',metaVersion===APP_VERSION&&metaBase===APP_BASE_VERSION&&titleOk&&footOk&&canonOk,
+        'meta='+metaVersion+' base='+metaBase+' title='+titleOk+' foot='+footOk+' canon='+canonOk);
+      const scoreText=(document.querySelector('#collEnergy > summary')?.textContent||'')+' '+
+        (document.getElementById('eFormula')?.textContent||'');
+      add('T0a scorelabel zonder energieclaim',/GEOMETRISCHE SCORE/.test(scoreText)&&!/E_eff|Eeff|𝓔/.test(scoreText),scoreText.trim());
       const diag=buildDiagRecord(1,2,3,{R:4,z:5});
       add('T0b diag-record ACN eindig',diag.ACN===3&&Object.values(diag).filter(v=>typeof v==='number').every(Number.isFinite),'ACN='+diag.ACN);
       const parsed=parseLengthInput('1.40897017 fm');
       add('T0c SI-lengteparser fm',Math.abs(parsed-RCORE_SST)<1e-24,'a='+parsed.toExponential(9));
-      const oldA=P.a;P.a=A_SIM_INPUT_FLOOR;const ct=contactThresholdInfo();P.a=oldA;
-      add('T0d contactvloer eindig',Number.isFinite(ct.effective)&&ct.effective>=ct.physical&&ct.effective>=ct.numerical,'d='+ct.effective.toExponential(3));
-      // T1 — ringsnelheid vs Kelvin-formule, drie kernmodellen
+      const parsedPlanck=parseLengthInput('planck'),fmtPlanck=fmtLengthSI(PLANCK_LENGTH);
+      add('T0d Planck-parser en formatter',Math.abs(parsedPlanck/PLANCK_LENGTH-1)<1e-15&&!/^0(?:\.0+)?\s/.test(fmtPlanck),fmtPlanck);
+      const oldA=P.a;P.a=A_SIM_INPUT_FLOOR;const ct=contactThresholdInfo(),gr=gapRatios(1e-9);P.a=oldA;
+      add('T0e contactvloer en exact g_a',Number.isFinite(ct.effective)&&ct.effective>=ct.physical&&ct.effective>=ct.numerical&&Math.abs(gr.physical-1e9)<1,
+        'd_eff='+ct.effective.toExponential(3)+' g_a='+gr.physical.toExponential(3));
+      // T1 — ringsnelheid vs Kelvin-formule, drie kernmodellen en volledige N-sweep
       for(const core of['hol','vast','gp']){
-        P.core=core;const N=256,R=0.07,fl=[{off:0,N,carrier:'A'}];
-        const Yl=ring(N,R),V=new Float64Array(3*N);
-        velocityCore(Yl,fl,V,false,{includeExternal:false});
-        const[rad,vz]=meanRadVz(V,N);
-        const err=Math.abs(vz-kelvinSpeed(R))/kelvinSpeed(R);
-        add('T1 Kelvin-snelheid core='+core+' (N=256)',err<5e-4,'relfout='+err.toExponential(2));
+        P.core=core;const R=0.07,errs=[];
+        for(const N of[48,96,144,192,256]){
+          const fl=[{off:0,N,carrier:'A'}],Yl=ring(N,R),V=new Float64Array(3*N);
+          velocityCore(Yl,fl,V,false,{includeExternal:false});
+          const vz=meanRadVz(V,N)[1];errs.push([N,Math.abs(vz-kelvinSpeed(R))/kelvinSpeed(R)]);
+        }
+        const maxErr=Math.max(...errs.map(x=>x[1]));
+        const highErr=Math.max(...errs.filter(x=>x[0]>=144).map(x=>x[1]));
+        add('T1 Kelvin-snelheid core='+core+' (N=48…256)',maxErr<7.5e-4&&highErr<1e-4,
+          'max='+maxErr.toExponential(2)+' high='+highErr.toExponential(2)+' · '+errs.map(x=>x[0]+':'+x[1].toExponential(1)).join(' '));
       }
       P.core='gp';
       // T2 — exacte segment-segmentafstand (interieurnadering)
       const d=Math.sqrt(segSegDist2(-1,0,0,1,0,0, 0,1e-3,-1, 0,1e-3,1));
       add('T2 segmentafstand interieur',Math.abs(d-1e-3)<1e-12,'d='+d.toExponential(6));
+      // Bekende passage: twee parallelle segmenten starten op d=1 en raken
+      // d*=0.2 na |dt|=0.8. Zelfde uitkomst met transportdiagnostiek aan/uit.
+      function syntheticHit(sign){let tau=0;
+        const got=bisectHitTime(sign,dt=>{tau=Math.abs(dt);},()=>{
+          const y=1-tau;
+          return segSegDist2(-1,0,0,1,0,0,0,y,-1,0,y,1)<=0.2*0.2;});
+        return got;}
+      P.twistProxyEnabled=false;const hitF=syntheticHit(1),hitB=syntheticHit(-1);
+      P.twistProxyEnabled=true;const hitDiag=syntheticHit(1);P.twistProxyEnabled=false;
+      add('T2b first-hit voor/achter + diagnose-invariant',Math.abs(hitF-0.8)<1e-4&&Math.abs(hitB+0.8)<1e-4&&hitDiag===hitF,
+        'voor='+hitF.toFixed(6)+' achter='+hitB.toFixed(6)+' diag='+hitDiag.toFixed(6));
       // T3 — topologische integertest + exacte writhe
       {const N=128,Yh=new Float64Array(6*N);
        for(let k=0;k<N;k++){const t=2*Math.PI*k/N;
@@ -4499,16 +4624,21 @@
        add('T3b ring Wr=0 (asin-conditionering ~1e-7)',Math.abs(wr)<5e-7,'Wr='+wr.toExponential(2));
        const wt=[192,384].map(n=>gauss2(0,n,0,n,true,sampleFourierKnot(IDEAL_TREFOIL_3_1_1.coeffs,n))[0]);
        add('T3c trefoil Wr exact (N=384)',Math.abs(Math.abs(wt[1])-3.417)<0.01,'Wr='+wt[1].toFixed(4)+' (N=192: '+wt[0].toFixed(4)+')');}
-      // T4 — batch-invariantie van de debet-loop (algoritme-replica)
+      // T4 — debet-/framerateschema-invariantie van dezelfde volledige-CFL-
+      // logica als de hoofdlus (zonder render- en evaluatiebudget).
       {const N=96,fl=[{off:0,N,carrier:'A'}];
-       function runBatched(perFrame){
-         const Yl=ring(N,0.07,0.05,5),B=bufs(3*N);let um=0,steps=0;
-         while(steps<24){let inFrame=0;
-           while(inFrame<perFrame&&steps<24){um=localRK4(Yl,fl,localDt(Yl,N,um),B);steps++;inFrame++;}}
-         return Yl;}
-       const A1=runBatched(1),A7=runBatched(7);let eq=true;
-       for(let i=0;i<A1.length;i++)if(A1[i]!==A7[i]){eq=false;break;}
-       add('T4 batch-invariantie (1 vs 7 stappen/frame)',eq,eq?'bit-identiek':'afwijking');}
+       const Yseed=ring(N,0.07,0.05,5),total=24*localDt(Yseed,N,0);
+       function schedule(n){const a=new Array(n).fill(total/n),sum=a.slice(0,-1).reduce((x,y)=>x+y,0);a[n-1]=total-sum;return a;}
+       function runScheduled(frames){
+         const Yl=Yseed.slice(),B=bufs(3*N);let um=0,steps=0,debt=0,dtNext=localDt(Yl,N,um);
+         for(const advance of frames){debt+=advance;
+           while(debt>=dtNext){um=localRK4(Yl,fl,dtNext,B);debt-=dtNext;steps++;dtNext=localDt(Yl,N,um);}
+           debt=Math.min(debt,dtNext);}
+         return{Yl,steps,debt};}
+       const A30=runScheduled(schedule(30)),A60=runScheduled(schedule(60)),A144=runScheduled(schedule(144));let eq=true;
+       for(let i=0;i<A30.Yl.length;i++)if(A30.Yl[i]!==A60.Yl[i]||A30.Yl[i]!==A144.Yl[i]){eq=false;break;}
+       add('T4 debet-invariantie (30/60/144 frames)',eq&&A30.steps===A60.steps&&A30.steps===A144.steps,
+         'stappen='+A30.steps+'/'+A60.steps+'/'+A144.steps+' '+(eq?'bit-identiek':'afwijking'));}
       // T5 — achterwaartse round-trip, 4e-orde
       {const N=96,fl=[{off:0,N,carrier:'A'}];
        function rt(dt,n){const Y0=ring(N,0.07,0.05,5),Yl=Y0.slice(),B=bufs(3*N);
@@ -4528,6 +4658,47 @@
        const eU=Math.abs(vz-(1-P.mfAlphaP)*U0)/Math.abs(U0);
        applyMfTemp('0');
        add('T6 wrijving Ṙ=−αU en (1−α′)U',eR<1e-10&&eU<1e-10,'εR='+eR.toExponential(1)+' εU='+eU.toExponential(1));}
+      // T7 — visuele ghost is exact krachteloos als doel én als bron.
+      {const N=64,main=ring(N,0.07),Yg=new Float64Array(6*N);Yg.set(main);
+       const ghost=ring(N,0.11);for(let i=0;i<ghost.length;i+=3)ghost[i+2]=0.04;Yg.set(ghost,3*N);
+       const V0=new Float64Array(3*N),Vg=new Float64Array(6*N);
+       velocityCore(main,[{off:0,N,carrier:'A'}],V0,false,{includeExternal:false});
+       velocityCore(Yg,[{off:0,N,carrier:'A'},{off:3*N,N,carrier:'G',ghost:true,gammaVal:0}],Vg,false,{includeExternal:false});
+       let eq=true;for(let i=0;i<V0.length;i++)if(V0[i]!==Vg[i]){eq=false;break;}
+       add('T7 ghost aan/uit dynamisch invariant',eq,eq?'bit-identiek':'afwijking');}
+      // T8 — achtergrond-RHS staat los van het displayframe en verschilt exact
+      // met Ω×r van de framegesplitste RHS.
+      {const N=64,Yl=ring(N,0.07),fl=[{off:0,N,carrier:'A'}];
+       const Vrot=new Float64Array(3*N),VlabR=new Float64Array(3*N),VlabA=new Float64Array(3*N);
+       P.mode='solo';P.w=0;P.Om=1.25;P.bgOmegaCoupling=false;P.coRot=true;
+       velocityCore(Yl,fl,Vrot,false,{});
+       P.bgOmegaCoupling=true;P.coRot=true;velocityCore(Yl,fl,VlabR,false,{});
+       P.coRot=false;velocityCore(Yl,fl,VlabA,false,{});
+       let eFrame=0,eDisplay=0;
+       for(let k=0;k<N;k++){
+         const i=3*k,ex=-P.Om*Yl[i+1],ey=P.Om*Yl[i];
+         eFrame=Math.max(eFrame,Math.abs((VlabR[i]-Vrot[i])-ex),Math.abs((VlabR[i+1]-Vrot[i+1])-ey),Math.abs(VlabR[i+2]-Vrot[i+2]));
+         eDisplay=Math.max(eDisplay,Math.abs(VlabR[i]-VlabA[i]),Math.abs(VlabR[i+1]-VlabA[i+1]),Math.abs(VlabR[i+2]-VlabA[i+2]));
+       }
+       add('T8 solver-/displayframe ontvlochten',eFrame<1e-12&&eDisplay===0,'E_RHS='+eFrame.toExponential(2)+' E_display='+eDisplay.toExponential(2));}
+      // T9 — wandmarge gebruikt de tube-rand in radiale én axiale richting.
+      {P.Rcyl=0.25;P.Hcyl=0.5;P.a=1e-3;
+       const mr=tubeBoundaryMarginAt(0.20,0,0),mz=tubeBoundaryMarginAt(0,0,0.49);
+       add('T9 buis-gecorrigeerde wandmarge',Math.abs(mr-0.049)<1e-15&&Math.abs(mz-0.009)<1e-15,'rad='+mr+' ax='+mz);}
+      // T10 — a_phys is metadata: bit-identieke snelheid bij Planck- en
+      // SST-schaal. String-probe gebruikt vrije Γ en activeert geen koppeling.
+      {const N=64,Yl=ring(N,0.07),fl=[{off:0,N,carrier:'A'}],Va=new Float64Array(3*N),Vb=new Float64Array(3*N);
+       P.bgOmegaCoupling=false;P.a=1.2415e-4;P.aPhys=PLANCK_LENGTH;
+       velocityCore(Yl,fl,Va,false,{includeExternal:false});P.aPhys=RCORE_SST;
+       velocityCore(Yl,fl,Vb,false,{includeExternal:false});
+       let eq=true;for(let i=0;i<Va.length;i++)if(Va[i]!==Vb[i]){eq=false;break;}
+       P.med='string';P.GaDemo=0.2;P.coreFlowLock=false;const gs=Gamma();
+       add('T10 a_phys passief + string-probe contract',eq&&Number.isFinite(gs)&&kappaMedium()===null,
+         (eq?'bit-identiek':'afwijking')+' Γ='+gs.toExponential(3));}
+      // T11 — Taylor-forcing is door de modeguard onmogelijk in botsing.
+      {P.taylorOsc.enabled=true;P.centerLock=false;P.mode='botsing';const blocked=!taylorOscillationApplies();
+       P.mode='solo';const allowed=taylorOscillationApplies();P.taylorOsc.enabled=false;
+       add('T11 Taylor-forcing uitsluitend solo',blocked&&allowed,'botsing='+(blocked?'uit':'aan')+' solo='+(allowed?'aan':'uit'));}
     }catch(err){results.push({name:'harnas-exceptie',pass:false,detail:String(err&&err.stack||err)});}
     finally{restore(S);}
     const rep={version:APP_VERSION,baseVersion:APP_BASE_VERSION,date:new Date().toISOString(),ms:Math.round(performance.now()-t0),
@@ -4564,16 +4735,31 @@
 
 function bindAPhysInput(){
   const inp=document.getElementById('sAPhys');
+  const slider=document.getElementById('sAPhysLog');
+  const preset=document.getElementById('aPhysPreset');
   if(!inp)return;
-  const apply=()=>{
-    const v=parseLengthInput(inp.value);
-    if(!Number.isFinite(v)||v<0)return;
+  const commit=(v,source)=>{
+    if(!Number.isFinite(v)||v<=0)return false;
     P.aPhys=v;
     inp.value=String(P.aPhys);
     const vEl=document.getElementById('vAPhys');if(vEl)vEl.textContent=fmtLengthSI(P.aPhys);
+    if(slider)slider.value=String(clamp(Math.log10(P.aPhys),Number(slider.min),Number(slider.max)));
+    if(preset&&source!=='preset'){
+      const rel=(a,b)=>Math.abs(a-b)/Math.max(Math.abs(b),1e-300);
+      preset.value=rel(v,PLANCK_LENGTH)<1e-9?'planck':
+        (rel(v,RCORE_SST)<1e-9?'sst':(rel(v,RCORE_HE_REF)<1e-9?'helium':'custom'));
+    }
+    ModelLog.logUser('aPhys',{value:P.aPhys,source:source||'text'});
+    return true;
   };
+  const apply=()=>commit(parseLengthInput(inp.value),'text');
   inp.addEventListener('change',apply);
   inp.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();apply();}});
+  slider?.addEventListener('input',()=>commit(Math.pow(10,Number(slider.value)),'slider'));
+  preset?.addEventListener('change',()=>{
+    const values={planck:PLANCK_LENGTH,sst:RCORE_SST,helium:RCORE_HE_REF};
+    if(values[preset.value])commit(values[preset.value],'preset');
+  });
 }
 bindAPhysInput();
 document.getElementById('cModelLog')?.addEventListener('change',e=>{ModelLog.setEnabled(e.target.checked);});
@@ -4635,15 +4821,16 @@
     while(stepDebt>=dtNext&&evals<EVAL_BUDGET){
       const signedDt=timeDir*dtNext;
       Ypre.set(Y); // v7.2: snapshot voor first-hit-bisectie
-      lastUmax=rk4Step(signedDt);
+      lastUmax=advanceFilamentCandidate(signedDt,tPhys+signedDt);
       let dtApplied=signedDt;
       // v7.2: contactdetectie (exacte segmentafstand) na iedere geaccepteerde
       // stap; bij hard contact wordt de 3a-passage binnen de stap gebisecteerd.
-      const cev=contactEvent(lia);
+      let cev=contactEvent(lia);
       if(cev&&!cev.warn)dtApplied=bisectFirstHit(signedDt,lia);
+      if(dtApplied!==signedDt)cev=contactEvent(lia)||cev;
+      // Eén update, uitsluitend voor het werkelijk geaccepteerde interval.
+      if(P.twistProxyEnabled)updateTwistProxy(dtApplied,K4);
       phi+=P.Om*dtApplied;tPhys+=dtApplied;
-      applyTaylorOscillation();
-      wrapFilamentCarriersZ();
       if(P.ghostStewartson) syncGhostRing();
       // v7.2 (RP1): tracers per geaccepteerde CFL-stap, met exact dezelfde
       // tijdstap als de filamenten — framerate-onafhankelijk per constructie.
@@ -4754,6 +4941,10 @@
       :(sA.R*100).toFixed(1)+' cm';
     if(sB)document.getElementById('hDz').textContent=(Math.abs(sB.z-sA.z)*100).toFixed(1)+' cm';
     document.getElementById('hT').textContent=tPhys<100?tPhys.toFixed(1)+' s':tPhys.toExponential(2)+' s';
+    {const revRow=document.getElementById('rowRevErr'),revErr=reverseReferenceError();
+     revRow.classList.toggle('hidden',!P.timeReverse);
+     if(P.timeReverse)document.getElementById('hRevErr').textContent=
+       (isFinite(revErr)?revErr.toExponential(3):'—')+' · Δt='+(tPhys-reverseReferenceTime).toExponential(2)+' s';}
     document.getElementById('hAcc').textContent=fmtAcc(Math.max(1e-3,effAcc));
     ModelLog.logDiag(buildDiagRecord(Wr,Lk,ACN,sA)); // v7.3.1: TDZ-vrij, begrensd tot 5 Hz
     hist.push({t:tPhys,RA:sA.R,RB:sB?sB.R:0,dz:sB?Math.abs(sB.z-sA.z):0,zA:sA.z,Wr,
@@ -4783,14 +4974,15 @@
         document.getElementById('hRdot').textContent=fmtSpeed(Rdot)+' / '+fmtSpeed(RdotTh);
       }
     }
-    // GEM-stijl E_eff-kaarten
+    // Geometrische score: uitsluitend de drie gedocumenteerde dimensieloze
+    // termen. ∂V is een visualisatietoggle en staat bewust niet in de som.
     let Lnow=0;for(const f of fils)Lnow+=arcLength(f);
     const Lhat=Lnow/Math.max(1e-9,L0);
-    const Eeff=P.wAl*ACN+P.wBe*Lhat+P.wGa*H;
+    const geoScore=P.wAl*ACN+P.wBe*Lhat+P.wGa*H;
     document.getElementById('cardC').textContent=ACN.toFixed(3);
     document.getElementById('cardL').textContent=Lhat.toFixed(4);
     document.getElementById('cardH').textContent=H.toFixed(3);
-    document.getElementById('cardE').textContent=Eeff.toFixed(3);
+    document.getElementById('cardE').textContent=geoScore.toFixed(3);
     document.getElementById('cardGam').textContent=fmtGa();
     drawSpark();
   }
