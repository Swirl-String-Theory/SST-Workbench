# A038-v0.5.0 provenance-aware modal dispatch

`provenance_lock.py` and `modal_dispatch.py` extend `paper_upgrade/gate.py::upstream_gate`. Fixture geometry/mesh certificates, placeholder hashes, and SELFTEST envelopes cannot emit `QUALIFIED_FOR_MODAL_ANALYSIS`.

A030/A035 remain absent from `REQUIRED`. Qualification is dispatch eligibility only, not dynamic stability.
