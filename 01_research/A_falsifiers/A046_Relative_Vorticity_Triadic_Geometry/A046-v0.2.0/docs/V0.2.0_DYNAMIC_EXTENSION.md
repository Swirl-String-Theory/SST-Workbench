# v0.2.0 dynamic / rotating-frame extension

v0.2.0 promotes the v0.1 structural result into an explicit three-layer test chain:

\[
\mathbf u
\longrightarrow
\{\boldsymbol\omega_{\rm rel},S_{ij}\}
\longrightarrow
Q_{\rm org}=\partial_i\partial_j(u_i u_j)
\longrightarrow
\Phi_{\rm bulk}.
\]

The first upgrade is stronger than the v0.1 discriminability test: the code now verifies that
\(S_{ij}\) and \(\boldsymbol\omega\) reconstruct the full local first-order velocity gradient,

\[
\partial_j u_i=S_{ij}-\frac12\epsilon_{ijk}\omega_k.
\]

Thus the surviving basis has a precise status: it is a complete decomposition of local
first-order kinematics. This still does not establish a gravitational metric.

The rotating-frame extension uses the orthodox identity

\[
\boldsymbol\eta
=\boldsymbol\omega_{\rm abs}
=\boldsymbol\omega_{\rm rel}+2\boldsymbol\Omega_p.
\]

A north/south local-tangent-plane control reverses both the matched cyclonic relative-vorticity
vector and the local planetary-rotation axial vector. The absolute-vorticity vector therefore
reverses sign while preserving magnitude. This is a hemisphere control, not a claim that an
axial vector reverses under arbitrary spatial parity.

Delay remains a separate constitutive/memory layer. The blind control uses a dimensionless
circulation signal and tests

\[
\Delta\phi=\omega\tau,
\qquad
M_\tau(t)=\Gamma(t)-\Gamma(t-\tau),
\]

including the \(\tau\to0\) limit. v0.2.0 deliberately does not insert delay into the metric ansatz;
that coupling remains a later falsifier target.

Finally, v0.2.0 seals the complete evidence tree, including Python logs, native build logs,
native test logs, the native module SHA-256, and Python/native parity results before reveal.
