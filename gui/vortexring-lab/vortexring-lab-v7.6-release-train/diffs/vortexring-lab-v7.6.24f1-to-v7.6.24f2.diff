--- vortexring-lab-v7.6.24f1.html
+++ vortexring-lab-v7.6.24f2.html
@@ -3,8 +3,8 @@
 <head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
-<title>Superfluïde vortexlab v7.6.24f1 · draggable bottom HUD hotfix</title>
-<meta name="vortexlab-version" content="7.6.24f1">
+<title>Superfluïde vortexlab v7.6.24f2 · responsive runner workflow + border drag hotfix</title>
+<meta name="vortexlab-version" content="7.6.24f2">
 <meta name="vortexlab-base" content="7.5.3">
 <meta name="vortexlab-parent" content="7.6.22">
 <meta name="vortexlab-patch" content="sst-vortex-stretching-gate,friedlander-three-profile-benchmark,material-line-strain,topology-preserving-step-guard,auto-relax-rollback,neumann-source-panel-bem,passive-first-hit,taylor-contact,exact-gap-ratios,planck-scale-probe,regression-matrix,v7.5.4-validator,dual-sidebar-ui,ideal-knot-catalog,fseries-knot-catalog,arclength-resampling,multicomponent-links,potential-flow-cp,tracer-three-stage-reset,tracer-20000,flag-below-topbar,bottom-simulator-overlays,flat-geometric-diagnostics,sst-diagnostics-siblings,speculative-two-knot-swirl-clock,two-route-clock-diagnostic,adjustable-initial-knot-separation,distance-sweep-control,spec-clock-quick-preset,spec-clock-quick-controls,spec-clock-overlay,model-log-text-export,model-log-tdz-hotfix,unmapped-proxy-semantics,stable-incremental-spec-overlay,free-distance-number-input,focus-safe-control-sync,bound-spec-clock-controls,far-distance-periodic-z-guard,phase-null-isolated-subtraction,single-reference-calibration-lock,stable-log-clock-bracket,differential-lag-accumulator,external-drift-cfl-bound,accepted-step-time-cap,playback-debt-reset,spec-clock-calibrate-autostart,spec-clock-safe-speed-preset,independent-four-panel-docks,run-header-dropdown,independent-panel-persistence,dual-overlay-offsets,benchmark-verdict-persistence,benchmark-restore-status,proxy-decomposition-snapshots,weighted-rigid-fit,counterfactual-shapley-attribution,direct-mutual-linearity,proxy-decomposition-exports,zero-safe-shapley-gate,cyclic-mutual-normalization,resolution-ladder-128-384,rot-mutual-interaction-export,proxy-decomposition-schema-1.1,normalization-benchmark,iso-full-mutual-reference-scales,circulation-length-scale,rms-arclength-scale,core-circulation-scale,normalization-parity-gate,normalization-resolution-gate,proxy-decomposition-schema-1.2,transfer-law-benchmark,dimensionless-transfer-registry,no-fit-transfer-gate,transfer-parity-null-resolution,transfer-field-ranking,proxy-decomposition-schema-1.3,canonical-vchar-notation,legacy-speed-symbol-removal,proxy-decomposition-schema-1.4,length-identification-benchmark,canonical-resolved-loop-length,calibrated-field-delta-comparison,absolute-vs-calibrated-route,ideal-ropelength-length-candidates,asim-length-negative-control,proxy-decomposition-schema-1.5,compact-spec-clock-sidebar,collapsible-spec-benchmarks,geom-kappa-benchmark,resolution-ladder-128-768,ideal-knot-radius-diameter-audit,proxy-decomposition-schema-1.6,continuum-audit,corrected-r6-six-point-gate,high-resolution-kappa-admissibility,reach-convergence-blocker,cross-knot-holdouts,timestamped-auto-exports,proxy-decomposition-schema-1.7,resizable-clock-sidebar,intrinsic-rigid-rotation-vector,canonical-holdout-embeddings,embedding-pair-audit,applicable-finite-kappa-aggregation,roadmap-panel,proxy-decomposition-schema-1.8,benchmark-gate-stabilization,mixed-tolerance-linearity,intrinsic-cyclic-index-gate,applicable-normalization-match,topology-specific-r22,benchmark-tracer-suppression,selectable-knot-holdouts,split-benchmark-runners,direct-cyclic-permutation,seven-one-holdout,proxy-decomposition-schema-2.0,knotplot-candidate-catalog,tlink-6-9-holdout,multisource-holdout-selector">
@@ -639,7 +639,7 @@
     @media(max-width:760px){.spec-knot-options{grid-template-columns:repeat(2,minmax(0,1fr))}}
 
 
-  /* v7.6.24f1 · draggable/collapsible bottom HUD + spark closed-state fix; v7.6.24f data base */
+  /* v7.6.24f2 · responsive sequenced runner workflow + border/title drag; v7.6.24f1 HUD base */
   .vl-global-run-strip{
     position:fixed;top:var(--vl-topbar-h);left:8px;right:8px;z-index:10020;
     min-height:48px;padding:7px 10px;border:1px solid rgba(85,214,255,.68);border-radius:0 0 9px 9px;
@@ -657,13 +657,17 @@
   .vl-global-run-progress>span{display:block;width:0;height:100%;background:linear-gradient(90deg,#55D6FF,#7BE8A8);transition:width .15s linear;}
   body.vl-run-strip-active #flag{top:calc(var(--vl-topbar-h) + 58px)!important;}
 
-  .spec-clock-runner-hub{margin:0 0 9px;padding:8px;border:1px solid rgba(255,174,69,.45);border-radius:8px;background:rgba(255,174,69,.045);}
+  .spec-clock-runner-hub{margin:0 0 9px;padding:8px;border:1px solid rgba(255,174,69,.45);border-radius:8px;background:rgba(255,174,69,.045);container-type:inline-size;}
   .spec-clock-runner-hub>summary{cursor:pointer;list-style:none;color:#FFE1B4;font-size:11px;font-weight:800;letter-spacing:.07em;display:flex;align-items:center;gap:7px;}
   .spec-clock-runner-hub>summary::-webkit-details-marker{display:none}.spec-clock-runner-hub>summary::before{content:"▾"}.spec-clock-runner-hub:not([open])>summary::before{content:"▸"}
-  .spec-clock-runner-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px;margin-top:8px;}
-  .spec-clock-runner-grid button{width:100%;min-height:34px;white-space:normal;line-height:1.25;}
-  .spec-clock-runner-grid .runner-stop{border-color:rgba(255,110,110,.55);}
-  @media(max-width:620px){.spec-clock-runner-grid{grid-template-columns:1fr;}}
+  .spec-clock-runner-grid{display:flex;flex-wrap:wrap;align-items:stretch;gap:6px;margin-top:8px;}
+  .spec-clock-runner-grid button{flex:1 1 155px;width:auto;min-width:0;min-height:34px;white-space:normal;line-height:1.25;}
+  .spec-clock-runner-grid .runner-stop{border-color:rgba(255,110,110,.72);background:rgba(255,110,110,.08);}
+  .spec-clock-runner-grid .runner-locked{opacity:.48;border-style:dashed;}
+  .spec-clock-runner-grid .runner-done:not(.runner-stop){border-color:rgba(123,232,168,.55);}
+  #bSpecBenchmarkStop,#bProxyDecompStop{display:none!important;}
+  .spec-clock-workflow-hint{flex:1 0 100%;font-size:9px;line-height:1.35;color:var(--muted);padding:2px 1px 0;}
+  @container (max-width:420px){.spec-clock-runner-grid button{flex-basis:100%;}}
 
   .spec-benchmark-table-wrap{max-height:none!important;overflow-x:auto!important;overflow-y:visible!important;scrollbar-gutter:auto!important;}
   .spec-benchmark-table thead th{position:sticky;top:0;}
@@ -680,7 +684,7 @@
   }
 
   .vl-sim-bottom-overlays{z-index:940!important;align-items:flex-end!important;overflow:visible!important;}
-  .vl-bottom-widget{display:flex;flex-direction:column;min-width:0;max-height:calc(100vh - var(--vl-topbar-h) - 72px);pointer-events:auto;}
+  .vl-bottom-widget{display:flex;flex-direction:column;position:relative;min-width:0;max-height:calc(100vh - var(--vl-topbar-h) - 72px);pointer-events:auto;box-sizing:border-box;border:1px solid transparent;border-radius:9px;padding:3px;}
   .vl-bottom-widget>summary{order:2;cursor:pointer;list-style:none;min-height:28px;padding:6px 8px;border:1px solid var(--vl-border);border-radius:7px;background:rgba(13,21,41,.97);box-shadow:0 8px 24px rgba(0,0,0,.36);color:#DCEBFA;font-size:8px;font-weight:800;letter-spacing:.1em;white-space:nowrap;display:flex;align-items:center;gap:6px;}
   .vl-bottom-widget>summary::-webkit-details-marker{display:none}.vl-bottom-widget>summary::before{content:"▾";color:var(--ice)}.vl-bottom-widget:not([open])>summary::before{content:"▴"}
   .vl-bottom-widget>summary::after{content:"inklappen";margin-left:auto;color:var(--dim);font-size:7px;font-weight:500;letter-spacing:0;}
@@ -693,9 +697,11 @@
   .vl-bottom-widget>summary:active{cursor:grabbing;}
   .vl-bottom-drag-grip{color:#6EDCFF;font-size:11px;line-height:1;letter-spacing:-.08em;opacity:.9;}
   .vl-bottom-widget-title{pointer-events:none;}
-  .vl-bottom-widget-undocked{position:fixed!important;right:auto!important;bottom:auto!important;z-index:10010!important;flex:none!important;max-width:min(720px,calc(100vw - 16px));}
+  .vl-bottom-widget-undocked{position:fixed!important;right:auto!important;bottom:auto!important;z-index:10010!important;flex:none!important;max-width:min(720px,calc(100vw - 16px));border-color:rgba(110,220,255,.48);background:rgba(8,17,33,.72);box-shadow:0 12px 34px rgba(0,0,0,.38);}
   .vl-bottom-widget-undocked>summary{box-shadow:0 10px 30px rgba(0,0,0,.48);}
   .vl-bottom-widget-undocked.vl-bottom-widget-dragging{opacity:.94;}
+  .vl-bottom-widget.vl-bottom-border-grab{border-color:rgba(110,220,255,.72);cursor:grab;}
+  .vl-bottom-widget.vl-bottom-widget-dragging{cursor:grabbing;}
   .vl-bottom-widget-live{flex:0 1 310px;width:min(310px,27vw)}
   .vl-bottom-widget-spec{flex:0 1 330px;width:min(330px,31vw)}
   .vl-bottom-widget-stats{flex:1 1 500px;width:min(530px,44vw)}
@@ -858,7 +864,7 @@
       <p><b>v7.6.18 gevalideerde normalisatie-uitkomst.</b> De browserexport gaf opnieuw ENGINE PASS en RESEARCH FAIL. De keuze tussen actuele of bevroren body-\(\Omega\) veranderde de baseline slechts op relatieve schaal \(3.2\times10^{-9}\). Geen van de zeven normalisaties sloot met de formele veldschaal: de minst verre kandidaat was \(\Gamma_{\rm eff}/a_{\rm sim}^2\), maar die bleef bij \(N=128\) nog \(7.68\times10^8\) en bij \(N=384\) circa \(4.52\times10^8\) maal te groot. De niet-singuliere normalisaties veranderden tussen \(N=256\) en \(N=384\) nog ongeveer 9.8%; \(|\Omega_{\rm mutual,0}|\) convergeerde beter maar produceerde door de kleine noemer een orde-éénuitkomst. De mismatch wordt daarom niet opgelost door alleen de denominator te vervangen; de ontbrekende stap is een onafhankelijk afgeleide overdrachtswet van directe mutual-\(\Delta\Omega\) naar \(\Delta\ln R\).</p>
       <p><b>v7.6.19 transfer-law benchmark.</b> De passieve snapshotpipeline evalueert een vooraf bevroren register van dimensieloze wetten voor \(\Delta\ln R=\mathcal T(\Delta\Omega_{\rm mutual},\Gamma,a,L,d,v_{\!\boldsymbol{\circlearrowleft}}^{\ast},r_c)\). Het register gebruikt uitsluitend coefficient 1 en bevat advectieve tijden \(a/v_{\!\boldsymbol{\circlearrowleft}}^{\ast},L/v_{\!\boldsymbol{\circlearrowleft}}^{\ast},d/v_{\!\boldsymbol{\circlearrowleft}}^{\ast},r_c/v_{\!\boldsymbol{\circlearrowleft}}^{\ast}\), circulatietijden \(a^2/\Gamma,L^2/\Gamma,d^2/\Gamma\), twee geometrisch gedempte varianten en één expliciete Buckingham-monomiaal met alle zeven invoervariabelen. Iedere wet krijgt een dimensie-identiteit, nuldriftlek, A/B-pariteit, N=128–384-convergentie en veldschaalratio. Geen vrije gain wordt berekend of toegepast; een kandidaat moet alle vooraf vastgelegde gates tegelijk halen.</p>
       <p><b>v7.6.20 canonieke notatie-audit.</b> Canon v0.8.20 reserveert \(v_{\!\boldsymbol{\circlearrowleft}}^{\ast}=\lVert\mathbf v_{\!\boldsymbol{\circlearrowleft}}\rVert\) voor de gekalibreerde scalaire karakteristieke swirl-snelheid en \(\mathbf u_{\!\boldsymbol{\circlearrowleft}}(\mathbf x,t)\) voor het lokale snelheidsveld. De benchmark gebruikt daarom uitsluitend de machine-key <code>vChar</code>, de interne constante <code>V_CHAR_SST</code> en de zichtbare notatie <code>v↺*</code>. De legacy snelheidsletter en de bijbehorende JSON-/exponent-key zijn volledig verwijderd; er is bewust geen alias of backwards-compatibilityveld toegevoegd. De numerieke kandidaatwetten en toleranties zijn ongewijzigd.</p>
-      <p><b>v7.6.24f1 HUD-hotfix.</b> LIVE STABILITEIT, SPEC CLOCK, STATS en SPARK zijn afzonderlijke sleepbare vensters. De samenvatting is de drag-handle; dubbelklik zet een venster terug in de onderste dock. Positie en open/dicht-status worden lokaal bewaard. Een expliciete closed-state voorkomt dat <code>#spark</code> door een oudere <code>display:block!important</code>-regel zichtbaar blijft wanneer het venster is ingeklapt. <b>v7.6.24f KnotPlot-uitbreiding.</b> Een derde externe catalogusbron <code>knotplot_knots_data.js</code> is beschikbaar in CONFIGURATIE en in de Swirl-Clock-holdoutselector. De eerste kandidaat is <b>Tlink_6_9</b>: een D1/40k KnotPlot-relaxatie van de driecomponenten-toruslink T(6,9), met drie trefoilcomponenten en afgeronde paarsgewijze linking |Lk|=6. Dit is een relaxed candidate, niet een gecertificeerde globale ideal/tight embedding. De holdout gebruikt alle drie componenten per drager, N=128 per component en a_sim=0.1 mm zodat de topology-guard niet al bij t=0 door de dunne D1-linkgeometrie wordt geactiveerd. <b>v7.6.24e1 startup-hotfix.</b> De globale runstrip bindt zijn lokale DOM-nodes nu veilig voordat de UI-shell aan document.body wordt gekoppeld. De overige reporting- en run-UI-wijzigingen van v7.6.24e blijven ongewijzigd. <b>v7.6.24e reporting- en run-UI-hotfix.</b> D14 bewaart nu het visuele snapshotbewijs in ieder analyseresultaat. Een globale status/progressbalk zweeft onder de header. Swirl-Clock-runners staan bovenaan bijeen, ongebruikte test- en resultaatsecties klappen bij start in, resultaatsecties markeren ongeziene wijzigingen, interne resultatentabellen hebben geen verticale scrollbars, en de onderste LIVE/SPEC/STATS/SPARK-HUD is per onderdeel naar de benedenrand inklapbaar.</p>
+      <p><b>v7.6.24f2 workflow/HUD-hotfix.</b> De runnerhub gebruikt responsieve flexknoppen, iedere runner heeft één Start/Stop-toggle en de ENGINE-volgorde SPEC → decompositie → holdouts → continuüm → confirmatoire suite wordt met zichtbare locks afgedwongen. Onderste HUD-vensters zijn via titelbalk én rand te slepen; dubbelklik op titel of rand klapt in, Shift+dubbelklik dockt terug. <b>v7.6.24f1 HUD-hotfix.</b> LIVE STABILITEIT, SPEC CLOCK, STATS en SPARK zijn afzonderlijke sleepbare vensters. De samenvatting is de drag-handle; dubbelklik zet een venster terug in de onderste dock. Positie en open/dicht-status worden lokaal bewaard. Een expliciete closed-state voorkomt dat <code>#spark</code> door een oudere <code>display:block!important</code>-regel zichtbaar blijft wanneer het venster is ingeklapt. <b>v7.6.24f KnotPlot-uitbreiding.</b> Een derde externe catalogusbron <code>knotplot_knots_data.js</code> is beschikbaar in CONFIGURATIE en in de Swirl-Clock-holdoutselector. De eerste kandidaat is <b>Tlink_6_9</b>: een D1/40k KnotPlot-relaxatie van de driecomponenten-toruslink T(6,9), met drie trefoilcomponenten en afgeronde paarsgewijze linking |Lk|=6. Dit is een relaxed candidate, niet een gecertificeerde globale ideal/tight embedding. De holdout gebruikt alle drie componenten per drager, N=128 per component en a_sim=0.1 mm zodat de topology-guard niet al bij t=0 door de dunne D1-linkgeometrie wordt geactiveerd. <b>v7.6.24e1 startup-hotfix.</b> De globale runstrip bindt zijn lokale DOM-nodes nu veilig voordat de UI-shell aan document.body wordt gekoppeld. De overige reporting- en run-UI-wijzigingen van v7.6.24e blijven ongewijzigd. <b>v7.6.24e reporting- en run-UI-hotfix.</b> D14 bewaart nu het visuele snapshotbewijs in ieder analyseresultaat. Een globale status/progressbalk zweeft onder de header. Swirl-Clock-runners staan bovenaan bijeen, ongebruikte test- en resultaatsecties klappen bij start in, resultaatsecties markeren ongeziene wijzigingen, interne resultatentabellen hebben geen verticale scrollbars, en de onderste LIVE/SPEC/STATS/SPARK-HUD is per onderdeel naar de benedenrand inklapbaar.</p>
       <p><b>v7.6.24c stabilisatie.</b> D2, D4 en D6 gebruiken zero-safe mixed tolerances en beoordelen uitsluitend toepasselijke observabelen. R22 is opgesplitst in trefoil-resolutie, topology-specifieke ideal-metadata en Fourier-provenance. Automatische benchmarks schakelen passieve tracers, stroomlijnen en potential-flowweergave tijdelijk uit en herstellen de oorspronkelijke visuele toestand bij complete, stop en fout. <b>v7.6.24b hotfix.</b> Cross-knotholdouts behandelen een nulprojectie van de legacy lab-z-faseproxy als geldig maar niet-informatief, zodat intrinsieke Ω-metingen kunnen doorlopen. De passieve Rosetta-probe biedt ℓ_P/2, ℓ_P en 2ℓ_P als expliciete radius/diameter-auditpresets. De dubbele browser-hovertekst “Toelichting” is bij alle info-iconen verwijderd. <b>v7.6.24a hotfix.</b> De proxy-decompositie initialiseert intrinsieke Ω-kinematica vóór digestberekening, logt setup/checkpointfouten expliciet en voert een echte analyse-smoketest uit. <b>v7.6.24 intrinsieke cross-knotbenchmark.</b> R24–R26 aggregeren uitsluitend toepasselijke eindige kandidaten. Cross-knotmetingen gebruiken een deterministisch gecanonicaliseerde embedding en exporteren de volledige rigid-rotationvector, de intrinsieke carrier-as, Ω<sub>∥</sub>, |Ω| en |Ω<sub>⊥</sub>|. Vijf topology/embeddingparen — 3₁, 4₁, 5₁, 5₂ en 6₁ — vergelijken Fourier- en Gilbert-idealbronnen zonder kandidaatfactoren te trainen. De CLOCK-sidebar is horizontaal resizebaar en bewaart zijn breedte.</p>
       <h2>WEDERZIJDSE WRIJVING (T &gt; 0) <span class="tag">v7</span></h2>
       <p>De knooppuntsnelheid volgt de Schwarz-vergelijking</p>
@@ -1646,7 +1652,7 @@
       </div>
     </details>
     <div class="note" id="footNote">
-      v7.6.24f1 · basis v7.5.3 · draggable/persistent bottom HUD + spark closed-state fix · KnotPlot Tlink_6_9 catalogus + holdout · D14 reporting fix · floating run strip · runner hub · collapsible results/HUD · selectable knot suites · split runners · exact cyclic D4 · 7₁ holdout · corrected D2/D6/R22 · benchmark tracers off + restore · decomposition-start hotfix · runtime smoke · expliciete error/abort logging · intrinsieke Ω-vector · canonical embedding pairs · R24–R26 finite filtering · resizebare CLOCK · roadmap · timestamped auto-export · onafhankelijke INFO/FLOW/KERN/CLOCK/DIAG-panelen · RUN-dropdown in header · SST vortex-stretching stability gate · Friedlander A₁/A₂/A₃-profielen · passieve first-hit/trials · Taylor-contact binnen iedere kandidaatstap · exact g_a plus effectieve numerieke contactratio · passieve Planck/String-schaalprobe · topology guard zonder numerieke reconnectie · Niveau-C 3D Neumann-BEM/MFS · fase-null geïsoleerde aftrek · enkelvoudig vergrendelde referentie · eerste-stap drift-CFL · 50 ms SPEC CLOCK-tijdcap · geautomatiseerde 10-run SPEC CLOCK-benchmark · passieve 5-kanaals proxy-decompositie · zero-safe D3 · mutual-genormaliseerde D4 · resolutieladder N=128–768 · expliciete ROT×MUTUAL-export · zeven-schaals normalisatiebenchmark · transfer-law register zonder fitfactor · canonieke L/v↺*-lengte-identificatie · κ_geom no-fit benchmark · Gilbert radius/diameter-audit · absolute/gekalibreerde veldroutes · dimensionale, nul-, pariteits- en convergentiegates · TXT/JSON/CSV-export · uitgebreide zelftests T0–T13 · ?selftest=1.
+      v7.6.24f2 · basis v7.5.3 · responsive runner-flex + start/stop toggles + workflow locks + border/title drag + double-click collapse · KnotPlot Tlink_6_9 catalogus + holdout · D14 reporting fix · floating run strip · runner hub · collapsible results/HUD · selectable knot suites · split runners · exact cyclic D4 · 7₁ holdout · corrected D2/D6/R22 · benchmark tracers off + restore · decomposition-start hotfix · runtime smoke · expliciete error/abort logging · intrinsieke Ω-vector · canonical embedding pairs · R24–R26 finite filtering · resizebare CLOCK · roadmap · timestamped auto-export · onafhankelijke INFO/FLOW/KERN/CLOCK/DIAG-panelen · RUN-dropdown in header · SST vortex-stretching stability gate · Friedlander A₁/A₂/A₃-profielen · passieve first-hit/trials · Taylor-contact binnen iedere kandidaatstap · exact g_a plus effectieve numerieke contactratio · passieve Planck/String-schaalprobe · topology guard zonder numerieke reconnectie · Niveau-C 3D Neumann-BEM/MFS · fase-null geïsoleerde aftrek · enkelvoudig vergrendelde referentie · eerste-stap drift-CFL · 50 ms SPEC CLOCK-tijdcap · geautomatiseerde 10-run SPEC CLOCK-benchmark · passieve 5-kanaals proxy-decompositie · zero-safe D3 · mutual-genormaliseerde D4 · resolutieladder N=128–768 · expliciete ROT×MUTUAL-export · zeven-schaals normalisatiebenchmark · transfer-law register zonder fitfactor · canonieke L/v↺*-lengte-identificatie · κ_geom no-fit benchmark · Gilbert radius/diameter-audit · absolute/gekalibreerde veldroutes · dimensionale, nul-, pariteits- en convergentiegates · TXT/JSON/CSV-export · uitgebreide zelftests T0–T13 · ?selftest=1.
     </div>
 </div>
 
@@ -1658,8 +1664,37 @@
 <script src="./knotplot_knots_data.js" onerror="void 0"></script>
 <script>
 "use strict";
-const APP_VERSION='7.6.24f1';
+const APP_VERSION='7.6.24f2';
 const APP_BASE_VERSION='7.5.3';
+const VL_CLOCK_WORKFLOW_KEY='vortexlab.clock.workflow.7.6.24f2';
+let VL_CLOCK_ACTIVE_MODE=null;
+const VL_CLOCK_RUNNER_DEFS=[
+  {id:'bSpecBenchmarkStart',mode:'spec',label:'SPEC CLOCK · 10-run',step:1},
+  {id:'bProxyDecompStart',mode:'decomposition',label:'Proxy-decompositie',step:2},
+  {id:'bHoldoutStart',mode:'holdout',label:'Geselecteerde holdouts',step:3},
+  {id:'bContinuumStart',mode:'continuum',label:'Continuüm N=128–768',step:4},
+  {id:'bGeomKappaStart',mode:'full-suite',label:'Volledige confirmatoire suite',step:5}
+];
+const VLClockWorkflow=(()=>{
+  const defaults=()=>({spec:false,decomposition:false,holdout:false,continuum:false});
+  let state=defaults();
+  try{state={...state,...JSON.parse(sessionStorage.getItem(VL_CLOCK_WORKFLOW_KEY)||'{}')};}catch(_){}
+  const save=()=>{try{sessionStorage.setItem(VL_CLOCK_WORKFLOW_KEY,JSON.stringify(state));}catch(_){}};
+  const unlocked=mode=>mode==='spec'||(mode==='decomposition'&&state.spec)||(mode==='holdout'&&state.spec&&state.decomposition)||(mode==='continuum'&&state.spec&&state.decomposition&&state.holdout)||(mode==='full-suite'&&state.spec&&state.decomposition&&state.holdout&&state.continuum);
+  const done=mode=>mode==='full-suite'?state.spec&&state.decomposition&&state.holdout&&state.continuum:!!state[mode];
+  const invalidateFrom=mode=>{const order=['spec','decomposition','holdout','continuum'],i=order.indexOf(mode);if(i<0)return;for(let k=i;k<order.length;k++)state[order[k]]=false;save();};
+  const begin=mode=>{if(mode!=='full-suite')invalidateFrom(mode);VL_CLOCK_ACTIVE_MODE=mode;vlRefreshClockRunnerWorkflowUi();};
+  const complete=(mode,pass)=>{VL_CLOCK_ACTIVE_MODE=null;if(pass){if(mode==='full-suite'){state={spec:true,decomposition:true,holdout:true,continuum:true};}else if(Object.prototype.hasOwnProperty.call(state,mode))state[mode]=true;}save();vlRefreshClockRunnerWorkflowUi();};
+  const abort=()=>{VL_CLOCK_ACTIVE_MODE=null;save();vlRefreshClockRunnerWorkflowUi();};
+  const invalidateSelection=()=>{state.holdout=false;state.continuum=false;save();vlRefreshClockRunnerWorkflowUi();};
+  const reason=mode=>{if(unlocked(mode))return '';if(mode==='decomposition')return 'Voer eerst SPEC CLOCK · 10-run uit met ENGINE PASS.';if(mode==='holdout')return 'Voer eerst de proxy-decompositie uit met ENGINE PASS.';if(mode==='continuum')return 'Voer eerst de geselecteerde holdouts uit met ENGINE PASS.';if(mode==='full-suite')return 'Voltooi eerst SPEC, decompositie, holdouts en continuüm met ENGINE PASS.';return 'Runner is nog vergrendeld.';};
+  return {unlocked,done,begin,complete,abort,invalidateSelection,reason,get state(){return {...state};}};
+})();
+function vlRefreshClockRunnerWorkflowUi(){
+  const any=!!VL_CLOCK_ACTIVE_MODE;
+  for(const def of VL_CLOCK_RUNNER_DEFS){const b=document.getElementById(def.id);if(!b)continue;const running=VL_CLOCK_ACTIVE_MODE===def.mode,unlocked=VLClockWorkflow.unlocked(def.mode),done=VLClockWorkflow.done(def.mode);b.disabled=any?!running:!unlocked;b.classList.toggle('runner-stop',running);b.classList.toggle('runner-locked',!unlocked&&!running);b.classList.toggle('runner-done',done&&!running);b.dataset.runnerMode=def.mode;b.textContent=running?`■ Stop · ${def.label}`:`${unlocked?(done?'✓':'▶'):'🔒'} ${def.label}`;b.title=running?'Klik om deze runner te stoppen.':(unlocked?`Stap ${def.step} · klik om te starten.`:VLClockWorkflow.reason(def.mode));}
+  const hint=document.getElementById('vlClockWorkflowHint');if(hint){if(VL_CLOCK_ACTIVE_MODE){const d=VL_CLOCK_RUNNER_DEFS.find(x=>x.mode===VL_CLOCK_ACTIVE_MODE);hint.textContent=`Actief: ${d?.label||VL_CLOCK_ACTIVE_MODE}. Alleen deze knop blijft beschikbaar als Stop.`;}else{const next=VL_CLOCK_RUNNER_DEFS.find(x=>x.mode!=='full-suite'&&!VLClockWorkflow.done(x.mode));hint.textContent=next?`Volgende vereiste stap: ${next.step}. ${next.label}. Een stap ontgrendelt pas na ENGINE PASS.`:'Korte workflow voltooid. De volledige confirmatoire suite is ontgrendeld.';}}
+}
 window.__vlBootComplete=false;
 window.addEventListener('error',function vlEarlyBootstrapError(event){
   if(window.__vlBootComplete)return;
@@ -1709,6 +1744,7 @@
   'v7.6.24: volledige rigid-rotationvector en intrinsieke Ω_parallel/Ω_perp-projecties geëxporteerd',
   'v7.6.24: canonicalized embeddingparen voor fseries/ideal 3_1, 4_1, 5_1, 5_2 en 6_1',
   'v7.6.24: ingebouwd roadmapblok voor v7.6.25–v7.7.0',
+  'v7.6.24f2: runnerknoppen responsief in flexrijen; start/stop per runner op één knop; workflow-locks dwingen SPEC → decompositie → holdouts → continuüm → confirmatoir af; onderste HUD sleepbaar via titel én rand, dubbelklik klapt in',
   'v7.6.24f1: onderste LIVE/SPEC/STATS/SPARK-widgets sleepbaar, inklapbaar en persistent; expliciete gesloten-state voor #spark',
   'v7.6.24f: KnotPlot-catalogusbron + Tlink_6_9 driecomponenten-holdout',
   'v7.6.24e1: startup-safe lokale binding voor globale runstrip in detached UI-shell',
@@ -3346,7 +3382,7 @@
   triggerTextDownload(window.ModelLog.exportText(),'text/plain;charset=utf-8',`vortexlab-session-${APP_VERSION.replace(/\./g,'-')}-${context}-${stamp}.txt`);
 }
 
-// ================= v7.6.24f1 draggable bottom HUD + v7.6.24f KnotPlot catalog =================
+// ================= v7.6.24f2 runner workflow + border drag; v7.6.24f KnotPlot catalog =================
 // v7.6.24a: intrinsic Ω before digest + runtime smoke + explicit abort logging
 function benchmarkVisualSnapshot(){
   return {tracerCount:P.tracerCount,showTracers:P.showTracers,showStreamlines:P.showStreamlines,showPotentialFlow:P.showPotentialFlow};
@@ -3492,12 +3528,12 @@
   }
   function applyKnotSelectionPreset(name){
     const sets={quick:['3_1'],core:['3_1','4_1','5_2'],full:Object.keys(KNOT_HOLDOUT_CATALOG),tlink:['Tlink_6_9']},chosen=sets[name]||sets.core;
-    document.querySelectorAll('[data-spec-knot]').forEach(x=>x.checked=chosen.includes(x.dataset.specKnot));const a=document.getElementById('cSpecHoldoutIdeal'),f=document.getElementById('cSpecHoldoutFseries'),k=document.getElementById('cSpecHoldoutKnotplot');if(name==='tlink'){if(a)a.checked=false;if(f)f.checked=false;if(k)k.checked=true;}else{if(a)a.checked=true;if(f)f.checked=true;}updateKnotSelectionUi();
+    document.querySelectorAll('[data-spec-knot]').forEach(x=>x.checked=chosen.includes(x.dataset.specKnot));const a=document.getElementById('cSpecHoldoutIdeal'),f=document.getElementById('cSpecHoldoutFseries'),k=document.getElementById('cSpecHoldoutKnotplot');if(name==='tlink'){if(a)a.checked=false;if(f)f.checked=false;if(k)k.checked=true;}else{if(a)a.checked=true;if(f)f.checked=true;}updateKnotSelectionUi();VLClockWorkflow.invalidateSelection();
   }
   function bindKnotSelector(){
     let saved=null;try{saved=JSON.parse(localStorage.getItem(KNOT_SELECTION_STORAGE)||'null');}catch(_){saved=null;}
     if(saved){const a=document.getElementById('cSpecHoldoutIdeal'),f=document.getElementById('cSpecHoldoutFseries'),k=document.getElementById('cSpecHoldoutKnotplot');if(a)a.checked=saved.ideal!==false;if(f)f.checked=saved.fseries!==false;if(k)k.checked=saved.knotplot!==false;document.querySelectorAll('[data-spec-knot]').forEach(x=>x.checked=(saved.topologies||[]).includes(x.dataset.specKnot));}
-    for(const node of document.querySelectorAll('#cSpecHoldoutIdeal,#cSpecHoldoutFseries,#cSpecHoldoutKnotplot,[data-spec-knot]'))node.addEventListener('change',updateKnotSelectionUi);
+    for(const node of document.querySelectorAll('#cSpecHoldoutIdeal,#cSpecHoldoutFseries,#cSpecHoldoutKnotplot,[data-spec-knot]'))node.addEventListener('change',()=>{updateKnotSelectionUi();VLClockWorkflow.invalidateSelection();});
     document.getElementById('bSpecKnotsQuick')?.addEventListener('click',()=>applyKnotSelectionPreset('quick'));
     document.getElementById('bSpecKnotsCore')?.addEventListener('click',()=>applyKnotSelectionPreset('core'));
     document.getElementById('bSpecKnotsFull')?.addEventListener('click',()=>applyKnotSelectionPreset('full'));
@@ -4111,14 +4147,15 @@
   }
   function complete(){
     active=false;pending=false;current=null;completedAt=new Date().toISOString();evaluate();const engine=worst(gates.filter(g=>g.group==='ENGINE').map(g=>g.status)),research=worst(gates.filter(g=>g.group==='RESEARCH').map(g=>g.status));
-    LastProxyDecompositionSummary={state:'completed',engine,research,completedAt,snapshots:results.length};restoreConfiguration();setStatus(`VOLTOOID · ENGINE ${engine} · RESEARCH ${research} · ${results.length} snapshots · handmatige sessie teruggezet naar t=0.`,engine==='FAIL'?'bad':(engine==='PASS'&&research==='PASS'?'good':'warn'));if(window.ModelLog)window.ModelLog.logEvent('proxy-decomposition-complete',{engine,research,report:reportObject()});updateSpecClockDisplay();render();
+    LastProxyDecompositionSummary={state:'completed',engine,research,completedAt,snapshots:results.length};VLClockWorkflow.complete(launchMode,engine==='PASS');restoreConfiguration();setStatus(`VOLTOOID · ENGINE ${engine} · RESEARCH ${research} · ${results.length} snapshots · handmatige sessie teruggezet naar t=0.`,engine==='FAIL'?'bad':(engine==='PASS'&&research==='PASS'?'good':'warn'));if(window.ModelLog)window.ModelLog.logEvent('proxy-decomposition-complete',{engine,research,report:reportObject()});updateSpecClockDisplay();render();
     if(specAutoExportEnabled()){const stamp=safeUtcStamp(completedAt);setTimeout(()=>download('txt',stamp),80);setTimeout(()=>download('json',stamp),180);setTimeout(()=>download('csv',stamp),280);setTimeout(()=>exportModelLogTimestamped(stamp,'proxy-'+launchMode),380);}
   }
   function start(mode='decomposition'){
     if(active)return;if(SpecClockBenchmark.active){setFlag('⚠ stop eerst de gewone SPEC CLOCK-benchmark.',true);return;}
     launchMode=mode==='normalization'?'normalization':mode==='transfer'?'transfer-law':mode==='length'?'length-identification':mode==='continuum'?'continuum':mode==='holdout'?'holdout':(mode==='geom-kappa'||mode==='continuum-holdout'||mode==='full-suite')?'full-suite':'decomposition';
+    if(!VLClockWorkflow.unlocked(launchMode)){const reason=VLClockWorkflow.reason(launchMode);setFlag('⚠ '+reason,true);setStatus('VERGRENDELD · '+reason,'warn');vlRefreshClockRunnerWorkflowUi();return;}
     scenarios=scenarioSetForMode(launchMode);if(!scenarios.length){setFlag('⚠ selecteer minstens één knoopbron en één knoop voor de holdoutrun.',true);setStatus('GEEN SCENARIO’S · pas de knoopselectie bovenaan CLOCK aan.','warn');return;}
-    LastProxyDecompositionSummary=null;LastSpecClockBenchmarkSummary=null;updateSpecClockDisplay();clearTimeout(timer);snapshot={P:clone(P),paused,presetValue:el('presetSelect')?.value||''};benchmarkVisualState=suppressBenchmarkVisuals('proxy-decomposition');results=[];gates=[];index=-1;current=null;checkpointIndex=0;pending=false;aborted=false;lastError=null;active=true;startedAt=new Date().toISOString();completedAt=null;
+    LastProxyDecompositionSummary=null;LastSpecClockBenchmarkSummary=null;updateSpecClockDisplay();clearTimeout(timer);snapshot={P:clone(P),paused,presetValue:el('presetSelect')?.value||''};benchmarkVisualState=suppressBenchmarkVisuals('proxy-decomposition');results=[];gates=[];index=-1;current=null;checkpointIndex=0;pending=false;aborted=false;lastError=null;active=true;VLClockWorkflow.begin(launchMode);startedAt=new Date().toISOString();completedAt=null;
     if(window.ModelLog){window.ModelLog.setEnabled(true);window.ModelLog.logEvent('proxy-decomposition-start',{schema:SCHEMA,launchMode,knotSelection:readKnotSelection(),cacheEntries:resultCache.size,scenarios,checkpointPlan:Object.fromEntries(scenarios.map(sc=>[sc.id,scenarioCheckpoints(sc)]))});}
     const messages={normalization:'START · normalisatieschalen op de korte proxy-suite…','transfer-law':'START · dimensieloze transferwetten op de korte proxy-suite…','length-identification':'START · L/v↺*-lengteklassen op de korte proxy-suite…',continuum:'START · uitsluitend trefoil-resolutieladder N=128–768…',holdout:'START · uitsluitend geselecteerde canonicalized knoopholdouts…','full-suite':'START · volledige confirmatoire proxy + continuüm + geselecteerde holdouts…',decomposition:'START · korte proxy-decompositie zonder continuüm- of holdoutherhaling…'};
     setStatus(messages[launchMode]||messages.decomposition,'running');render();startScenario();
@@ -4126,14 +4163,14 @@
   function stop(reason='gebruiker'){
     if(!active)return;clearTimeout(timer);const reasonText=String(reason),stoppedScenario=current?.id||null,stoppedCheckpoint=checkpointIndex,total=expectedSnapshotCount();aborted=true;active=false;pending=false;completedAt=new Date().toISOString();setPausedState(true,'proxy-decomposition-stop');
     const abortRecord={reason:reasonText,scenarioId:stoppedScenario,checkpointIndex:stoppedCheckpoint,completedSnapshots:results.length,totalSnapshots:total,lastError,completedAt};if(window.ModelLog)window.ModelLog.logEvent('proxy-decomposition-abort',abortRecord);
-    current=null;LastProxyDecompositionSummary={state:'aborted',completedSnapshots:results.length,totalSnapshots:total,completedAt,error:lastError};restoreConfiguration();gates=[{group:'ENGINE',id:'aborted',label:'Decompositie afgebroken',status:/error/i.test(reasonText)?'FAIL':'WARN',metrics:{completedSnapshots:results.length,totalSnapshots:total,scenarioId:stoppedScenario,checkpointIndex:stoppedCheckpoint},explanation:reasonText}];setStatus(`AFGEBROKEN · ${results.length}/${total} snapshots · ${reasonText}`,/error/i.test(reasonText)?'bad':'warn');render();updateSpecClockDisplay();
+    current=null;VLClockWorkflow.abort();LastProxyDecompositionSummary={state:'aborted',completedSnapshots:results.length,totalSnapshots:total,completedAt,error:lastError};restoreConfiguration();gates=[{group:'ENGINE',id:'aborted',label:'Decompositie afgebroken',status:/error/i.test(reasonText)?'FAIL':'WARN',metrics:{completedSnapshots:results.length,totalSnapshots:total,scenarioId:stoppedScenario,checkpointIndex:stoppedCheckpoint},explanation:reasonText}];setStatus(`AFGEBROKEN · ${results.length}/${total} snapshots · ${reasonText}`,/error/i.test(reasonText)?'bad':'warn');render();updateSpecClockDisplay();
     if(/error/i.test(reasonText)&&specAutoExportEnabled()){const stamp=safeUtcStamp(completedAt);setTimeout(()=>exportModelLogTimestamped(stamp,'proxy-error'),100);}
   }
   function metricKey(){return el('proxyDecompMetric')?.value||'phaseLog';}
   function normalizationMetricKey(){return el('proxyNormMetric')?.value||'netLog';}
   function render(){
     const total=expectedSnapshotCount(),done=results.length,progress=el('proxyDecompProgress');if(progress)progress.style.width=(total?100*done/total:0).toFixed(1)+'%';
-    const starts=['bProxyDecompStart','bContinuumStart','bHoldoutStart','bGeomKappaStart'].map(el),stop=el('bProxyDecompStop'),txt=el('bProxyDecompExportTxt'),json=el('bProxyDecompExportJson'),csv=el('bProxyDecompExportCsv');for(const b of starts)if(b)b.disabled=active;if(stop)stop.disabled=!active;for(const b of [txt,json,csv])if(b)b.disabled=active||!results.length;
+    const stop=el('bProxyDecompStop'),txt=el('bProxyDecompExportTxt'),json=el('bProxyDecompExportJson'),csv=el('bProxyDecompExportCsv');if(stop)stop.hidden=true;for(const b of [txt,json,csv])if(b)b.disabled=active||!results.length;vlRefreshClockRunnerWorkflowUi();
     const summary=el('proxyDecompSummary');if(summary)summary.innerHTML=gates.map(g=>`<span>${g.label}</span><b class="${statusClass(g.status)}">${g.status}</b>`).join('');
     const latest=results[results.length-1],rows=el('proxyDecompRows'),water=el('proxyDecompWaterfall'),key=metricKey();
     if(rows){if(!latest)rows.innerHTML='<tr><td colspan="5" style="text-align:left;color:var(--muted);">Nog geen decompositieresultaten.</td></tr>';else{const totalVal=latest.shapley[key].total;rows.innerHTML=CHANNELS.map(c=>{const a=latest.shapley.A[key].phi[c],b=latest.shapley.B[key].phi[c],net=latest.shapley[key].phi[c],pct=100*net/Math.max(Math.abs(totalVal),1e-300);return `<tr><td>${c}</td><td>${a.toExponential(3)}</td><td>${b.toExponential(3)}</td><td>${net.toExponential(3)}</td><td>${pct.toFixed(2)}%</td></tr>`;}).join('')+`<tr><td>RESIDUAL</td><td>—</td><td>—</td><td>${latest.shapley[key].residual.toExponential(3)}</td><td>${(100*latest.shapley[key].relativeResidual).toExponential(2)}%</td></tr>`;}}
@@ -4160,7 +4197,7 @@
   function reportText(){const r=reportObject(),lines=['VortexLab SPEC CLOCK proxy-decomposition + normalization + transfer-law benchmark','schema='+r.schema,'version='+APP_VERSION+' base='+APP_BASE_VERSION,'launchMode='+launchMode,'startedAt='+startedAt+' completedAt='+completedAt,'ENGINE='+r.engineVerdict+' RESEARCH='+r.researchVerdict,'','[gates]'];gates.forEach((g,i)=>lines.push(`${i+1}. ${g.group} ${g.status} ${g.id} · ${g.label} · metrics=${JSON.stringify(g.metrics)} · ${g.explanation}`));lines.push('','[continuum-audit]',JSON.stringify(r.continuumAudit),'','[cross-knot-holdouts]',JSON.stringify(r.crossKnotAudit),'','[normalization-definitions]',JSON.stringify(NORMALIZATIONS),'','[transfer-law-definitions]',JSON.stringify(r.transferLawDefinitions),'','[length-candidate-definitions]',JSON.stringify(r.lengthCandidateDefinitions),'','[geom-kappa-definitions]',JSON.stringify(r.geomKappaDefinitions),'','[ideal-knot-convention-audit]',JSON.stringify(r.idealKnotConventionAudit),'','[snapshots]');results.forEach((x,i)=>lines.push(`${i+1}. ${JSON.stringify(x)}`));lines.push('','[boundary]',r.scientificBoundary,'[shapley-normalization]',r.normalizationConvention,'[candidate-normalizations]',r.candidateNormalizationConvention,'[interaction]',r.interactionConvention,'[transfer-law]',r.transferLawConvention,'[length-identification]',r.lengthConvention,'[geom-kappa]',r.geomKappaConvention);return lines.join('\n');}
   function reportCsv(){const lines=['recordType,scenario,tPhys,resolution,id,A_value,B_value,netLog,netLinear,netAsinh,denominatorA,denominatorB,fieldScaleRatio,extra1,extra2,pure'];for(const r of results){for(const c of CHANNELS){const ix=r.interactions.phaseLog;lines.push(['SHAPLEY',r.scenarioId,r.tPhys,r.resolution,c,r.shapley.A.phaseLog.phi[c],r.shapley.B.phaseLog.phi[c],r.shapley.phaseLog.phi[c],r.shapley.deltaFrac.phi[c],r.shapley.rawOmega.phi[c],'','',Math.abs(r.shapley.phaseLog.phi[c])/Math.max(Math.abs(r.fieldBracket?.min||0),Math.abs(r.fieldBracket?.max||0),1e-300),ix.interaction,r.cyclicIndex.maxMutualRelative,r.purity].join(','));}for(const n of NORMALIZATIONS){const v=r.normalizations.net[n.id];lines.push(['NORMALIZATION',r.scenarioId,r.tPhys,r.resolution,n.id,v.A.offset,v.B.offset,v.netLog,v.netLinear,v.netAsinh,v.A.denominatorCurrent,v.B.denominatorCurrent,v.fieldScaleRatio,v.A.qCurrent,v.B.qCurrent,r.purity].join(','));}for(const l of TRANSFER_LAWS){const v=r.transferLaws.net[l.id];lines.push(['TRANSFER_LAW',r.scenarioId,r.tPhys,r.resolution,l.id,v.A.value,v.B.value,v.value,'','','','',v.fieldScaleRatio,l.formula,v.requiredCoefficientToFieldEdge,r.purity].join(','));}for(const l of LENGTH_CANDIDATES){const v=r.lengthBenchmark.net[l.id];lines.push(['LENGTH_ROUTE',r.scenarioId,r.tPhys,r.resolution,l.id,v.A.absoluteCurrent,v.B.absoluteCurrent,v.delta,v.absoluteCurrent,'',v.A.lengthCurrent,v.B.lengthCurrent,v.deltaScaleRatio,l.kind,v.absoluteScaleRatio,r.purity].join(','));}for(const k of GEOM_KAPPA_CANDIDATES){const v=r.geomKappa.candidates[k.id];lines.push(['GEOM_KAPPA',r.scenarioId,r.tPhys,r.resolution,k.id,v.kappa,'',v.prediction,'','','','',v.ratio,k.formula,v.relativeResidual,r.purity].join(','));}for(const w of ['A','B'])for(const state of ['current','calibration'])for(const field of ['iso','mutual','full']){const v=r.intrinsicKinematics?.[w]?.[state]?.[field];if(v)lines.push(['INTRINSIC_OMEGA',r.scenarioId,r.tPhys,r.resolution,w+'_'+state+'_'+field,v.vector[0],v.vector[1],v.parallel,v.magnitude,v.perpendicular,v.axis[0],v.axis[1],v.axis[2],r.knot?.topologyKey||'',r.knot?.embedding||'',r.purity].join(','));}}return lines.join('\n');}
   function download(kind,stamp=safeUtcStamp(completedAt||new Date())){const text=kind==='json'?JSON.stringify(reportObject(),null,2):kind==='csv'?reportCsv():reportText(),type=kind==='json'?'application/json':kind==='csv'?'text/csv;charset=utf-8':'text/plain;charset=utf-8',mode=String(launchMode||'decomposition').replace(/[^a-z0-9-]+/gi,'-');triggerTextDownload(text,type,`vortexlab-spec-clock-proxy-decomposition-${APP_VERSION.replace(/\./g,'-')}-${mode}-${stamp}.${kind}`);}
-  function bind(){bindKnotSelector();el('bProxyDecompStart')?.addEventListener('click',()=>start('decomposition'));el('bContinuumStart')?.addEventListener('click',()=>start('continuum'));el('bHoldoutStart')?.addEventListener('click',()=>start('holdout'));el('bGeomKappaStart')?.addEventListener('click',()=>start('full-suite'));el('bProxyDecompStop')?.addEventListener('click',()=>stop('handmatig gestopt'));el('bProxyDecompExportTxt')?.addEventListener('click',()=>download('txt'));el('bProxyDecompExportJson')?.addEventListener('click',()=>download('json'));el('bProxyDecompExportCsv')?.addEventListener('click',()=>download('csv'));el('proxyDecompMetric')?.addEventListener('change',render);el('proxyNormMetric')?.addEventListener('change',render);render();}
+  function bind(){bindKnotSelector();const toggle=(id,mode)=>el(id)?.addEventListener('click',()=>{if(active){if(launchMode===mode)stop('handmatig gestopt');return;}start(mode);});toggle('bProxyDecompStart','decomposition');toggle('bHoldoutStart','holdout');toggle('bContinuumStart','continuum');toggle('bGeomKappaStart','full-suite');const legacyStop=el('bProxyDecompStop');if(legacyStop){legacyStop.hidden=true;legacyStop.addEventListener('click',()=>stop('handmatig gestopt'));}el('bProxyDecompExportTxt')?.addEventListener('click',()=>download('txt'));el('bProxyDecompExportJson')?.addEventListener('click',()=>download('json'));el('bProxyDecompExportCsv')?.addEventListener('click',()=>download('csv'));el('proxyDecompMetric')?.addEventListener('change',render);el('proxyNormMetric')?.addEventListener('change',render);render();}
   function selfTest(){
     const coeff={GEOM:1,PARAM:-2,ROT:3,TRANS:.5,MUTUAL_BS:-4},cf=[];for(let mask=0;mask<32;mask++){let v=0;for(const c of CHANNELS)if(mask&BITS[c])v+=coeff[c];cf.push({metrics:{phaseLog:v}});}const s=shapley(cf,'phaseLog');
     const ref=new Float64Array([0,0,0,1,0,0,0,2,0,0,0,3,1,2,4]),ang=.47,ca=Math.cos(ang),sa=Math.sin(ang),cur=new Float64Array(ref.length);for(let k=0;k<ref.length/3;k++){const i=3*k,x=ref[i],y=ref[i+1],z=ref[i+2];cur[i]=2+ca*x-sa*y;cur[i+1]=-1+sa*x+ca*y;cur[i+2]=.5+z;}const weights=new Float64Array(ref.length/3).fill(1),pose=quaternionRotation(ref,cur,weights),aligned=rotateAlign(ref,pose);let poseErr=0;for(let i=0;i<cur.length;i++)poseErr=Math.max(poseErr,Math.abs(cur[i]-aligned[i]));
@@ -4174,11 +4211,11 @@
     const ok=CHANNELS.every(c=>Math.abs(s.phi[c]-coeff[c])<1e-12)&&Math.abs(s.residual)<1e-12&&poseErr<1e-7&&fitErr<1e-9&&fit.reconstructionRel<1e-12&&Number.isFinite(ss.full)&&ss.relativeResidual<1e-10&&pipelineErr<1e-12&&zeroSafe.score<=1&&Math.abs(ix.interaction-7)<1e-12&&normalizationFinite&&normalizationError<1e-12&&transferFinite&&transferDimensions&&canonicalVChar&&lengthFinite&&lengthIdentityScore<=1&&geomFinite&&idealAudit.pass&&continuumSyntheticPass&&holdoutCatalogs&&holdoutDefinitions&&visualPolicyAudit&&runtimeSmokePass&&!!document.getElementById('specKnotSelector');
     return {schema:SCHEMA,channels:CHANNELS.slice(),normalizations:NORMALIZATIONS.map(n=>n.id),ok,residual:s.residual,poseError:poseErr,rigidFitError:fitErr,pipelineResidual:ss.residual,pipelineError:pipelineErr,zeroSafeScore:zeroSafe.score,interactionIdentityError:Math.abs(ix.interaction-7),normalizationError,normalizationFinite,transferFinite,transferDimensions,canonicalVChar,lengthFinite,lengthIdentity,lengthIdentityScore,lengthCandidates:LENGTH_CANDIDATES.map(l=>l.id),transferLaws:TRANSFER_LAWS.map(l=>l.id),geomKappas:GEOM_KAPPA_CANDIDATES.map(k=>k.id),geomFinite,idealConventionAudit:idealAudit,continuumSynthetic,continuumSyntheticPass,holdoutCatalogs,holdoutDefinitions,visualPolicyAudit,runtimeSmokePass,runtimeSmoke};
   }
-  return {start,stop,bind,render,frameTick,capAcceptedDt,afterAcceptedStep,reportObject,selfTest,readKnotSelection,selectedHoldoutScenarios,scenarioSetForMode,get active(){return active;},get resolutionOverride(){return proxyDecompResolutionOverride;}};
+  return {start,stop,bind,render,frameTick,capAcceptedDt,afterAcceptedStep,reportObject,selfTest,readKnotSelection,selectedHoldoutScenarios,scenarioSetForMode,get active(){return active;},get launchMode(){return launchMode;},get resolutionOverride(){return proxyDecompResolutionOverride;}};
 })();
 
 
-// ================= v7.6.24f1 / v7.6.24f geautomatiseerde SPEC CLOCK benchmark =================
+// ================= v7.6.24f2 / v7.6.24f geautomatiseerde SPEC CLOCK benchmark =================
 const SpecClockBenchmark=(()=>{
   const SCHEMA='vortexlab-spec-clock-benchmark/1.0';
   const scenarios=[
@@ -4204,9 +4241,8 @@
     const node=el('specBenchmarkStatus');if(node){node.textContent=text;node.className='spec-benchmark-status '+cls;}
   }
   function updateButtons(){
-    const start=el('bSpecBenchmarkStart'),stop=el('bSpecBenchmarkStop'),txt=el('bSpecBenchmarkExportTxt'),json=el('bSpecBenchmarkExportJson');
-    if(start)start.disabled=active;if(stop)stop.disabled=!active;
-    const has=results.length>0&&!active;if(txt)txt.disabled=!has;if(json)json.disabled=!has;
+    const stop=el('bSpecBenchmarkStop'),txt=el('bSpecBenchmarkExportTxt'),json=el('bSpecBenchmarkExportJson');if(stop)stop.hidden=true;
+    const has=results.length>0&&!active;if(txt)txt.disabled=!has;if(json)json.disabled=!has;vlRefreshClockRunnerWorkflowUi();
   }
   function render(){
     const progress=el('specBenchmarkProgress');if(progress)progress.style.width=((active?Math.max(0,index)+(current?Math.min(1,tPhys/Math.max(current.duration,1e-9)):0):results.length)/scenarios.length*100).toFixed(1)+'%';
@@ -4337,7 +4373,7 @@
     active=false;current=null;pending=false;completedAt=new Date().toISOString();evaluate();
     const engine=worst(gates.filter(g=>g.group==='ENGINE').map(g=>g.status));
     const research=worst(gates.filter(g=>g.group==='RESEARCH').map(g=>g.status));
-    LastSpecClockBenchmarkSummary={state:'completed',engine,research,completedAt,runCount:results.length};
+    LastSpecClockBenchmarkSummary={state:'completed',engine,research,completedAt,runCount:results.length};VLClockWorkflow.complete('spec',engine==='PASS');
     restoreConfiguration();
     setStatus(`VOLTOOID · ENGINE ${engine} · RESEARCH PROXY ${research} · configuratie teruggezet naar t=0; alleen een nieuwe handmatige sweep vereist herkalibratie.`,engine==='PASS'?(research==='PASS'?'good':'warn'):'bad');
     
@@ -4347,17 +4383,18 @@
   }
   function start(){
     if(active)return;
+    if(!VLClockWorkflow.unlocked('spec')){setFlag('⚠ '+VLClockWorkflow.reason('spec'),true);return;}
     if(SpecClockProxyDecomposition.active){setFlag('⚠ stop eerst de proxy-decompositiebenchmark.',true);return;}
     LastSpecClockBenchmarkSummary=null;LastProxyDecompositionSummary=null;updateSpecClockDisplay();
     clearTimeout(timer);snapshot={P:clone(P),paused,presetValue:el('presetSelect')?.value||''};benchmarkVisualState=suppressBenchmarkVisuals('spec-benchmark');
-    results=[];gates=[];index=-1;current=null;pending=false;aborted=false;active=true;startedAt=new Date().toISOString();completedAt=null;
+    results=[];gates=[];index=-1;current=null;pending=false;aborted=false;active=true;VLClockWorkflow.begin('spec');startedAt=new Date().toISOString();completedAt=null;
     if(window.ModelLog){window.ModelLog.setEnabled(true);window.ModelLog.logEvent('spec-benchmark-start',{schema:SCHEMA,scenarioCount:scenarios.length});}
     setStatus('START · reproduceerbare configuratie wordt opgebouwd…','running');render();startScenario();
   }
   function stop(reason='gebruiker'){
     if(!active)return;
     clearTimeout(timer);aborted=true;active=false;pending=false;current=null;completedAt=new Date().toISOString();setPausedState(true,'spec-benchmark-stop');
-    LastSpecClockBenchmarkSummary={state:'aborted',completedRuns:results.length,totalRuns:scenarios.length,completedAt};
+    LastSpecClockBenchmarkSummary={state:'aborted',completedRuns:results.length,totalRuns:scenarios.length,completedAt};VLClockWorkflow.abort();
     restoreConfiguration();
     gates=[{group:'ENGINE',id:'aborted',label:'Benchmark afgebroken',status:'WARN',metrics:{completedRuns:results.length,totalRuns:scenarios.length},explanation:String(reason)}];
     setStatus(`AFGEBROKEN · ${results.length}/${scenarios.length} runs voltooid · configuratie teruggezet naar t=0.`,'warn');
@@ -4378,7 +4415,7 @@
     const text=kind==='json'?JSON.stringify(reportObject(),null,2):reportText();triggerTextDownload(text,kind==='json'?'application/json':'text/plain;charset=utf-8',`vortexlab-spec-clock-benchmark-${APP_VERSION.replace(/\./g,'-')}-${stamp}.${kind}`);
   }
   function bind(){
-    el('bSpecBenchmarkStart')?.addEventListener('click',start);el('bSpecBenchmarkStop')?.addEventListener('click',()=>stop('handmatig gestopt'));
+    el('bSpecBenchmarkStart')?.addEventListener('click',()=>active?stop('handmatig gestopt'):start());const legacyStop=el('bSpecBenchmarkStop');if(legacyStop){legacyStop.hidden=true;legacyStop.addEventListener('click',()=>stop('handmatig gestopt'));}
     el('bSpecBenchmarkExportTxt')?.addEventListener('click',()=>download('txt'));el('bSpecBenchmarkExportJson')?.addEventListener('click',()=>download('json'));render();
   }
   return {start,stop,bind,render,frameTick,capAcceptedDt,afterAcceptedStep,reportObject,get lastSummary(){return LastSpecClockBenchmarkSummary;},get active(){return active;}};
@@ -7324,10 +7361,11 @@
 }
 function initSpecClockRunnerHub(){
   const selector=vlNode('specKnotSelector'),panel=selector?.parentElement;if(!selector||!panel||vlNode('vlClockRunnerHub'))return;
-  const hub=document.createElement('details');hub.id='vlClockRunnerHub';hub.className='spec-clock-runner-hub';hub.open=true;hub.innerHTML='<summary>▶ TESTRUNNERS · SNEL / DECOMPOSITIE / CONTINUÜM / HOLDOUTS</summary><div class="spec-clock-runner-grid" id="vlClockRunnerGrid"></div>';
-  selector.insertAdjacentElement('afterend',hub);const grid=vlNode('vlClockRunnerGrid');
-  const specs=[['bSpecBenchmarkStart','spec','SPEC CLOCK · 10-run'],['bProxyDecompStart','decomposition','Proxy-decompositie'],['bContinuumStart','continuum','Continuüm N=128–768'],['bHoldoutStart','holdout','Geselecteerde holdouts'],['bGeomKappaStart','full-suite','Volledige confirmatoire suite'],['bSpecBenchmarkStop','spec','Stop SPEC CLOCK'],['bProxyDecompStop','decomposition','Stop diagnose']];
-  for(const [id,mode,label] of specs){const b=vlNode(id);if(!b)continue;b.textContent=(id.includes('Stop')?'■ ':'▶ ')+label;if(id.includes('Stop'))b.classList.add('runner-stop');b.addEventListener('click',()=>vlFocusClockRunner(mode));grid.appendChild(b);}
+  const hub=document.createElement('details');hub.id='vlClockRunnerHub';hub.className='spec-clock-runner-hub';hub.open=true;hub.innerHTML='<summary>▶ TESTRUNNERS · VERPLICHTE ENGINE-VOLGORDE</summary><div class="spec-clock-runner-grid" id="vlClockRunnerGrid"><div class="spec-clock-workflow-hint" id="vlClockWorkflowHint"></div></div>';
+  selector.insertAdjacentElement('afterend',hub);const grid=vlNode('vlClockRunnerGrid'),hint=vlNode('vlClockWorkflowHint');
+  for(const def of VL_CLOCK_RUNNER_DEFS){const b=vlNode(def.id);if(!b)continue;b.classList.add('runner-primary');b.addEventListener('click',()=>vlFocusClockRunner(def.mode));grid.insertBefore(b,hint);}
+  for(const id of ['bSpecBenchmarkStop','bProxyDecompStop']){const b=vlNode(id);if(b)b.hidden=true;}
+  vlRefreshClockRunnerWorkflowUi();
 }
 const VL_BOTTOM_WIDGET_STORAGE='vortexlab.bottomWidgets.v1';
 function vlReadBottomWidgetState(){try{return JSON.parse(localStorage.getItem(VL_BOTTOM_WIDGET_STORAGE)||'{}')||{};}catch(_){return {};}}
@@ -7355,21 +7393,23 @@
   }
 }
 function vlEnableBottomWidgetDrag(widget){
-  const summary=widget?.querySelector(':scope > summary');if(!summary||summary.dataset.vlDragBound==='1')return;summary.dataset.vlDragBound='1';
-  let drag=null,suppressClick=false;
-  summary.title='Sleep om dit venster te verplaatsen · klik om in/uit te klappen · dubbelklik om terug te docken';
-  const end=e=>{if(!drag)return;try{summary.releasePointerCapture(e.pointerId);}catch(_){}
+  const summary=widget?.querySelector(':scope > summary');if(!summary||widget.dataset.vlDragBound==='1')return;widget.dataset.vlDragBound='1';summary.dataset.vlDragBound='1';
+  let drag=null,suppressClick=false;const EDGE=9;
+  summary.title='Sleep titel of vensterrand · dubbelklik om in te klappen · Shift+dubbelklik om terug te docken';
+  const onBorder=e=>{const r=widget.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;return x<=EDGE||y<=EDGE||r.width-x<=EDGE||r.height-y<=EDGE;};
+  const onHandle=e=>summary.contains(e.target)||onBorder(e);
+  const end=e=>{if(!drag)return;try{widget.releasePointerCapture(e.pointerId);}catch(_){}
     if(drag.moved){const rect=widget.getBoundingClientRect();vlStoreBottomWidget(widget,{undocked:true,left:rect.left,top:rect.top,width:rect.width});suppressClick=true;}
     widget.classList.remove('vl-bottom-widget-dragging');drag=null;
   };
-  summary.addEventListener('pointerdown',e=>{if(e.button!==0||e.target.closest('button,a,input,select'))return;const rect=widget.getBoundingClientRect();drag={pointerId:e.pointerId,x:e.clientX,y:e.clientY,left:rect.left,top:rect.top,width:rect.width,moved:false};try{summary.setPointerCapture(e.pointerId);}catch(_){}},true);
-  summary.addEventListener('pointermove',e=>{if(!drag||e.pointerId!==drag.pointerId)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(!drag.moved&&Math.hypot(dx,dy)<4)return;
+  widget.addEventListener('pointerdown',e=>{if(e.button!==0||!onHandle(e))return;if(!onBorder(e)&&e.target.closest('button,a,input,select,textarea'))return;const rect=widget.getBoundingClientRect();drag={pointerId:e.pointerId,x:e.clientX,y:e.clientY,left:rect.left,top:rect.top,width:rect.width,moved:false};try{widget.setPointerCapture(e.pointerId);}catch(_){}},true);
+  widget.addEventListener('pointermove',e=>{if(!drag){widget.classList.toggle('vl-bottom-border-grab',onBorder(e));return;}if(e.pointerId!==drag.pointerId)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(!drag.moved&&Math.hypot(dx,dy)<4)return;
     if(!drag.moved){drag.moved=true;widget.classList.add('vl-bottom-widget-undocked','vl-bottom-widget-dragging');widget.style.width=Math.max(150,drag.width)+'px';}
     const p=vlClampBottomWidget(widget,drag.left+dx,drag.top+dy);widget.style.left=p.left+'px';widget.style.top=p.top+'px';e.preventDefault();
   },{passive:false});
-  summary.addEventListener('pointerup',end,true);summary.addEventListener('pointercancel',end,true);
-  summary.addEventListener('click',e=>{if(!suppressClick)return;suppressClick=false;e.preventDefault();e.stopImmediatePropagation();},true);
-  summary.addEventListener('dblclick',e=>{e.preventDefault();e.stopPropagation();vlDockBottomWidget(widget);},true);
+  widget.addEventListener('pointerup',end,true);widget.addEventListener('pointercancel',end,true);widget.addEventListener('pointerleave',()=>{if(!drag)widget.classList.remove('vl-bottom-border-grab');});
+  widget.addEventListener('click',e=>{if(!suppressClick)return;suppressClick=false;e.preventDefault();e.stopImmediatePropagation();},true);
+  widget.addEventListener('dblclick',e=>{if(!onHandle(e))return;e.preventDefault();e.stopImmediatePropagation();if(e.shiftKey)vlDockBottomWidget(widget);widget.open=false;vlStoreBottomWidget(widget);},true);
   widget.addEventListener('toggle',()=>vlStoreBottomWidget(widget));
   vlApplyBottomWidgetState(widget);
 }
@@ -7618,6 +7658,7 @@
 bindSpecAutoExportControl();
 SpecClockBenchmark.bind();
 SpecClockProxyDecomposition.bind();
+vlRefreshClockRunnerWorkflowUi();
 bindSignedRange('Vn','revVn',x=>fmtAxialMmPerS(x),x=>{P.vnZ=x*1e-3;});
 bindRange('MfA',x=>x.toFixed(3),x=>{P.mfAlpha=clamp(x,0,1);});
 bindRange('MfAp',x=>x.toFixed(4),x=>{P.mfAlphaP=clamp(x,-0.5,0.5);});
@@ -8370,9 +8411,11 @@
        add('T0e24 runstrip, runner-hub en inklapbare HUD aanwezig',!!strip&&!!hub&&resultDetails>=2&&bottomWidgets>=4,'strip='+!!strip+' hub='+!!hub+' results='+resultDetails+' bottom='+bottomWidgets);}
       {const strip=document.getElementById('vlGlobalRunStrip'),close=document.getElementById('vlGlobalRunClose');if(strip){strip.classList.add('active');document.body.classList.add('vl-run-strip-active');}close?.click();
        add('T0e24b runstrip-close is startup-safe gebonden',!!strip&&!!close&&!strip.classList.contains('active')&&!document.body.classList.contains('vl-run-strip-active'),'strip='+!!strip+' close='+!!close);}
-      {const sparkWidget=document.querySelector('.vl-bottom-widget-spark'),spark=document.getElementById('spark'),dragBound=[...document.querySelectorAll('.vl-bottom-widget>summary')].every(x=>x.dataset.vlDragBound==='1');
+      {const sparkWidget=document.querySelector('.vl-bottom-widget-spark'),spark=document.getElementById('spark'),dragBound=[...document.querySelectorAll('.vl-bottom-widget')].every(x=>x.dataset.vlDragBound==='1');
        const wasOpen=sparkWidget?.open;if(sparkWidget)sparkWidget.open=false;const closedHidden=!!sparkWidget&&!!spark&&getComputedStyle(spark).display==='none';if(sparkWidget)sparkWidget.open=!!wasOpen;
        add('T0e24c SPARK en alle onderste HUD-widgets drag/collapse-ready',dragBound&&closedHidden,'dragBound='+dragBound+' sparkClosedHidden='+closedHidden);}
+      {const defs=VL_CLOCK_RUNNER_DEFS.map(x=>document.getElementById(x.id)),flex=getComputedStyle(document.getElementById('vlClockRunnerGrid')).display==='flex',legacyHidden=document.getElementById('bSpecBenchmarkStop')?.hidden&&document.getElementById('bProxyDecompStop')?.hidden;
+       add('T0e24d runner-flex, één toggle per test en workflow-locks',defs.every(Boolean)&&flex&&legacyHidden&&VLClockWorkflow.unlocked('spec')&&VL_CLOCK_RUNNER_DEFS[1]?.step===2,'defs='+defs.filter(Boolean).length+' flex='+flex+' legacyHidden='+legacyHidden);}
       {const sample={parameters:{tracerCount:0,showTracers:false,showStreamlines:false,showPotentialFlow:false}},proof={tracerCount:0,showTracers:false,showStreamlines:false,showPotentialFlow:false};
        add('T0e25 D14 bewijsveld semantiek',Object.keys(proof).every(k=>sample.parameters[k]===proof[k]),JSON.stringify(proof));}
       {const overlay=document.getElementById('specClockOverlay'),body=document.getElementById('specClockOverlayBody'),oldEnabled=P.specClockEnabled;
