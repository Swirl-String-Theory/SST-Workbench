# Route A Preregistered Derivation Protocol

Status: `[PREREGISTERED TARGET] [NOT DERIVED]`.

The only acceptable next Route-A move is an independent derivation of the SST vacuum vortex-line density.

## Target

```tex
\sigma_{\rm pierce}\Lambda_L = \frac{1}{2L_p^2}.
```

This target may be used for final comparison only.  It may not be used to construct the derivation.

## Forbidden derivation inputs

- `G_N`
- `L_p`
- `t_p`
- `F_gr^max`
- coefficients selected because they improve the numerical match

## Required predeclared model ingredients

- definition of line density `Lambda_L` as vortex length per vacuum volume;
- horizon-piercing stereology and orientation distribution;
- entropy per piercing `sigma_pierce`;
- cutoff/renormalization rule, if any;
- all dimensionless kernels before numerical comparison.

## Pass condition

A future Route-A derivation passes the discipline gate only if it reports `Lambda_L` and `sigma_pierce` first, without using the target, and only then compares to `1/(2L_p^2)`.
