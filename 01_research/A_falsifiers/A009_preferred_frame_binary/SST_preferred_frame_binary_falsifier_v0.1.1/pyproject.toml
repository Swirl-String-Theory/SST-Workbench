[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-preferred-frame-binary-falsifier"
version = "0.1.1"
description = "SST Workbench preferred-frame, dipole-radiation, Euler-mode, and binary-pulsar falsifier pack"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]
