{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CSV logical schema for explicit active kinks",
  "description": "The actual sidecar is CSV. Required columns: comp,s_norm. Optional multiplier is the solver-supplied nonnegative KKT multiplier.",
  "required_columns": ["comp", "s_norm"],
  "optional_columns": ["multiplier"]
}
