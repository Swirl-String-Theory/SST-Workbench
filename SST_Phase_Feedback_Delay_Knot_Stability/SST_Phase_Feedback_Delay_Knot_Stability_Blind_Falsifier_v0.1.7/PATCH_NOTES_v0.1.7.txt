SST Phase-Feedback Delay Knot Stability Blind Falsifier v0.1.7
================================================================

Resolver-wrapper hotfix:
- removes nested _RESOLVE_INPUT.cmd dependency from run_05, run_10, run_all_blind;
- passes --out-file directly in the same batch process;
- run_all_blind resolves only once, inside run_10_prepare_blind.cmd;
- retires _RESOLVE_INPUT.cmd so stale copies cannot silently interfere;
- adds run_06_check_dataset.cmd.

Target-machine evidence before this patch:
- native/Python parity PASS, relative error 0;
- 5 pytest tests PASS;
- resolver selftest PASS;
- 41 production *_i10000.txt files found in the KnotPlot matrix out folder;
- only wrapper invocation failed with missing --out-file.

Scientific kernels and preregistered gates are unchanged.
