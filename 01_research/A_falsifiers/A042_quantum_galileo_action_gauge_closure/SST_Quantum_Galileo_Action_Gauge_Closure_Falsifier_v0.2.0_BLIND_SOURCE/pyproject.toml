[build-system]
requires = ["setuptools>=70", "wheel", "pybind11>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-quantum-galileo-action-gauge-closure-falsifier"
version = "0.2.0"
description = "Blind SST/QGI action-gauge closure falsifier"
requires-python = ">=3.10"
dependencies = [
  "numpy>=2.0,<3",
  "matplotlib>=3.9"
]
