# VortexLab v7.6.23 package

Open `vortexring-lab-v7.6.23.html` in a browser. Keep `ideal_knots_data.js` and `fourier_knots_data.js` beside the HTML file.

Primary new run:

```text
RUN → SST CLOCK · continuum + κ_geom + cross-knot holdouts
```

The run contains 13 scenarios and 35 passive snapshots. Automatic timestamped exports are enabled by default in the CLOCK panel. The benchmark never writes results back to the solver.

See `vortexring-lab-v7.6.23-changelog.md` for the corrected R6/R27 logic, continuum audit and holdout definitions.
