--- /mnt/data/vortexring-lab-v7.6-release-train/releases/v7.6.0/vortexring-lab-v7.6.0.html	2026-07-15 01:44:57.596686641 +0000
+++ /mnt/data/vortexring-lab-v7.6-release-train/releases/v7.6.1/vortexring-lab-v7.6.1.html	2026-07-15 01:44:57.807042084 +0000
@@ -3,11 +3,11 @@
 <head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
-<title>Superfluïde vortexlab v7.6.0 · dual-sidebar UI · ideale knopen · potential flow/Cₚ</title>
-<meta name="vortexlab-version" content="7.6.0">
+<title>Superfluïde vortexlab v7.6.1 · dual-sidebar UI · ideal + fseries knopen · potential flow/Cₚ</title>
+<meta name="vortexlab-version" content="7.6.1">
 <meta name="vortexlab-base" content="7.5.3">
-<meta name="vortexlab-parent" content="7.5.4-zflow-header-paired-docks">
-<meta name="vortexlab-patch" content="sst-vortex-stretching-gate,friedlander-three-profile-benchmark,material-line-strain,topology-preserving-step-guard,auto-relax-rollback,neumann-source-panel-bem,passive-first-hit,taylor-contact,exact-gap-ratios,planck-scale-probe,regression-matrix,v7.5.4-validator,dual-sidebar-ui,ideal-knot-catalog,arclength-resampling,multicomponent-links,potential-flow-cp,tracer-three-stage-reset,tracer-20000">
+<meta name="vortexlab-parent" content="7.6.0">
+<meta name="vortexlab-patch" content="sst-vortex-stretching-gate,friedlander-three-profile-benchmark,material-line-strain,topology-preserving-step-guard,auto-relax-rollback,neumann-source-panel-bem,passive-first-hit,taylor-contact,exact-gap-ratios,planck-scale-probe,regression-matrix,v7.5.4-validator,dual-sidebar-ui,ideal-knot-catalog,fseries-knot-catalog,arclength-resampling,multicomponent-links,potential-flow-cp,tracer-three-stage-reset,tracer-20000">
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.9/katex.min.css">
 <style>
   :root{
@@ -611,7 +611,7 @@
 
       <h2>NIET GEMODELLEERD</h2>
       <p>Reconnecties en lokale GP-reconnectiebruggen (de topology guard stopt vóór doorsnijding) · cilinderwandbeelden/-BEM en vortex-pinning (de Niveau-C BEM geldt uitsluitend rond de gekozen knooptube) · fysische Taylor/Stewartson-continuumdynamica · kern-twist · normale-vloeistof-terugreactie (\(\mathbf v_n\) is opgelegd; geen zelfconsistente twee-vloeistof-/HVBK-dynamica) · kernvolume bij rek · echte roosterdynamica (Tkachenko-modi) · snaardynamica of andere stringtheorie-veldvergelijkingen.</p>
-      <p class="note" style="max-width:820px;">Ingebouwd: ring 0₁, Hopf-link 2₂, ideal trefoil 3₁, figure-eight 4₁, cinquefoil 5₁ en three-twist 5₂. De trefoil gebruikt Brian Gilbert ideal <code>3:1:1</code> (183 Fourier-modi). 4₁ en 5₁ gebruiken gladde analytische parametrisaties; 5₂ gebruikt een Lissajous-representatie. Optioneel: leg <code>ideal_knots_data.js</code> naast dit bestand voor de volledige knopencatalogus.</p>
+      <p class="note" style="max-width:820px;">Ingebouwd: ring 0₁, Hopf-link 2₂, ideal trefoil 3₁, figure-eight 4₁, cinquefoil 5₁ en three-twist 5₂. Leg <code>ideal_knots_data.js</code> naast dit bestand voor Brian Gilberts tight/ideal catalogus en <code>fourier_knots_data.js</code> voor de aanvullende compacte <code>.fseries</code>-curven. De fseries-curven zijn gesloten Fourier-representaties, maar niet automatisch ropelength-minimaliserend.</p>
     </div>
 </div>
 
@@ -656,7 +656,7 @@
           </select>
         </div>
         <button type="button" id="bResetIdealKnot" class="hidden" title="Herstel uitsluitend de geselecteerde ideale catalogusgeometrie">↺ Ideale vorm</button>
-        <div class="note" id="idealKnotInfo">De externe catalogus wordt uit ideal_knots_data.js in dezelfde map geladen. Ideal betekent hier uitsluitend een ideale geometrische beginvoorwaarde; de gekozen filamentdynamica mag de vorm daarna vervormen.</div>
+        <div class="note" id="idealKnotInfo">Externe geometrieën worden uit ideal_knots_data.js (Gilbert tight/ideal) en fourier_knots_data.js (compacte standaardcurven) geladen. De gekozen filamentdynamica mag de beginvorm daarna vervormen.</div>
         <div class="ctrl" id="qualRow"><label>kwaliteit</label>
           <div class="seg" id="qualSeg">
             <button type="button" class="seg-btn active" data-qual="laag">Laag</button>
@@ -1152,7 +1152,7 @@
       </div>
     </details>
     <div class="note" id="footNote">
-      v7.6.0 · basis v7.5.3 · SST vortex-stretching stability gate · Friedlander A₁/A₂/A₃-profielen · passieve first-hit/trials · Taylor-contact binnen iedere kandidaatstap · exact g_a plus effectieve numerieke contactratio · passieve Planck/String-schaalprobe · topology guard zonder numerieke reconnectie · Niveau-C 3D Neumann-BEM/MFS · uitgebreide zelftests T0–T13 · ?selftest=1.
+      v7.6.1 · basis v7.5.3 · SST vortex-stretching stability gate · Friedlander A₁/A₂/A₃-profielen · passieve first-hit/trials · Taylor-contact binnen iedere kandidaatstap · exact g_a plus effectieve numerieke contactratio · passieve Planck/String-schaalprobe · topology guard zonder numerieke reconnectie · Niveau-C 3D Neumann-BEM/MFS · uitgebreide zelftests T0–T13 · ?selftest=1.
     </div>
 </div>
 
@@ -1160,9 +1160,10 @@
 <script src="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.9/katex.min.js"></script>
 <script src="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.9/contrib/auto-render.min.js"></script>
 <script src="./ideal_knots_data.js" onerror="void 0"></script>
+<script src="./fourier_knots_data.js" onerror="void 0"></script>
 <script>
 "use strict";
-const APP_VERSION='7.6.0';
+const APP_VERSION='7.6.1';
 const APP_BASE_VERSION='7.5.3';
 const APP_PATCH_NOTES=[
   'Topologiebehoud: contact-CFL, transient-contact bracketing en landing aan de veilige zijde van de 3a-grens; geen numerieke reconnectie of doorsnijding wordt geaccepteerd',
@@ -1401,7 +1402,7 @@
   return BUILTIN_TOPOLOGIES[P.topo] || BUILTIN_TOPOLOGIES.trefoil;
 }
 function topologyLabel(){
-  if(P.knotKey) return `ideal knoop/link ${P.knotKey}`;
+  if(P.knotKey) return `${P.knotSource==='fseries'?'Fouriercurve':'ideal knoop/link'} ${P.knotKey}`;
   if(P.knotIdx>=0) return `ideal knoop/link ${knotLabel(P.knotIdx)}`;
   return topologyInfo().label;
 }
@@ -1426,7 +1427,7 @@
   // v7.4.2-exclusiviteitsguard is hiermee een type-invariant van de enum).
   solverFrame:'corot', displayFrame:'corot', bgFlow:'none',
   R0:0.07, zA:-0.42, zB:0.42, zSolo:0.0, Rcyl:0.25, Hcyl:0.5,
-  knotIdx:-1, knotKey:'', compA:1, compB:1, idealComponentMode:'all',
+  knotIdx:-1, knotKey:'', knotSource:'builtin', compA:1, compB:1, idealComponentMode:'all',
   ccwA:true, ccwB:false, mirrorB:false, vzA:0, vzB:0, lockVz:true,
   vis:'line', tubeMat:'solid', showCenterline:false,
   revOm:false, revOmBundle:false, revGa:false, revOff:false, revW:false, revVzA:false, revVzB:false,
@@ -2155,12 +2156,19 @@
 }
 function getIdealKnotCatalog(){
   if(typeof IDEAL_KNOT_IDS!=='undefined'&&typeof IDEAL_KNOT_DB!=='undefined')
-    return {ids:IDEAL_KNOT_IDS,db:IDEAL_KNOT_DB};
+    return {ids:IDEAL_KNOT_IDS,db:IDEAL_KNOT_DB,source:'ideal'};
+  return null;
+}
+function getFourierKnotCatalog(){
+  if(typeof FSERIES_KNOT_IDS!=='undefined'&&typeof FSERIES_KNOT_DB!=='undefined')
+    return {ids:FSERIES_KNOT_IDS,db:FSERIES_KNOT_DB,source:'fseries'};
   return null;
 }
 function activeKnotEntry(){
-  const catalog=getIdealKnotCatalog();
-  if(P.knotKey&&catalog?.db?.[P.knotKey])return catalog.db[P.knotKey];
+  if(P.knotKey){
+    const catalog=P.knotSource==='fseries'?getFourierKnotCatalog():getIdealKnotCatalog();
+    if(catalog?.db?.[P.knotKey])return catalog.db[P.knotKey];
+  }
   if(P.knotIdx>=0&&Array.isArray(window.IDEAL_KNOTS))return window.IDEAL_KNOTS[P.knotIdx]||null;
   return null;
 }
@@ -2170,6 +2178,9 @@
   if(Array.isArray(entry.coeffs))return [{coeffs:entry.coeffs,L:entry.L}];
   return [];
 }
+function finiteMetaNumber(v){
+  return v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v))?Number(v):NaN;
+}
 function activeCoeffs(forCarrier){
   const entry=activeKnotEntry();
   const comps=knotEntryComponents(entry);
@@ -4607,13 +4618,20 @@
   if(reset)reset.classList.toggle('hidden',!entry);
   if(!note)return;
   if(!entry){
-    note.textContent=getIdealKnotCatalog()?
-      'Ingebouwde geometrie actief. Kies een catalogusobject voor volledige Fourierdata uit ideal_knots_data.js. Ideal is uitsluitend een geometrische beginvoorwaarde; de filamentdynamica mag de vorm daarna vervormen.':
-      'ideal_knots_data.js is niet geladen; de ingebouwde topologieën blijven beschikbaar.';
+    const ni=getIdealKnotCatalog()?.ids?.length||0,nf=getFourierKnotCatalog()?.ids?.length||0;
+    note.textContent=`Ingebouwde geometrie actief. Externe catalogi: ${ni} Gilbert ideal/tight objecten en ${nf} compacte fseries-curven geladen. Fseries is een geometrische Fourierbron, niet automatisch een ideal knot.`;
     return;
   }
-  const comps=knotEntryComponents(entry),modes=comps.map(c=>(c.coeffs||[]).length),L=Number(entry.L);
-  note.textContent=`Ideal ID: ${entry.knotId||P.knotKey||knotLabel(P.knotIdx)} · Conway: ${entry.conway||'—'} · componenten: ${comps.length} · cataloguslengte L: ${Number.isFinite(L)?L.toFixed(6):'niet opgegeven'} · Fouriertermen: ${modes.join(' + ')} · modus: ${P.idealComponentMode==='all'?'volledig object':'één component'} · bron: ideal_knots_data.js. Ideal betekent hier een geometrische beginvoorwaarde, niet een bewezen stationaire Euler-, GP/NLSE- of SST-oplossing.`;
+  const comps=knotEntryComponents(entry);
+  const modes=comps.map(c=>{const q=c.coeffs||[];return `${q.length} termen / I_max ${q.reduce((m,v)=>Math.max(m,Math.abs(Number(v.I)||0)),0)}`;});
+  const L0=finiteMetaNumber(entry.L),componentLengths=comps.map(c=>finiteMetaNumber(c.L)).filter(Number.isFinite);
+  const L=Number.isFinite(L0)?L0:(componentLengths.length===comps.length?componentLengths.reduce((a,b)=>a+b,0):NaN);
+  const isFs=P.knotSource==='fseries'||entry.ideal===false;
+  const source=isFs?'fourier_knots_data.js (.fseries)':'ideal_knots_data.js (Brian Gilbert)';
+  const status=isFs?'compacte gesloten Fouriercurve; niet als tight/ideal gecertificeerd':'tight/ideal brongeometrie; nog geen stationaire Euler-, GP/NLSE- of SST-oplossing';
+  const indexInfo=isFs&&Number.isInteger(entry.harmonicStart)?` · harmonische eerste regel j=${entry.harmonicStart}`:'';
+  const warning=entry.warning?` · WAARSCHUWING bron: ${entry.warning}`:'';
+  note.textContent=`${isFs?'Fseries':'Ideal'} ID: ${entry.knotId||P.knotKey||knotLabel(P.knotIdx)} · Conway: ${entry.conway||'—'} · componenten: ${comps.length} · bronlengte L: ${Number.isFinite(L)?L.toFixed(6):'niet opgegeven'} · Fourier: ${modes.join(' + ')}${indexInfo} · modus: ${P.idealComponentMode==='all'?'volledig object':'één component'} · bron: ${source} · status: ${status}${warning}.`;
 }
 function syncCompSelects(){
   const entry=activeKnotEntry(),comps=knotEntryComponents(entry);
@@ -5249,7 +5267,7 @@
   if(v==='solo'&&P.topo==='ring'&&P.knotIdx<0&&!P.knotKey)P.topo='trefoil';
   syncUi();resetState();
 });
-document.getElementById('topoSelect').addEventListener('change',e=>{const v=e.target.value;if(v===P.topo&&!P.knotKey&&P.knotIdx<0)return;P.topo=v;P.knotIdx=-1;P.knotKey='';P.idealComponentMode='all';syncKnotSel();syncUi();resetState();});
+document.getElementById('topoSelect').addEventListener('change',e=>{const v=e.target.value;if(v===P.topo&&!P.knotKey&&P.knotIdx<0)return;P.topo=v;P.knotIdx=-1;P.knotKey='';P.knotSource='builtin';P.idealComponentMode='all';syncKnotSel();syncUi();resetState();});
 segHandler('interSeg','inter',v=>{if(v===P.inter)return;P.inter=v;syncUi();resetState();});
 segHandler('coreSeg','core',v=>{if(v===P.core)return;P.core=v;syncUi();updateSubtitle();});
 segHandler('qualSeg','qual',v=>{if(v===P.qual)return;P.qual=v;resetState();});
@@ -5481,25 +5499,43 @@
 function syncKnotSel(){
   const sel=document.getElementById('knotSelect');
   if(!sel)return;
-  if(P.knotKey)sel.value=P.knotKey;
-  else if(P.knotIdx>=0)sel.value=String(P.knotIdx);
-  else sel.value=sel.querySelector('option[value=""]')?'':'-1';
+  if(P.knotKey)sel.value=`${P.knotSource||'ideal'}|${P.knotKey}`;
+  else if(P.knotIdx>=0)sel.value=`legacy|${P.knotIdx}`;
+  else sel.value='builtin|';
 }
 (function initKnots(){
   const row=document.getElementById('knotRow'),sel=document.getElementById('knotSelect');if(!row||!sel)return;row.classList.remove('hidden');
-  const catalog=getIdealKnotCatalog();
-  if(catalog){
-    const singles=[],links=[];for(const id of catalog.ids){const k=catalog.db[id],nc=knotEntryComponents(k).length;const label=`${id}${k.conway?' · Conway '+k.conway:''}${Number.isFinite(Number(k.L))?' · L='+Number(k.L).toFixed(4):''}${nc>1?' · '+nc+' comp':''}`;(nc>1?links:singles).push(`<option value="${id}">${label}</option>`);}
-    sel.innerHTML='<optgroup label="INGEBOUWD"><option value="">3:1:1 trefoil (ingebouwd)</option></optgroup>'+
-      `<optgroup label="IDEALE KNOPEN">${singles.join('')}</optgroup><optgroup label="IDEALE LINKS">${links.join('')}</optgroup>`;
-  }else if(Array.isArray(window.IDEAL_KNOTS)&&window.IDEAL_KNOTS.length){
-    sel.innerHTML='<optgroup label="INGEBOUWD"><option value="-1">3:1:1 trefoil (ingebouwd)</option></optgroup><optgroup label="IDEALE CATALOGUS">'+window.IDEAL_KNOTS.map((e,i)=>`<option value="${i}">${knotLabel(i)}</option>`).join('')+'</optgroup>';
-  }else{
-    sel.innerHTML='<option value="">3:1:1 trefoil (ingebouwd)</option><option value="" disabled>— externe ideal_knots_data.js niet geladen —</option>';
-  }
+  const ideal=getIdealKnotCatalog(),fseries=getFourierKnotCatalog();
+  const groups=['<optgroup label="INGEBOUWD"><option value="builtin|">3:1:1 trefoil (ingebouwd)</option></optgroup>'];
+  if(ideal){
+    const singles=[],links=[];
+    for(const id of ideal.ids){
+      const k=ideal.db[id],nc=knotEntryComponents(k).length;
+      const componentLengths=knotEntryComponents(k).map(c=>finiteMetaNumber(c.L)).filter(Number.isFinite);
+      const componentL=nc>1&&componentLengths.length===nc?componentLengths.reduce((a,b)=>a+b,0):NaN;
+      const L0=finiteMetaNumber(k.L),L=Number.isFinite(L0)?L0:componentL;
+      const label=`${id}${k.conway?' · Conway '+k.conway:''}${Number.isFinite(L)?' · L='+L.toFixed(4):''}${nc>1?' · '+nc+' comp':''}`;
+      (nc>1?links:singles).push(`<option value="ideal|${id}">${label}</option>`);
+    }
+    groups.push(`<optgroup label="GILBERT IDEAL / TIGHT KNOPEN">${singles.join('')}</optgroup>`);
+    groups.push(`<optgroup label="GILBERT IDEAL / TIGHT LINKS">${links.join('')}</optgroup>`);
+  }
+  if(fseries){
+    const opts=fseries.ids.map(id=>{const k=fseries.db[id],warn=k.warning?' ⚠':'';return `<option value="fseries|${id}">${id} · ${k.components?.[0]?.coeffs?.length||0} harmonischen${warn}</option>`;});
+    groups.push(`<optgroup label="COMPACTE FSERIES — NIET AUTOMATISCH IDEAL">${opts.join('')}</optgroup>`);
+  }
+  if(!ideal&&!fseries&&Array.isArray(window.IDEAL_KNOTS)&&window.IDEAL_KNOTS.length){
+    groups.push('<optgroup label="LEGACY IDEALE CATALOGUS">'+window.IDEAL_KNOTS.map((e,i)=>`<option value="legacy|${i}">${knotLabel(i)}</option>`).join('')+'</optgroup>');
+  }
+  if(!ideal)groups.push('<optgroup label="CATALOGUSSTATUS"><option disabled>ideal_knots_data.js niet geladen</option></optgroup>');
+  if(!fseries)groups.push('<optgroup label="CATALOGUSSTATUS"><option disabled>fourier_knots_data.js niet geladen</option></optgroup>');
+  sel.innerHTML=groups.join('');
   sel.addEventListener('change',()=>{
-    const catalogNow=getIdealKnotCatalog();
-    if(catalogNow){P.knotKey=sel.value;P.knotIdx=-1;}else{const idx=parseInt(sel.value,10);P.knotIdx=Number.isInteger(idx)?idx:-1;P.knotKey='';}
+    const sep=sel.value.indexOf('|'),source=sep>=0?sel.value.slice(0,sep):'builtin',key=sep>=0?sel.value.slice(sep+1):'';
+    P.knotIdx=-1;P.knotKey='';P.knotSource='builtin';
+    if(source==='ideal'&&getIdealKnotCatalog()?.db?.[key]){P.knotSource='ideal';P.knotKey=key;}
+    else if(source==='fseries'&&getFourierKnotCatalog()?.db?.[key]){P.knotSource='fseries';P.knotKey=key;}
+    else if(source==='legacy'){const idx=parseInt(key,10);P.knotIdx=Number.isInteger(idx)?idx:-1;P.knotSource='legacy';}
     P.topo='trefoil';P.idealComponentMode='all';P.compA=1;P.compB=1;document.getElementById('topoSelect').value='trefoil';
     syncCompSelects();syncUi();updateSubtitle();resetState();
   });
@@ -5527,11 +5563,14 @@
   console.log('[Topology] ingebouwde catalogus OK');
 })();
 (function idealCatalogSanity(){
-  const catalog=getIdealKnotCatalog();if(!catalog)return;
-  console.assert(new Set(catalog.ids).size===catalog.ids.length,'ideal catalogus: unieke IDs');
-  console.assert(catalog.ids.every(id=>catalog.db[id]&&knotEntryComponents(catalog.db[id]).every(c=>(c.coeffs||[]).every(q=>q.A?.length===3&&q.B?.length===3&&q.A.concat(q.B).every(Number.isFinite)))),'ideal catalogus: eindige 3D Fouriercoëfficiënten');
-  const tre=catalog.db['3:1:1'];if(tre){const raw=sampleIdealComponent(knotEntryComponents(tre)[0],512);let L=0;for(let i=0;i<512;i++){const j=(i+1)%512;L+=Math.hypot(raw[3*j]-raw[3*i],raw[3*j+1]-raw[3*i+1],raw[3*j+2]-raw[3*i+2]);}console.assert(Math.abs(L-16.371637)<0.02,'ideal 3:1:1 booglengte',L);}
-  console.log('[Ideal knots] catalogusadapter OK',catalog.ids.length);
+  const check=(catalog,label)=>{
+    if(!catalog)return;
+    console.assert(new Set(catalog.ids).size===catalog.ids.length,`${label}: unieke IDs`);
+    console.assert(catalog.ids.every(id=>catalog.db[id]&&knotEntryComponents(catalog.db[id]).every(c=>(c.coeffs||[]).every(q=>Number.isFinite(Number(q.I))&&q.A?.length===3&&q.B?.length===3&&q.A.concat(q.B).every(Number.isFinite)))),`${label}: eindige 3D Fouriercoëfficiënten`);
+    console.log(`[${label}] catalogusadapter OK`,catalog.ids.length);
+  };
+  const ideal=getIdealKnotCatalog();check(ideal,'Ideal knots');check(getFourierKnotCatalog(),'Fseries knots');
+  const tre=ideal?.db?.['3:1:1'];if(tre){const raw=sampleIdealComponent(knotEntryComponents(tre)[0],512);let L=0;for(let i=0;i<512;i++){const j=(i+1)%512;L+=Math.hypot(raw[3*j]-raw[3*i],raw[3*j+1]-raw[3*i+1],raw[3*j+2]-raw[3*i+2]);}console.assert(Math.abs(L-16.371637)<0.02,'ideal 3:1:1 booglengte',L);}
 })();
 (function potentialFlowSanity(){
   const R=1,U=2,stag=potentialFields(0,R*(1+1e-9),R,U),shoulder=potentialFields(R*(1+1e-9),0,R,U),far=potentialFields(0,100*R,R,U);
