[project]
name = "sst-maxwell-dynamical-field-closure-falsifier"
version = "0.1.0"
description = "Target-blind falsifier for SST transverse-mode, displacement-current, and gravitational energy-deficit closures"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
