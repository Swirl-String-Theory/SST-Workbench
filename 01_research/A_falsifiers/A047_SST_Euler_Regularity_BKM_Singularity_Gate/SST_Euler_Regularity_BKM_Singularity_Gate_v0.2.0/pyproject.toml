[build-system]
requires = ["setuptools>=70", "wheel", "pybind11>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-euler-regularity-bkm-gate"
version = "0.2.0"
description = "Blind PKLSA-driven Euler regularity / BKM singularity falsification gate"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0", "pybind11>=3.0"]
