[build-system]
requires = ["setuptools>=70", "wheel", "pybind11>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sst-euler-regularity-bkm-gate"
version = "0.1.0"
description = "Blind theorem-driven Euler regularity / BKM singularity gate for SST trefoil seeds"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0", "pybind11>=3.0"]
