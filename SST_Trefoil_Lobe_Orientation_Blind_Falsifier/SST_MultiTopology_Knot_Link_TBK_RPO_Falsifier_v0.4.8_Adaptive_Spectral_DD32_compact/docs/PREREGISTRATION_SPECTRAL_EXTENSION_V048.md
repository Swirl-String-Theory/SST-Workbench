# Preregistration — v0.4.8 adaptive spectral extension

Frozen before result inspection for the v0.4.8 extension campaign:

- archive: all 127 bundled inputs;
- N=720 fixed;
- k_max ladder: 16 baseline -> 24 -> 32 -> 48 -> 64;
- 24 and 32 mandatory; 48/64 adaptive only for unresolved datasets;
- epsilon convergence set: 0.001, 0.002, 0.004, 0.008;
- epsilon reference: 0.004;
- P2 threshold: normalized growth <= 0.12;
- contraction ratio <= 0.75;
- last relative growth change <= 0.03;
- exact k_max eigenvector weight <= 0.10;
- high-quarter Kelvin-tail fraction <= 0.05 when Kelvin-family weight >= 0.05;
- spectral Nyquist fraction <= 0.75 on the least-sampled component;
- P2 verdict must be stable across the active three-point tail;
- power-tail fit is diagnostic only and requires measured-tail support;
- threshold-overlap under the uncertainty proxy prevents convergence;
- unresolved at k=64 remains explicitly unresolved.

No nonlinear ringdown/RPO/Floquet result is generated by these extension rungs. Those remain separate confirmatory dynamics tests after spectral closure.
