# Changelog

## v0.2.0

- recursive `campaigns/` discovery;
- zero-argument `run_all.cmd` and `run_scan.cmd`;
- nested-manifest merge and raw-file auto-detection;
- spectrum NPZ support;
- VortexLab diagnostic log parser;
- diagnostic-only quarantine when no spatial `k` information exists;
- real excerpt from prior VortexLab session included for importer/QC testing;
- prior Library VortexLab source index added;
- demo/synthetic inputs excluded from normal blind scans;
- new campaign-scanner tests and recursive blind regression test.
