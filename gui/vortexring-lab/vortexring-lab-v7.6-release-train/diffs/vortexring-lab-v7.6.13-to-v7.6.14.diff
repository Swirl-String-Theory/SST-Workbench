--- vortexring-lab-v7.6.13.html
+++ vortexring-lab-v7.6.14.html
@@ -3,11 +3,11 @@
 <head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
-<title>Superfluïde vortexlab v7.6.13 · dual-sidebar UI · ideal + fseries knopen · potential flow/Cₚ</title>
-<meta name="vortexlab-version" content="7.6.13">
+<title>Superfluïde vortexlab v7.6.14 · independent four-panel UI · ideal + fseries knopen · potential flow/Cₚ</title>
+<meta name="vortexlab-version" content="7.6.14">
 <meta name="vortexlab-base" content="7.5.3">
-<meta name="vortexlab-parent" content="7.6.11">
-<meta name="vortexlab-patch" content="sst-vortex-stretching-gate,friedlander-three-profile-benchmark,material-line-strain,topology-preserving-step-guard,auto-relax-rollback,neumann-source-panel-bem,passive-first-hit,taylor-contact,exact-gap-ratios,planck-scale-probe,regression-matrix,v7.5.4-validator,dual-sidebar-ui,ideal-knot-catalog,fseries-knot-catalog,arclength-resampling,multicomponent-links,potential-flow-cp,tracer-three-stage-reset,tracer-20000,flag-below-topbar,bottom-simulator-overlays,flat-geometric-diagnostics,sst-diagnostics-siblings,speculative-two-knot-swirl-clock,two-route-clock-diagnostic,adjustable-initial-knot-separation,distance-sweep-control,spec-clock-quick-preset,spec-clock-quick-controls,spec-clock-overlay,model-log-text-export,model-log-tdz-hotfix,unmapped-proxy-semantics,stable-incremental-spec-overlay,free-distance-number-input,focus-safe-control-sync,bound-spec-clock-controls,far-distance-periodic-z-guard,phase-null-isolated-subtraction,single-reference-calibration-lock,stable-log-clock-bracket,differential-lag-accumulator,external-drift-cfl-bound,accepted-step-time-cap,playback-debt-reset,spec-clock-calibrate-autostart,spec-clock-safe-speed-preset">
+<meta name="vortexlab-parent" content="7.6.13">
+<meta name="vortexlab-patch" content="sst-vortex-stretching-gate,friedlander-three-profile-benchmark,material-line-strain,topology-preserving-step-guard,auto-relax-rollback,neumann-source-panel-bem,passive-first-hit,taylor-contact,exact-gap-ratios,planck-scale-probe,regression-matrix,v7.5.4-validator,dual-sidebar-ui,ideal-knot-catalog,fseries-knot-catalog,arclength-resampling,multicomponent-links,potential-flow-cp,tracer-three-stage-reset,tracer-20000,flag-below-topbar,bottom-simulator-overlays,flat-geometric-diagnostics,sst-diagnostics-siblings,speculative-two-knot-swirl-clock,two-route-clock-diagnostic,adjustable-initial-knot-separation,distance-sweep-control,spec-clock-quick-preset,spec-clock-quick-controls,spec-clock-overlay,model-log-text-export,model-log-tdz-hotfix,unmapped-proxy-semantics,stable-incremental-spec-overlay,free-distance-number-input,focus-safe-control-sync,bound-spec-clock-controls,far-distance-periodic-z-guard,phase-null-isolated-subtraction,single-reference-calibration-lock,stable-log-clock-bracket,differential-lag-accumulator,external-drift-cfl-bound,accepted-step-time-cap,playback-debt-reset,spec-clock-calibrate-autostart,spec-clock-safe-speed-preset,independent-four-panel-docks,run-header-dropdown,independent-panel-persistence,dual-overlay-offsets">
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.9/katex.min.css">
 <style>
   :root{
@@ -351,15 +351,18 @@
   .vl-speed-range{width:112px;accent-color:var(--vl-active);}
   .vl-speed-value{min-width:48px;color:var(--vl-text);font-family:"IBM Plex Mono",Consolas,monospace;
     font-size:10px;text-align:right;font-variant-numeric:tabular-nums;}
-  .vl-header-view-btn[aria-expanded="true"]{border-color:var(--vl-active);background:rgba(85,214,255,.12);}
+  .vl-header-menu-btn[aria-expanded="true"],.vl-header-view-btn[aria-expanded="true"]{border-color:var(--vl-active);background:rgba(85,214,255,.12);}
   .vl-header-dropdown{position:fixed;top:calc(var(--vl-topbar-h) + 6px);right:calc(var(--vl-rail-w) + 8px);
     z-index:92;width:clamp(350px,28vw,470px);max-height:calc(100dvh - var(--vl-topbar-h) - 16px);
     overflow-y:auto;overflow-x:hidden;padding:10px;border:1px solid var(--vl-border);border-radius:10px;
     background:rgba(13,21,41,.985);box-shadow:0 18px 48px rgba(0,0,0,.42);display:none;pointer-events:auto;}
   .vl-header-dropdown.open{display:block;}
-  .vl-header-dropdown>#collVis{display:block!important;margin:0!important;border:0!important;background:transparent!important;}
-  .vl-header-dropdown>#collVis>summary{display:none!important;}
-  .vl-header-dropdown>#collVis>.coll-body{padding:0!important;}
+  .vl-header-dropdown>details{display:block!important;margin:0!important;border:0!important;background:transparent!important;}
+  .vl-header-dropdown>details>summary{display:none!important;}
+  .vl-header-dropdown>details>.coll-body{padding:0!important;}
+  .vl-header-dropdown.vl-run-menu{width:clamp(360px,31vw,500px);}
+  .vl-header-dropdown.vl-run-menu .run-row{grid-template-columns:1fr;}
+  .vl-header-dropdown.vl-run-menu #bPause,.vl-header-dropdown.vl-run-menu #presetSelect{display:none!important;}
   #vl-left-flow #subCylinder{margin:0 0 7px;border-color:rgba(85,214,255,.34);background:rgba(85,214,255,.035);}
   #vl-left-flow #subCylinder>summary{min-height:32px;padding:6px 9px;font-size:9px;}
   .potential-flow-readout{margin-top:7px;padding:7px 8px;border:1px solid var(--vl-border);border-radius:8px;
@@ -379,7 +382,7 @@
   }
   .vl-sim-bottom-overlays{
     position:fixed;left:var(--vl-overlay-left,calc(var(--vl-rail-w) + var(--vl-panel-w) + 12px));
-    right:12px;bottom:10px;z-index:58;display:flex;align-items:flex-end;gap:8px;
+    right:var(--vl-overlay-right,12px);bottom:10px;z-index:58;display:flex;align-items:flex-end;gap:8px;
     min-width:0;pointer-events:none;transition:left .18s ease;
   }
   .vl-sim-bottom-overlays>*{pointer-events:auto;min-width:0;}
@@ -462,10 +465,10 @@
     body.vl-dual-ui #flag{max-width:calc(100vw - 20px);}
   }
 
-  .vl-dock{position:fixed;top:var(--vl-topbar-h);bottom:0;width:calc(var(--vl-rail-w) + var(--vl-panel-w));pointer-events:none;z-index:65;}
+  .vl-dock{position:fixed;top:var(--vl-topbar-h);bottom:0;width:calc(var(--vl-rail-w) + var(--vl-panel-w) + var(--vl-panel-w));pointer-events:none;z-index:65;}
   .vl-dock-left{left:0;}.vl-dock-right{right:0;}
   .vl-dock-rail{position:absolute;top:0;bottom:0;width:var(--vl-rail-w);display:flex;flex-direction:column;
-    background:rgba(12,20,38,.97);border-color:var(--vl-border);pointer-events:auto;overflow-y:auto;scrollbar-width:none;}
+    background:rgba(12,20,38,.97);border-color:var(--vl-border);pointer-events:auto;overflow-y:auto;scrollbar-width:none;z-index:5;}
   .vl-dock-rail::-webkit-scrollbar{display:none;}
   .vl-dock-left .vl-dock-rail{left:0;border-right:1px solid var(--vl-border);}
   .vl-dock-right .vl-dock-rail{right:0;border-left:1px solid var(--vl-border);}
@@ -476,14 +479,17 @@
   .vl-dock-tab:hover,.vl-dock-tab:focus-visible{color:#EEF7FF;background:rgba(85,214,255,.07);outline:none;}
   .vl-dock-tab.active{color:#EEF7FF;background:rgba(85,214,255,.13);box-shadow:inset 3px 0 0 var(--vl-active);}
   .vl-dock-right .vl-dock-tab.active{box-shadow:inset -3px 0 0 var(--vl-active);}
-  .vl-dock-panel{position:absolute;top:0;bottom:0;width:var(--vl-panel-w);background:var(--vl-surface);
-    border-color:var(--vl-border);box-shadow:0 14px 40px rgba(0,0,0,.36);pointer-events:auto;
-    transition:transform .18s ease,visibility .18s ease;visibility:hidden;overflow:hidden;}
-  .vl-dock-left .vl-dock-panel{left:var(--vl-rail-w);border-right:1px solid var(--vl-border);transform:translateX(calc(-100% - var(--vl-rail-w)));}
-  .vl-dock-right .vl-dock-panel{right:var(--vl-rail-w);border-left:1px solid var(--vl-border);transform:translateX(calc(100% + var(--vl-rail-w)));}
-  .vl-dock.is-open .vl-dock-panel{transform:translateX(0);visibility:visible;}
-  .vl-pane{position:absolute;inset:0;display:none;min-width:0;}
-  .vl-pane.active{display:flex;flex-direction:column;}
+  .vl-dock-panel{position:absolute;top:0;bottom:0;width:calc(var(--vl-panel-w) + var(--vl-panel-w));pointer-events:none;overflow:visible;}
+  .vl-dock-left .vl-dock-panel{left:var(--vl-rail-w);}
+  .vl-dock-right .vl-dock-panel{right:var(--vl-rail-w);}
+  .vl-pane{position:absolute;top:0;bottom:0;width:var(--vl-panel-w);min-width:0;display:flex;flex-direction:column;
+    background:var(--vl-surface);border-color:var(--vl-border);box-shadow:0 14px 40px rgba(0,0,0,.36);
+    pointer-events:none;visibility:hidden;opacity:0;overflow:hidden;z-index:1;
+    transition:transform .18s ease,left .18s ease,right .18s ease,opacity .12s ease,visibility .18s ease;}
+  .vl-dock-left .vl-pane{left:0;border-right:1px solid var(--vl-border);transform:translateX(-100%);}
+  .vl-dock-right .vl-pane{right:0;border-left:1px solid var(--vl-border);transform:translateX(100%);}
+  .vl-pane.vl-pane-open{pointer-events:auto;visibility:visible;opacity:1;transform:translateX(0);}
+  .vl-pane[data-slot="1"]{z-index:2;}
   .vl-pane-header{height:46px;flex:0 0 46px;display:flex;align-items:center;gap:8px;padding:0 9px 0 12px;
     border-bottom:1px solid var(--vl-border);background:var(--vl-surface-2);position:relative;z-index:4;}
   .vl-pane-header h2{font-size:11px;letter-spacing:.12em;color:#F0F6FC;margin:0;flex:1;}
@@ -543,7 +549,7 @@
     .vl-topbar-title{display:none;}
   }
   @media (max-width:820px){
-    :root{--vl-rail-w:40px;--vl-panel-w:calc(100vw - var(--vl-rail-w));}
+    :root{--vl-rail-w:40px;--vl-panel-w:min(340px,76vw);}
     .vl-topbar{justify-content:flex-start;overflow-x:auto;overflow-y:hidden;scrollbar-width:none;}
     .vl-header-dropdown{right:8px;width:min(440px,calc(100vw - 16px));}
     .vl-topbar::-webkit-scrollbar{display:none;}
@@ -715,6 +721,7 @@
       <p>Omdat de relatieve oriëntatie tussen de canonieke interne swirl en deze externe bijdrage niet is afgeleid, wordt geen enkelvoudige totale snelheid geclaimd. In plaats daarvan toont de diagnose de formele bracket \(v_i\in[|v_{\circlearrowleft}-u_i|,v_{\circlearrowleft}+u_i]\) en projecteert die via de SST-klokansatz \(d\tau_i/dt=\sqrt{1-v_i^2/c^2}\). Dit mengt de canonieke horn-snelheid met een meterschaal-filamentdiagnose en is daarom een ongemapte Research-Track-diagnose, geen canonieke superpositieregel.</p>
       <p>Vanaf v7.6.11 gebruikt de tweede route geen kale verhouding van twee globale body-frequenties meer. Op exact dezelfde actuele geometrie worden per drager twee snelheidsvelden geëvalueerd: het volledige veld en een geïsoleerd veld met alleen de eigen drager plus dezelfde achtergrondvoorwaarden. De directe wederzijdse fase-nullproxy is \(\delta_i=(\Omega_i^{\rm full}-\Omega_i^{\rm iso})/|\Omega_i^{\rm iso}|\). Eén verre kalibratie legt \(\delta_{i,0}\) vast, waarna \(\eta_i^{\rm phase-null}=1+\delta_i-\delta_{i,0}\). De referentie wordt daarna vergrendeld tot een geometrische reset. Dit onderdrukt gemeenschappelijke achtergrondrotatie en zelfgeïnduceerde vormdrift, maar maakt de proxy nog niet canoniek: een SST-interne fase-observable en overdrachtswet ontbreken nog steeds. De veldbracket wordt bovendien in \(\ln\eta\)-ruimte berekend en de lag wordt differentieel geïntegreerd, zodat sub-ulp verschillen nabij één niet door floating-point-cancellatie verdwijnen.</p>
       <p><b>v7.6.13 integrator- en benchmarkbootstrap.</b> Voorgeschreven axiale drift begrenst de CFL-stap vanaf het eerste frame. Daarnaast geldt een playback-onafhankelijke bovengrens van 0.05 s zolang SPEC CLOCK actief is. De preset en iedere geometrische Reset zetten de klokrun op 1× en pauzeren bij \(t=0\); de eerste fase-nullkalibratie start de sweep automatisch. Een wijziging van snelheid, pauzestatus of drift wist alleen onafgewerkt playback-debet en kan daardoor geen achterstallige reuzenstap meer vrijgeven.</p>
+      <p><b>v7.6.14 onafhankelijke vierpaneel-layout.</b> INFO, FLOW, KERN en DIAG kunnen nu ieder afzonderlijk worden geopend en gesloten. FLOW en KERN bezetten de binnenste posities richting canvas; zodra INFO of DIAG erbij wordt geopend, schuiven die respectievelijk buiten FLOW en buiten KERN. Daardoor zijn nul tot vier panelen tegelijk mogelijk. SCENARIO &amp; RUN staat als afzonderlijke RUN-dropdown in de header naast VIEW.</p>
       <h2>WEDERZIJDSE WRIJVING (T &gt; 0) <span class="tag">v7</span></h2>
       <p>De knooppuntsnelheid volgt de Schwarz-vergelijking</p>
       <p>\[ \dot{\mathbf s} \;=\; \mathbf v_s \;+\; \alpha\,\hat{\mathbf s}'\times(\mathbf v_n-\mathbf v_s)\;-\;\alpha'\,\hat{\mathbf s}'\times\big[\hat{\mathbf s}'\times(\mathbf v_n-\mathbf v_s)\big], \]</p>
@@ -1369,7 +1376,7 @@
       </div>
     </details>
     <div class="note" id="footNote">
-      v7.6.13 · basis v7.5.3 · SST vortex-stretching stability gate · Friedlander A₁/A₂/A₃-profielen · passieve first-hit/trials · Taylor-contact binnen iedere kandidaatstap · exact g_a plus effectieve numerieke contactratio · passieve Planck/String-schaalprobe · topology guard zonder numerieke reconnectie · Niveau-C 3D Neumann-BEM/MFS · fase-null geïsoleerde aftrek · enkelvoudig vergrendelde referentie · eerste-stap drift-CFL · 50 ms SPEC CLOCK-tijdcap · geautomatiseerde 10-run SPEC CLOCK-benchmark · Rcyl/Hcyl-logging · uitgebreide zelftests T0–T13 · ?selftest=1.
+      v7.6.14 · basis v7.5.3 · onafhankelijke INFO/FLOW/KERN/DIAG-panelen · RUN-dropdown in header · SST vortex-stretching stability gate · Friedlander A₁/A₂/A₃-profielen · passieve first-hit/trials · Taylor-contact binnen iedere kandidaatstap · exact g_a plus effectieve numerieke contactratio · passieve Planck/String-schaalprobe · topology guard zonder numerieke reconnectie · Niveau-C 3D Neumann-BEM/MFS · fase-null geïsoleerde aftrek · enkelvoudig vergrendelde referentie · eerste-stap drift-CFL · 50 ms SPEC CLOCK-tijdcap · geautomatiseerde 10-run SPEC CLOCK-benchmark · Rcyl/Hcyl-logging · uitgebreide zelftests T0–T13 · ?selftest=1.
     </div>
 </div>
 
@@ -1380,7 +1387,7 @@
 <script src="./fourier_knots_data.js" onerror="void 0"></script>
 <script>
 "use strict";
-const APP_VERSION='7.6.13';
+const APP_VERSION='7.6.14';
 const APP_BASE_VERSION='7.5.3';
 window.__vlBootComplete=false;
 window.addEventListener('error',function vlEarlyBootstrapError(event){
@@ -5897,18 +5904,27 @@
   const note=vlNode('accCapacityNote');group.title=note?.textContent.trim()||'Afspeeltempo; verandert de volledige CFL-stappen niet.';
   group.append(lab,range,value);host.appendChild(group);source.closest('.ctrl')?.classList.add('vl-proxy-source');sync();return range;
 }
-function vlCreateHeaderDropdown(buttonLabel,node,shell){
+function vlCreateHeaderDropdown(buttonLabel,node,shell,key='view',ariaLabel=null){
   if(!node||!shell)return null;
-  const button=document.createElement('button');button.type='button';button.className='vl-header-view-btn';
-  button.textContent=buttonLabel+' ▾';button.setAttribute('aria-expanded','false');button.setAttribute('aria-controls','vlHeaderViewMenu');
-  button.title='Open weergave-instellingen';
-  const menu=document.createElement('section');menu.className='vl-header-dropdown';menu.id='vlHeaderViewMenu';menu.setAttribute('aria-label','Weergave-instellingen');
+  const safeKey=String(key||buttonLabel).toLowerCase().replace(/[^a-z0-9_-]+/g,'-');
+  const menuId='vlHeader'+safeKey[0].toUpperCase()+safeKey.slice(1)+'Menu';
+  const button=document.createElement('button');button.type='button';button.className='vl-header-menu-btn vl-header-view-btn';
+  button.textContent=buttonLabel+' ▾';button.setAttribute('aria-expanded','false');button.setAttribute('aria-controls',menuId);
+  button.title='Open '+buttonLabel.toLowerCase()+'-instellingen';
+  const menu=document.createElement('section');menu.className='vl-header-dropdown vl-'+safeKey+'-menu';menu.id=menuId;menu.setAttribute('aria-label',ariaLabel||buttonLabel+'-instellingen');
   if(node.tagName==='DETAILS')node.open=true;menu.appendChild(node);shell.appendChild(menu);
-  const setOpen=value=>{const open=!!value;menu.classList.toggle('open',open);button.setAttribute('aria-expanded',open?'true':'false');};
+  const registry=window.__vlHeaderDropdowns||(window.__vlHeaderDropdowns=new Set());
+  const api={button,menu,setOpen:null};registry.add(api);
+  const setOpen=value=>{
+    const open=!!value;
+    if(open)registry.forEach(other=>{if(other!==api)other.setOpen?.(false);});
+    menu.classList.toggle('open',open);button.setAttribute('aria-expanded',open?'true':'false');
+  };
+  api.setOpen=setOpen;
   button.addEventListener('click',e=>{e.stopPropagation();setOpen(!menu.classList.contains('open'));});
   document.addEventListener('pointerdown',e=>{if(menu.classList.contains('open')&&!menu.contains(e.target)&&e.target!==button)setOpen(false);});
   document.addEventListener('keydown',e=>{if(e.key==='Escape')setOpen(false);});
-  return {button,menu,setOpen};
+  return api;
 }
 
 function vlMakePane(id,title,subtitle,nodes){
@@ -5921,43 +5937,68 @@
   nodes.filter(Boolean).forEach(node=>scroll.appendChild(node));
   header.append(h,sub,close);pane.append(header,scroll);return {pane,close};
 }
-function vlCreateDock(side,specs,defaultKey,onActivate=null){
+function vlCreateIndependentDock(side,specs,defaultOpenKeys=[],outerToInnerOrder=null){
   const dock=document.createElement('div');dock.className='vl-dock vl-dock-'+side;dock.id='vlDock'+side[0].toUpperCase()+side.slice(1);
-  const rail=document.createElement('nav');rail.className='vl-dock-rail';rail.setAttribute('role','tablist');rail.setAttribute('aria-label',side==='left'?'Linker simulatorpanelen':'Rechter simulatorpanelen');
+  const rail=document.createElement('nav');rail.className='vl-dock-rail';rail.setAttribute('role','group');rail.setAttribute('aria-label',side==='left'?'Onafhankelijke linker simulatorpanelen':'Onafhankelijke rechter simulatorpanelen');
   const panel=document.createElement('aside');panel.className='vl-dock-panel';
   dock.append(rail,panel);
-  const buttons=[],panes=[];
-  specs.forEach(spec=>{
+  const entries=new Map();
+  specs.forEach((spec,index)=>{
     const made=vlMakePane('vl-'+side+'-'+spec.key,spec.title,spec.subtitle,spec.nodes);
-    panel.appendChild(made.pane);panes.push(made.pane);
-    const b=document.createElement('button');b.type='button';b.className='vl-dock-tab';b.dataset.key=spec.key;
+    panel.appendChild(made.pane);
+    const b=document.createElement('button');b.type='button';b.className='vl-dock-tab';b.dataset.key=spec.key;b.id='vl-'+side+'-'+spec.key+'-toggle';
     b.innerHTML='<span class="vl-tab-icon" aria-hidden="true">'+spec.icon+'</span><span>'+spec.short+'</span>';
-    b.title=spec.tooltip;b.setAttribute('role','tab');b.setAttribute('aria-controls',made.pane.id);b.setAttribute('aria-selected','false');
-    rail.appendChild(b);buttons.push(b);made.close.addEventListener('click',()=>setOpen(false));
+    b.title=spec.tooltip;b.setAttribute('aria-controls',made.pane.id);b.setAttribute('aria-pressed','false');
+    made.pane.setAttribute('aria-labelledby',b.id);rail.appendChild(b);
+    entries.set(spec.key,{spec,pane:made.pane,button:b,close:made.close,index,open:false});
   });
-  let activeKey=defaultKey;
-  try{activeKey=localStorage.getItem('vortexlab.dual.'+side+'.tab')||defaultKey;}catch(_){ }
-  if(!specs.some(spec=>spec.key===activeKey))activeKey=defaultKey;
-  let open=true;
-  try{const stored=localStorage.getItem('vortexlab.dual.'+side+'.open');if(stored!==null)open=stored==='1';}catch(_){ }
-  function activate(key,focus=false,notify=true){
-    const idx=Math.max(0,specs.findIndex(s=>s.key===key));activeKey=specs[idx].key;
-    buttons.forEach((b,i)=>{const on=i===idx;b.classList.toggle('active',on);b.setAttribute('aria-selected',on?'true':'false');b.tabIndex=on?0:-1;});
-    panes.forEach((p,i)=>p.classList.toggle('active',i===idx));
-    try{localStorage.setItem('vortexlab.dual.'+side+'.tab',activeKey);}catch(_){ }
-    if(focus)buttons[idx].focus({preventScroll:true});
-    if(notify&&typeof onActivate==='function')onActivate(activeKey);
-  }
-  function setOpen(value){open=!!value;dock.classList.toggle('is-open',open);try{localStorage.setItem('vortexlab.dual.'+side+'.open',open?'1':'0');}catch(_){ }window.vlUpdateOverlayOffsets?.();}
-  buttons.forEach((b,i)=>{
-    b.addEventListener('click',()=>{if(activeKey===specs[i].key&&open)setOpen(false);else{activate(specs[i].key);setOpen(true);window.vlEnforceDockSpace?.(side);}});
-    b.addEventListener('keydown',e=>{
-      let next=null;if(e.key==='ArrowDown'||e.key==='ArrowRight')next=i+1;else if(e.key==='ArrowUp'||e.key==='ArrowLeft')next=i-1;else if(e.key==='Home')next=0;else if(e.key==='End')next=buttons.length-1;
-      if(next!==null){e.preventDefault();next=(next+buttons.length)%buttons.length;activate(specs[next].key,true);setOpen(true);}
+  const slotOrder=(outerToInnerOrder||specs.map(s=>s.key)).filter(key=>entries.has(key));
+  function storedOpen(key,fallback){
+    try{const raw=localStorage.getItem('vortexlab.quad.'+side+'.'+key+'.open');if(raw!==null)return raw==='1';}catch(_){ }
+    return fallback;
+  }
+  entries.forEach((entry,key)=>{entry.open=storedOpen(key,defaultOpenKeys.includes(key));});
+  function layout(){
+    const visible=slotOrder.filter(key=>entries.get(key)?.open);
+    visible.forEach((key,slot)=>{
+      const entry=entries.get(key);entry.pane.dataset.slot=String(slot);
+      const offset=slot===0?'0px':'var(--vl-panel-w)';
+      if(side==='left'){entry.pane.style.left=offset;entry.pane.style.right='auto';}
+      else{entry.pane.style.right=offset;entry.pane.style.left='auto';}
+    });
+    entries.forEach(entry=>{
+      entry.pane.classList.toggle('vl-pane-open',entry.open);
+      entry.button.classList.toggle('active',entry.open);
+      entry.button.setAttribute('aria-pressed',entry.open?'true':'false');
+    });
+    dock.dataset.openCount=String(visible.length);
+    window.vlUpdateOverlayOffsets?.();
+  }
+  function setOpen(key,value){
+    const entry=entries.get(key);if(!entry)return;
+    entry.open=!!value;
+    try{localStorage.setItem('vortexlab.quad.'+side+'.'+key+'.open',entry.open?'1':'0');}catch(_){ }
+    layout();
+  }
+  function toggle(key){const entry=entries.get(key);if(entry)setOpen(key,!entry.open);}
+  const buttons=[...entries.values()].map(entry=>entry.button);
+  entries.forEach((entry,key)=>{
+    entry.button.addEventListener('click',()=>toggle(key));
+    entry.close.addEventListener('click',()=>setOpen(key,false));
+    entry.button.addEventListener('keydown',e=>{
+      const i=buttons.indexOf(entry.button);let next=null;
+      if(e.key==='ArrowDown'||e.key==='ArrowRight')next=i+1;
+      else if(e.key==='ArrowUp'||e.key==='ArrowLeft')next=i-1;
+      else if(e.key==='Home')next=0;else if(e.key==='End')next=buttons.length-1;
+      if(next!==null){e.preventDefault();next=(next+buttons.length)%buttons.length;buttons[next].focus({preventScroll:true});}
     });
   });
-  activate(activeKey,false,false);setOpen(open);
-  return {dock,setOpen,isOpen:()=>open,activate,getActive:()=>activeKey};
+  layout();
+  return {
+    dock,setOpen,toggle,closeAll:()=>{entries.forEach((_,key)=>setOpen(key,false));},
+    isOpen:key=>!!entries.get(key)?.open,getOpenCount:()=>[...entries.values()].filter(entry=>entry.open).length,
+    getOpenKeys:()=>slotOrder.filter(key=>entries.get(key)?.open)
+  };
 }
 function vlInitTooltips(root){
   const tip=document.createElement('div');tip.className='vl-tooltip';tip.id='vlTooltip';tip.setAttribute('role','tooltip');document.body.appendChild(tip);
@@ -6012,7 +6053,7 @@
   document.body.dataset.vlDualUi='1';document.body.classList.add('vl-dual-ui');
   const shell=document.createElement('div');shell.className='vl-ui-shell';shell.id='vlUiShell';
   const top=document.createElement('div');top.className='vl-topbar';top.id='vlTopRunBar';
-  const title=document.createElement('div');title.className='vl-topbar-title';title.textContent='RUN';top.appendChild(title);
+  const title=document.createElement('div');title.className='vl-topbar-title';title.textContent='VORTEXLAB';top.appendChild(title);
   const reset=vlNode('bReset'),resetParticles=vlNode('bResetParticles'),pause=vlNode('bPause');
   vlMakeHiddenOriginal(reset,'↺ Vortex');vlMakeHiddenOriginal(resetParticles,'↺ Deeltjes');
   if(reset){reset.title='Reset de vortexgeometrie naar de beginconfiguratie';top.appendChild(reset);}
@@ -6022,7 +6063,9 @@
   vlMoveSelectToTopbar('presetSelect','Preset',top,'vl-preset-group');
   vlCreateProxySelect('qualSeg','qual','Kwaliteit',top);
   vlCreateProxySelect('medSeg','med','Medium',top);
-  const viewDropdown=vlCreateHeaderDropdown('VIEW',vlNode('collVis'),shell);if(viewDropdown)top.appendChild(viewDropdown.button);
+  const foot=vlNode('footNote'),runBody=document.querySelector('#collRun > .coll-body');if(foot&&runBody)runBody.appendChild(foot);
+  const runDropdown=vlCreateHeaderDropdown('RUN',vlNode('collRun'),shell,'run','Scenario, tijdomkering, logging en benchmarkbediening');if(runDropdown)top.appendChild(runDropdown.button);
+  const viewDropdown=vlCreateHeaderDropdown('VIEW',vlNode('collVis'),shell,'view','Weergave-instellingen');if(viewDropdown)top.appendChild(viewDropdown.button);
   shell.insertBefore(top,shell.firstChild);
 
   // Informatiepaneel: de vroegere permanente linker overlay wordt volledig behouden,
@@ -6049,33 +6092,26 @@
   const autoRelax=document.querySelector('#collStability .auto-relax-row');if(autoRelax&&vortexFlags)vortexFlags.appendChild(autoRelax);
   const topology=vlNode('cTopologyGuard')?.parentElement;if(topology&&vortexFlags)vortexFlags.appendChild(topology);
 
-  const foot=vlNode('footNote'),runBody=document.querySelector('#collRun > .coll-body');if(foot&&runBody)runBody.appendChild(foot);
   const leftSpecs=[
-    {key:'info',title:'INFORMATIE & OVERZICHT',short:'INFO',icon:'ⓘ',subtitle:'metingen',tooltip:'HUD, topologiekaarten, sparkline en live stabiliteitsinformatie. Activeert gelijktijdig DIAG rechts.',nodes:[info]},
-    {key:'flow',title:'MODEL · FLOW + CILINDER',short:'FLOW',icon:'≈',subtitle:'links · gecombineerd',tooltip:'Compacte cilindergeometrie plus interactie, achtergrondstroming, axiale drift, vortexbundel, potential flow en wederzijdse wrijving. Activeert VORTEX + KERN rechts.',nodes:[vlNode('subCylinder'),vlNode('subFlow'),vlNode('potentialFlowPanel'),vlNode('collFriction')]},
-    {key:'run',title:'SCENARIO & RUN',short:'RUN',icon:'▶',subtitle:'uitgebreid',tooltip:'Tijdomkering en model logging; primaire runbediening staat in de header.',nodes:[vlNode('collRun')]}
+    {key:'info',title:'INFORMATIE & OVERZICHT',short:'INFO',icon:'ⓘ',subtitle:'buiten · links',tooltip:'Open of sluit INFO onafhankelijk. Wanneer FLOW ook open is, staat INFO links buiten FLOW.',nodes:[info]},
+    {key:'flow',title:'MODEL · FLOW + CILINDER',short:'FLOW',icon:'≈',subtitle:'binnen · links',tooltip:'Open of sluit FLOW onafhankelijk. FLOW is het linker paneel dat het dichtst bij het canvas staat.',nodes:[vlNode('subCylinder'),vlNode('subFlow'),vlNode('potentialFlowPanel'),vlNode('collFriction')]}
   ];
   const rightSpecs=[
-    {key:'core',title:'MODEL · VORTEX + KERN',short:'KERN',icon:'◉',subtitle:'rechts · gecombineerd',tooltip:'Topologie, dragerconfiguratie, oriëntatie, centrum-lock, topology guard, kernmodel, circulatie, quanta en SST-schalen. Activeert FLOW + CILINDER links.',nodes:[vlNode('subVortex'),vlNode('subCore')]},
-    {key:'diag',title:'DIAGNOSTIEK',short:'DIAG',icon:'∑',subtitle:'metingen',tooltip:'Geometrische descriptoren, SST Research-Track, speculative swirl clock, stretch-gate en BEM/diagnostische uitlezingen. Activeert INFO links.',nodes:[vlNode('collDiagnostics'),vlNode('sstResearchDiagnostics'),vlNode('chiPhasePanel'),vlNode('specClockPanel'),vlNode('stretchGatePanel')]}
+    {key:'core',title:'MODEL · VORTEX + KERN',short:'KERN',icon:'◉',subtitle:'binnen · rechts',tooltip:'Open of sluit KERN onafhankelijk. KERN is het rechter paneel dat het dichtst bij het canvas staat.',nodes:[vlNode('subVortex'),vlNode('subCore')]},
+    {key:'diag',title:'DIAGNOSTIEK',short:'DIAG',icon:'∑',subtitle:'buiten · rechts',tooltip:'Open of sluit DIAG onafhankelijk. Wanneer KERN ook open is, staat DIAG rechts buiten KERN.',nodes:[vlNode('collDiagnostics'),vlNode('sstResearchDiagnostics'),vlNode('chiPhasePanel'),vlNode('specClockPanel'),vlNode('stretchGatePanel')]}
   ];
-  let left=null,right=null,pairing=false;
-  const fromLeft=key=>{if(pairing||!right)return;const target=key==='info'?'diag':(key==='flow'?'core':null);if(!target)return;pairing=true;right.activate(target,false,false);right.setOpen(true);pairing=false;};
-  const fromRight=key=>{if(pairing||!left)return;const target=key==='diag'?'info':(key==='core'?'flow':null);if(!target)return;pairing=true;left.activate(target,false,false);left.setOpen(true);pairing=false;};
-  left=vlCreateDock('left',leftSpecs,'flow',fromLeft);right=vlCreateDock('right',rightSpecs,'core',fromRight);
+  const left=vlCreateIndependentDock('left',leftSpecs,['flow'],['info','flow']);
+  const right=vlCreateIndependentDock('right',rightSpecs,['core'],['diag','core']);
+  window.vlPanelLayout={left,right};
   window.vlUpdateOverlayOffsets=()=>{
-    const leftEdge=left?.isOpen()
-      ?'calc(var(--vl-rail-w) + var(--vl-panel-w) + 12px)'
-      :'calc(var(--vl-rail-w) + 12px)';
-    document.documentElement.style.setProperty('--vl-overlay-left',leftEdge);
+    const leftCount=left?.getOpenCount?.()||0,rightCount=right?.getOpenCount?.()||0;
+    const panelOffset=count=>count<=0?'0px':(count===1?'var(--vl-panel-w)':'calc(var(--vl-panel-w) + var(--vl-panel-w))');
+    document.documentElement.style.setProperty('--vl-overlay-left','calc(var(--vl-rail-w) + '+panelOffset(leftCount)+' + 12px)');
+    document.documentElement.style.setProperty('--vl-overlay-right','calc(var(--vl-rail-w) + '+panelOffset(rightCount)+' + 12px)');
   };
   window.vlUpdateOverlayOffsets();
-  if(left.getActive()==='info'||left.getActive()==='flow')fromLeft(left.getActive());
   shell.append(left.dock,right.dock);document.body.appendChild(shell);
-  window.vlEnforceDockSpace=side=>{if(innerWidth<1080){if(side==='left')right.setOpen(false);else left.setOpen(false);}};
-  window.addEventListener('resize',()=>{if(innerWidth<1080&&left.isOpen()&&right.isOpen())right.setOpen(false);},{passive:true});
-  if(innerWidth<1080&&left.isOpen()&&right.isOpen())right.setOpen(false);
-  document.addEventListener('keydown',e=>{if(e.key==='Escape'){left.setOpen(false);right.setOpen(false);}});
+  document.addEventListener('keydown',e=>{if(e.key==='Escape'){left.closeAll();right.closeAll();}});
 
   // Oude structuur en alle oorspronkelijke teksten blijven in de DOM als bron/provenance.
   const preserved=document.createElement('div');preserved.id='vlPreservedSources';preserved.className='vl-preserved-sources';
@@ -6953,6 +6989,14 @@
        add('T0z eerste-stap-CFL kent opgelegde driftsnelheid',Math.abs(ub-.005)<1e-15,'u_bound='+ub.toExponential(3));
        add('T0aa SPEC CLOCK tijdstapcap 50 ms',Math.abs(cap-SPEC_CLOCK_MAX_ACCEPTED_DT)<1e-15,'dt_cap='+cap.toFixed(3)+' s');
        P.mode=oldMode;P.vzA=oldA;P.vzB=oldB;P.lockVz=oldLock;P.specClockEnabled=oldClock;}
+      {const layout=window.vlPanelLayout,runMenu=document.getElementById('vlHeaderRunMenu'),viewMenu=document.getElementById('vlHeaderViewMenu');
+       const keys=['info','flow','core','diag'],toggles=keys.map(key=>document.querySelector('.vl-dock-tab[data-key="'+key+'"]'));
+       const before={info:layout?.left.isOpen('info'),flow:layout?.left.isOpen('flow'),core:layout?.right.isOpen('core'),diag:layout?.right.isOpen('diag')};
+       if(layout){layout.left.setOpen('info',!before.info);}
+       const independent=!!layout&&layout.right.isOpen('diag')===before.diag;
+       if(layout){layout.left.setOpen('info',before.info);layout.left.setOpen('flow',before.flow);layout.right.setOpen('core',before.core);layout.right.setOpen('diag',before.diag);}
+       const structure=toggles.every(Boolean)&&!document.querySelector('.vl-dock-tab[data-key="run"]')&&!!runMenu&&!!viewMenu&&runMenu!==viewMenu;
+       add('T0ab vier panelen onafhankelijk + RUN/VIEW afzonderlijk in header',structure&&independent,'structure='+structure+' independent='+independent);}
       P.ccwA=true;P.ccwB=false;const orient=relativeCarrierOrientationSign();
       add('T0h relatieve drageroriëntatie',orient===-1,'s_A*s_B='+orient);
       // T0i — v7.5: verse-startdefaults (D4 + frame-ontvlechting), bevroren bij scriptparse
