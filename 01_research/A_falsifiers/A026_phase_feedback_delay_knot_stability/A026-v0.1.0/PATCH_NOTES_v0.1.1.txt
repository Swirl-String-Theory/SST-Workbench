SST Phase-Feedback-Delay Knot Stability Blind Falsifier v0.1.1

Fixes:
- Reuse initialized MSVC environment via DISTUTILS_USE_SDK=1.
- Direct vcvarsall initialization only when cl.exe is not already on PATH.
- Treat cl.exe + link.exe availability as the vcvarsall success gate.
- Avoid setuptools nested cmd /u /c vcvarsall failure seen on target Windows.
- Add robust KnotPlot matrix path resolution for one/two-level-deep workbench layouts.
- Keep native import, parity and pytest as hard gates.

Scientific model/gates unchanged from v0.1.0.
