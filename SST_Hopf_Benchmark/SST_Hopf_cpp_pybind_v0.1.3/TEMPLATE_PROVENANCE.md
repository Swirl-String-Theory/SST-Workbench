# Template provenance

Base workflow: `SST_cpp_pybind_audit_template.zip`.

Preserved design features:

- `cpp/native.cpp` pybind kernel;
- Python package loader;
- hash-based build stamp;
- compiler -> setuptools fallback;
- pure-Python fallback philosophy;
- `run_*` command-line workflow.

Project-specific package name: `sst_hopf_native`.
